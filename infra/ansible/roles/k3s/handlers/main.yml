---
- name: Apply K3s kernel settings
  ansible.builtin.command: /usr/sbin/sysctl --system
  changed_when: true

- name: Reload systemd
  ansible.builtin.systemd_service:
    daemon_reload: true

- name: Restart K3s
  ansible.builtin.systemd_service:
    name: k3s
    state: restarted
