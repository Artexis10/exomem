{{- if and (eq .Values.workloadMode "serve") .Values.routes.enabled }}
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: {{ .Values.resourceName }}-strip-cell
  labels:
    {{- include "exomem-cell.labels" . | nindent 4 }}
spec:
  stripPrefix:
    prefixes:
      - /cells/{{ .Values.cellId }}
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: {{ .Values.resourceName }}-control
  labels:
    {{- include "exomem-cell.labels" . | nindent 4 }}
spec:
  entryPoints: [web]
  routes:
    - kind: Rule
      match: Host(`{{ .Values.routes.controlHostname }}`) && PathPrefix(`/cells/{{ .Values.cellId }}/private/exomem/v1`)
      middlewares:
        - name: {{ .Values.resourceName }}-strip-cell
      services:
        - name: {{ .Values.resourceName }}
          port: 8765
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: {{ .Values.resourceName }}-transfer
  labels:
    {{- include "exomem-cell.labels" . | nindent 4 }}
spec:
  entryPoints: [web]
  routes:
    - kind: Rule
      match: Host(`{{ .Values.routes.transferHostname }}`) && (Path(`/cells/{{ .Values.cellId }}/public/exomem/v2/transfers/upload`) || Path(`/cells/{{ .Values.cellId }}/public/exomem/v2/transfers/download`))
      middlewares:
        - name: {{ .Values.resourceName }}-strip-cell
      services:
        - name: {{ .Values.resourceName }}
          port: 8765
{{- end }}
