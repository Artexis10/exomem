## ADDED Requirements

### Requirement: No-nudge scenario families f20–f26

The bench SHALL define seven pre-registered families as dated §7 amendment entries under the accepted post-ratification governance, each with deterministic assertions from the closed registry, explicit negative controls, and a named mechanism-removal condition that must turn the family red.

- `f20 structural_emergence` (corpus): a note accumulating structurally distinct durable-unit clusters across successive writes SHALL surface a promotion-class signal within a frozen phase budget. The fixture SHALL bind to structure — categories, anchors, disjoint link neighborhoods — and never to fixture vocabulary; the generator SHALL assert no cluster-name token appears in any assertion parameter. Four frequency- and length-matched twins SHALL stay quiet: a bounded-scope page, a page with one or two tangents, a deliberate hub, and a legitimately heterogeneous log page whose high dispersion is intentional.
- `f21 entity_emergence` (corpus): an identity recurring with reusable facts across at least three distinct sources SHALL surface an entity-candidate signal; a frequency-matched incidental-mention twin SHALL stay quiet, so a string-frequency mutant cannot pass both. Fixtures SHALL include lowercase and non-Latin referents so script bias is measured rather than hidden.
- `f22 unsolicited_contradiction` (corpus): evidence materially invalidating a durable conclusion SHALL surface the pair without any topical agent turn in the trajectory; a concordant-evidence twin in the same similarity band SHALL stay quiet. Surfacing-route provenance SHALL be reported, never asserted.
- `f23 dismissal_respect` (operational): a dismissed fingerprint SHALL NOT reappear across repeated maintenance passes, an engine restart, and prominence reconfiguration spanning the full level range, while a material change SHALL still reopen the item. The family SHALL also assert counter-emission governance: a bulk batch of writes SHALL NOT emit one identical counters block per write.
- `f24 fresh_session_reconstruction` (operational): the continuation packet SHALL contain every seeded decision unit, open question, and latest plan state by reference, SHALL exclude a same-sized foreign-project decoy set entirely, and SHALL respect a frozen size budget.
- `f25 restructure_lifecycle` (operational): after a suggested restructure is applied, the original structural signal SHALL be absent from every open view while the review state holds no dismissal for it, and no merge- or consolidation-class signal SHALL target the new children within the frozen window.
- `f26 hookless_episode_carrier` (operational journey): on a hookless, compact-detail surface, a conversational durable conclusion followed by an abrupt session end and a fresh session SHALL demonstrate the full carrier path — the due-state block present in the actual compact responses, the capture landed, and reconstruction succeeding — with deterministic assertions on carrier presence.

Unprompted-ness SHALL be a trajectory property: the scenario proves the user never asked by containing zero topical agent turns between evidence ingest and the asserted snapshot.

#### Scenario: The structural-emergence positive fires within budget while all four twins stay quiet

- **WHEN** the f20 corpus advances through its scripted writes and maintenance passes
- **THEN** a promotion-class signal targeting the accumulating note is present in an open view at or before the frozen budget phase
- **AND** no promotion-class signal targets the bounded-scope, tangent, hub, or heterogeneous-log twin at any snapshot

#### Scenario: A mechanism removal turns the family red

- **WHEN** the detector, consumer, or carrier a family names is disabled and the family re-runs
- **THEN** the family's positive assertions fail
- **AND** the grep-markdown and no-memory controls remain red, so a control pass can never substitute for the mechanism

#### Scenario: The carrier journey proves delivery on a thin client

- **WHEN** the f26 journey runs on a hookless surface with compact response detail
- **THEN** the due-state block is present in the compact mutation and recall responses the journey actually received
- **AND** the fresh session's reconstruction assertions pass against the packet artifact

### Requirement: Quiet assertions prove absence on every declared surface

The registry SHALL include the meta-predicate `signal_absence_checked_across_all_surfaces`. Every negative-control assertion SHALL compose it: absence SHALL be established in each surface the fairness packet declares — audit findings, review queues, proposal queues, and the due-state counters block — and an empty or missing projection SHALL evaluate as an error, never as a pass.

#### Scenario: Relocating a nag does not pass a quiet assertion

- **WHEN** a product variant emits no queue item for a twin but names it in the counters block
- **THEN** the twin's quiet assertion fails

#### Scenario: A missing projection cannot fake silence

- **WHEN** a declared surface cannot be projected for a snapshot
- **THEN** the quiet assertion evaluates as an error rather than a pass

### Requirement: The false-positive ceiling is declared and hard

Zero promotion-class signals on any negative twin SHALL be a hard in-family assertion for f20 and f21. The production false-positive budget — the rate of dismissals carrying the irrelevant reason — SHALL receive its threshold at calibration and SHALL be reported beside, never instead of, the in-family ceiling. An automation metric SHALL NOT be reported without its paired false-positive dual from the same runs, and no aggregate score SHALL exist.

#### Scenario: A metric cannot publish without its dual

- **WHEN** a run report includes a detection-latency or interventions-per-journey value
- **THEN** the same report includes the paired false-positive measure from the same runs

### Requirement: Emergence budget constants are calibrated once, then frozen and versioned

The f20, f21, and f25 budget and window constants SHALL be frozen in the amendment after a one-time calibration study in which at least three expert annotators label the emergence corpora for the intervention point, taking the median. The study protocol and its raw labels SHALL ship with the judge-agreement assets. Changing a frozen constant SHALL require a new dated §7 amendment with its own receipt; no runtime, fixture, or report path may retune a constant silently. Live judging of the intervention point SHALL NOT occur in any run.

#### Scenario: A constant changes only by amendment

- **WHEN** a later change proposes a different budget constant
- **THEN** the change lands as a new dated amendment with its own receipt
- **AND** runs pin the amendment revision they executed under

### Requirement: Scenario operations cover maintenance, triage, restructure, and configuration

The scenario operation vocabulary SHALL add `maintenance_pass` (run the documented maintenance and attention sweep), `triage_decision` (dismiss, snooze, or reopen a surfaced item through the documented surface), `apply_restructure` (execute a suggested restructure through the documented interface), and `configure` (set a documented engagement level). Families defined by this amendment SHALL use these operations; existing families SHALL be unaffected.

#### Scenario: A journey scripts a dismissal and a reconfiguration

- **WHEN** an f23 scenario executes `triage_decision` then `configure` operations
- **THEN** each operation routes through the product's documented surface
- **AND** the snapshot sequence records the resulting open-view state for assertion

### Requirement: Sequence-2 families are expected red and withheld until acknowledgment

The amendment SHALL record that f20, f21, and f22 positives are expected to fail on the current runtime, naming them falsification targets for the no-nudge programme. The accepted registration-is-not-release gate SHALL withhold every sequence-2 family from comparative runs, scores, and claims until the founder acknowledgment lands. The amendment SHALL add no catastrophic assertions; it SHALL extend the scope of the existing `no_retired_state_served_as_current` catastrophic assertion to the continuation packet and SHALL state that extension explicitly.

#### Scenario: An unacknowledged family cannot enter a comparative claim

- **WHEN** a comparative run executes before the sequence-2 acknowledgment exists
- **THEN** the sequence-2 families are withheld from that run's scores and claims
- **AND** the withholding is reported, never silent
