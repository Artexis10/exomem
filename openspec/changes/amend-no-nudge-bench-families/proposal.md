## Why

The 2026-08-15 no-nudge architecture audit committed Exomem to becoming measurably more automatic: structural emergence surfaced without the user asking, entities recognized from recurrence, contradictions surfaced unprompted, dismissals respected forever, fresh sessions reconstructed from durable state, and restructures that clear their own signals without churn. None of that is falsifiable today — the epistemic-state bench's ratified families f01–f19 cover loop representation and closure, not autonomous surfacing, nag governance, or the delivery carrier. Contracts-first sequencing is the house rule the f15–f19 amendment established: the measurement files before the machinery, so the detectors that follow are built against pre-registered acceptance rather than tuned to pass improvised tests. The flagship positives are expected red on today's runtime; that is the point — they are falsification targets for the no-nudge programme, withheld from comparative claims until founder acknowledgment exactly as sequence 1 prescribes.

## What Changes

- File amendment sequence 2 to the ratified pre-registration through the accepted post-ratification governance: families f20–f26, their assertions, and their scenario-schema additions land as dated §7 amendments with an amendment receipt, withheld from runs, scores, and claims until founder acknowledgment.
- Add seven families: `structural_emergence` (France-shaped positive plus four frequency- and length-matched negative twins including the legitimately heterogeneous log page), `entity_emergence`, `unsolicited_contradiction`, `dismissal_respect` (including counter-repetition governance under bulk writes), `fresh_session_reconstruction` (continuation-packet containment and exclusion by ref), `restructure_lifecycle` (cleared-by-state-change, zero churn), and `hookless_episode_carrier` (the end-to-end thin-client episode).
- Register the new closed-set assertions, headlined by the anti-vacuity meta-predicate `signal_absence_checked_across_all_surfaces`: a quiet assertion passes only when absence is proven on every declared surface, including the due-state counters block.
- Extend the scenario operation vocabulary with `maintenance_pass`, `triage_decision`, `apply_restructure`, and `configure`.
- Declare the false-positive ceiling: zero promotion-class signals on any negative twin is a hard in-family assertion.
- Freeze the emergence budget constants through a one-time three-expert calibration study whose data ships with the judge-agreement assets; recalibration requires a new dated amendment, never silent retuning.
- Add no catastrophic assertions; extend the scope of the existing `no_retired_state_served_as_current` catastrophic assertion to the continuation packet.
- Keep the agent-judged dimensions (intervention usefulness, continuation sufficiency) in the membench agent track behind the judge-agreement gate, outside the pre-registered deterministic set.

## Capabilities

### Modified Capabilities

- `epistemic-state-bench`: seven no-nudge families with pre-registered assertions, negative twins, a declared false-positive ceiling, new scenario operations, calibrated-then-frozen budget constants, and expected-red status on the current runtime, all landing through the accepted amendment governance.

## Impact

The pre-registration document (§7 amendment entries and a sequence-2 amendment receipt), the assertion registry and scenario schema mirrors, red fixtures for every refusal path, fixture corpora for the positives and twins, the calibration-study protocol and its judge-agreement assets, and one scripted track-D journey for the carrier family. No product code changes; no tool, schema, or envelope changes; the withhold gate keeps every new family out of comparative results until acknowledgment. The bench families run against the local runtime by design: a capability that only passes on Hosted is a spec violation.

Deliberately out of scope: any detector, consumer, or carrier implementation (those are separate changes built against these contracts); plan-versus-reality assertions (family f16 owns them — only a latency readout over its existing trajectories is added); live judging of "when would an expert intervene" (frozen constants replace it after calibration).
