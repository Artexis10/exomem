## 0. Delegation Intake And Prerequisites

- [x] 0.1 Obtain explicit approval for the exact `bound-corpus-context-flight-join` revision: replace its unresolved duration with one shared non-configurable 2.0-second caller-derived budget, add the already-settled/no-flight immediate fast path, add a red test that proves no wait call occurs, and require mutation proof that deleting the fast path makes that test fail; apply only that approved artifact revision and verify `openspec validate bound-corpus-context-flight-join --strict` passes.
- [ ] 0.2 Reconcile the implementation and closure evidence for `shorten-mutation-critical-section`, `rebuild-graph-without-blocking-writes`, `fix-deferred-work-drain`, `converge-graph-incrementally`, `bound-graph-recovery-funnel`, and `bound-corpus-context-flight-join`; sync/archive only changes whose required code, tests, merge, and live evidence are complete, then verify `openspec validate --all --strict` passes and no prerequisite leaves overlapping unmerged edits.
- [ ] 0.3 Freeze a clean `origin/main` base SHA after prerequisite reconciliation, record the owning integration worktree, and verify `git status --short`, `git worktree list --porcelain`, and `git branch --contains <base-sha>` show an uncontested base with no shared-primary implementation work.
- [ ] 0.4 Prepare one delegation packet for each of Lanes 1–5 from this approved OpenSpec change, pinning the DAG `1 → {2,3,4} → 5`, the Lane 1 protocol surface, base SHA, scope allowlist, guarded files, named red-first nodes, full guard/mutant matrix, exact commands, and fresh reviewer assignment; run `scripts/codex_task.sh verify <lane-worktree>` for every prepared worktree and verify no packet has an unresolved decision, `TBD`, overlapping writable path, or missing acceptance gate before any worker starts.

## 1. Lane 1 — Receipt Lifecycle And Restart Recovery

- [ ] 1.1 In an isolated foundation worktree, add red tests in `tests/test_derived_batch_receipts.py` for additive schema migration, bounded/content-free rows, exact component enumeration, stable advisory result ids, state-root placement, and refusal before canonical replacement when durable preparation is unavailable; verify the new tests fail for the intended missing behavior before implementation.
- [ ] 1.2 Implement the additive derived-batch, path, component, pending-recall, and advisory-result schema plus typed public receipt interfaces in `src/exomem/deferred_index.py` and narrowly scoped new modules; verify the red schema/preparation tests pass and legacy semantic, graph, and full-upsert rows remain readable and drainable.
- [ ] 1.3 Add red crash-cut tests for before-state, exact after-state, mixed state, supersession, stale generation, expired claim, and newer-revision CAS protection; implement proof, claim, retry/backoff, abort, supersede, and completion transitions, then verify only exact current after-state can publish or clear its receipt.
- [ ] 1.4 Add red restart/fairness tests proving pending components schedule on server start, rotate after failure, obey resource modes without halting correctness work, and never mint redundant full-upsert debt when exact custody exists; implement bounded scheduling integration and verify `env EXOMEM_STATE_ROOT="$PWD/.task/state-lane1" uv run pytest --basetemp="$PWD/.task/pytest-lane1" -q tests/test_derived_batch_receipts.py tests/test_deferred_index.py tests/test_deferred_work_drain.py tests/test_graph_deferred_queue.py` passes.
- [ ] 1.5 Run receipt mutation proofs from a harness-owned source copy: remove pre-commit preparation and require `test_receipt_prepare_failure_leaves_canonical_untouched` red, bypass after-hash proof and require `test_mixed_or_stale_state_never_activates_components` red, and bypass completion CAS and require `test_older_revision_cannot_clear_newer_custody` red; restore the lane, rerun those exact nodes green with `env EXOMEM_STATE_ROOT="$PWD/.task/state-lane1" uv run pytest --basetemp="$PWD/.task/pytest-lane1" -q tests/test_derived_batch_receipts.py`, run `uv run ruff check src/exomem tests/test_derived_batch_receipts.py` and `git diff --check`, and record outputs.
- [ ] 1.6 Assign an author-independent fresh reviewer to inspect the actual foundation diff for crash safety, state placement, privacy, mixed-version migration, CAS lineage, and task scope; apply any accepted corrections and require the same reviewer to recheck the corrected diff before the integration owner accepts the lane commit.

## 2. Lane 2 — Exact Pending Read-Your-Write

- [ ] 2.1 From the accepted Lane 1 SHA, add red tests in `tests/test_pending_recall_delta.py` for immediate create/edit/delete/move visibility by direct path, stable ref, keyword, and hybrid recall; require stale pre-edit/deleted rows to be suppressed and moved identities to appear once, then verify the tests fail for the missing pending overlay.
- [ ] 2.2 Implement an O(changed-paths) pending-recall delta and merge/shadow semantics behind Lane 1's frozen pending-visibility protocol in a dedicated module plus `freshness.py`, `find.py`, `lexstore.py`, and `memory_refs.py`; verify `env EXOMEM_STATE_ROOT="$PWD/.task/state-lane2" uv run pytest --basetemp="$PWD/.task/pytest-lane2" -q tests/test_pending_recall_delta.py tests/test_read_after_write_visibility.py tests/test_lexical_deferred_upsert.py tests/test_records_recall_index_sync.py` passes without a corpus walk on the write path.
- [ ] 2.3 Add red restart, overflow, corrupt-row, superseding-write, and publication-order tests; implement startup hydration before managed recall readiness, exact-generation retirement after persistent publication, and typed warming/unavailable failure when complete coverage cannot be proven, then verify no restart path silently serves stale catalogue state.
- [ ] 2.4 Add authorization-projection tests proving pending rows are selected and released under the current request's policy while L0/withheld content is absent before scoring/caps; verify pending operational records expose no content through status or telemetry with `env EXOMEM_STATE_ROOT="$PWD/.task/state-lane2" uv run pytest --basetemp="$PWD/.task/pytest-lane2" -q tests/test_pending_recall_delta.py tests/test_governance_projection_authorization.py tests/test_governance_projection_wire_gate.py tests/test_projection_lag_tolerance.py`.
- [ ] 2.5 Run overlay mutation proofs in a harness-owned source copy: remove stale-row shadowing and require `test_pending_edit_delete_hides_stale_rows` red; bypass overflow/hydration failure and require `test_unprovable_pending_overflow_returns_warming` red; remove startup hydration and require `test_restart_hydrates_pending_before_ready` red. Restore the lane, run `env EXOMEM_STATE_ROOT="$PWD/.task/state-lane2" uv run pytest --basetemp="$PWD/.task/pytest-lane2" -q tests/test_pending_recall_delta.py`, `uv run ruff check src/exomem tests/test_pending_recall_delta.py`, and `git diff --check`, and record outputs.
- [ ] 2.6 Assign an author-independent fresh reviewer to attack exactness across create/edit/delete/move/restart, authorization projection, deduplication, overflow, and publication ordering; apply accepted corrections and require reviewer recheck before lane acceptance.

## 3. Lane 3 — Canonical Acknowledgement And Terminal Contract

- [ ] 3.1 From the accepted Lane 1 SHA, use its frozen protocol fakes to add red public-leaf tests in `tests/test_fast_write_ack.py` that inject slow graph, embedding, claim, lexical, resolver, and advisory components and prove canonical success can return with exact custody; add `test_receipt_prepare_failure_leaves_canonical_untouched` and `test_slow_component_runs_after_authority_release`, then verify the new nodes fail for the intended missing behavior.
- [ ] 3.2 Integrate prepared receipt creation before the first canonical replacement and exact post-commit proof/handoff through `vault.py`, `writer_lease.py`, `index_sync.py`, and the governed write leaves; preserve validation, graph epoch, governance catalogue, rollback, fencing, idempotency, and committed-uncertain semantics, then verify failure-injection and existing transactional tests pass.
- [ ] 3.3 Add red `test_components_share_one_absolute_post_commit_deadline`, `test_real_custody_failure_is_not_laundered_to_pending`, and already-settled/no-flight tests; implement one fixed 2.0-second `deadline_monotonic` shared across every optional post-canonical observation with no configurable extension, and verify sequential slow components cannot multiply it while real failures remain real.
- [ ] 3.4 Add red compact/full/legacy projection and exact-retry tests for `derived_sync`, `derived_sync_components`, preserved `graph_sync`, `advisory_sync`, result-ref presence for completed/pending/failed but not-required absence, ref retention for terminal replay, closed failure code/next action, and immutable original terminal; implement projection and verify `env EXOMEM_STATE_ROOT="$PWD/.task/state-lane3" uv run pytest --basetemp="$PWD/.task/pytest-lane3" -q tests/test_fast_write_ack.py tests/test_mutation_terminal.py tests/test_graph_idempotency_handoff.py tests/test_bounded_graph_join.py` passes.
- [ ] 3.5 Cover every governed semantic write route with a parameterized handoff test using the Lane 1 protocol fakes, including compiled remember/add/edit/replace/observe paths and batch callers; verify each route prepares custody once, invokes the pending-visibility protocol once, signals drains once, releases mutation authority before slow work, and performs no default model/full-corpus work on the request thread. Production integration with the Lane 2/Lane 4 implementations remains Lane 5 ownership.
- [ ] 3.6 Run acknowledgement mutation proofs in a harness-owned source copy: remove preparation and require `test_receipt_prepare_failure_leaves_canonical_untouched` red; retain authority across a slow component and require `test_slow_component_runs_after_authority_release` red; replace the shared deadline with per-component durations and require `test_components_share_one_absolute_post_commit_deadline` red; launder a registration failure and require `test_real_custody_failure_is_not_laundered_to_pending` red. Restore the lane, run `env EXOMEM_STATE_ROOT="$PWD/.task/state-lane3" uv run pytest --basetemp="$PWD/.task/pytest-lane3" -q tests/test_fast_write_ack.py tests/test_mutation_terminal.py`, `uv run ruff check src/exomem tests/test_fast_write_ack.py`, and `git diff --check`, and record outputs.
- [ ] 3.7 Assign an author-independent fresh reviewer to attack canonical/receipt ordering, rollback cuts, idempotent replay, terminal truthfulness, fixed deadline, no-op latency, and complete route coverage; apply accepted corrections and require reviewer recheck before lane acceptance.

## 4. Lane 4 — Deferred Advisory And Exact Result Retrieval

- [ ] 4.1 From the accepted Lane 1 SHA, add red component/lookup tests in `tests/test_deferred_write_advisory.py` proving worker custody does not require a foreground writer, returns a stable opaque result ref, and transitions exact lookup from pending to ready/failed/superseded across success, failure, restart, replay, and material target change; use a Lane 1 protocol fake for terminal handoff and leave the real default-write integration test to Lane 5.
- [ ] 4.2 Implement the receipt-owned advisory worker and one-generation vector reuse in narrowly scoped advisory/embedding modules and `corpus_aware.py`; keep deterministic thresholds, exact fingerprint suppression, soft-fail semantics, current-generation proof, and explicit synchronous `suggestions=true`, then verify no generation is encoded twice when embedding and advisory share it.
- [ ] 4.3 Implement exact-only `review_memory(mode="write-advisory-result", ref=...)` routing in `commands.py`; add red tests for missing/malformed/unknown refs, no list form, bounded ready warnings, and surface parity, then regenerate only the required command documentation/schema fixtures and verify MCP, REST, and CLI use the same leaf.
- [ ] 4.4 Add red governance tests proving every lookup revalidates target/counterpart fingerprints and the current release plane, withheld candidates are indistinguishable from absence, and ready review refs retain existing triage/family-disposition behavior; verify ordinary attention, activation, audit, due-state, bootstrap, and unrelated responses are byte-identical with or without the job/result record.
- [ ] 4.5 Run advisory mutation proofs in a harness-owned source copy: duplicate generation encoding and require `test_embedding_and_advisory_encode_generation_once` red; bypass lookup authorization and require `test_advisory_result_withheld_candidate_is_absent` red; inject a result into attention/due-state and require `test_advisory_result_never_joins_review_carriers` red. Restore the lane, run `env EXOMEM_STATE_ROOT="$PWD/.task/state-lane4" uv run pytest --basetemp="$PWD/.task/pytest-lane4" -q tests/test_deferred_write_advisory.py tests/test_write_advisory_suppression.py tests/test_attention.py tests/test_due_state_projection.py`, `uv run ruff check src/exomem tests/test_deferred_write_advisory.py`, and `git diff --check`, and record outputs.
- [ ] 4.6 Assign an author-independent fresh reviewer to attack result-ref capability leakage, authorization-time projection, supersession, restart durability, vector reuse, explicit-suggestions compatibility, and review-queue isolation; apply accepted corrections and require reviewer recheck before lane acceptance.

## 5. Lane 5 — Integration, Observability, Evidence, And Rollout

- [ ] 5.1 In a dedicated Lane 5 worktree based on the accepted Lane 1 SHA, merge the independently reviewed Lane 2, Lane 3, and Lane 4 commits and resolve only the frozen protocol wiring; verify the aggregate diff stays within the approved scope and rerun the exact Lane 1–4 focused commands before adding Lane 5 bytes.
- [ ] 5.2 Wire the Lane 2 pending publisher and Lane 4 advisory worker/result resolver through the Lane 1 protocols used by Lane 3; add red `tests/test_fast_write_integration.py` nodes including `test_default_write_never_encodes_advisory_inline`, immediate write→stable-ref/keyword/hybrid visibility, completed/pending/failed result refs, restart, and exact replay, then verify `env EXOMEM_STATE_ROOT="$PWD/.task/state-lane5" uv run pytest --basetemp="$PWD/.task/pytest-lane5" -q tests/test_fast_write_integration.py` passes.
- [ ] 5.3 Add content-free call-ledger phases and diagnostics for receipt prepare/proof, canonical commit, pending visibility, acknowledgement, component depth/age/completion, advisory state, and vector reuse; extend `scripts/semantic_write_latency.py` to record public acknowledgement plus immediate stable-ref/keyword/hybrid reads while preserving boundary/validation/commit rows, then verify `env EXOMEM_STATE_ROOT="$PWD/.task/state-lane5" uv run pytest --basetemp="$PWD/.task/pytest-lane5" -q tests/test_call_ledger.py tests/test_semantic_write_latency_gate.py tests/test_fast_write_integration.py` passes.
- [ ] 5.4 Implement the exact deterministic gate in `scripts/semantic_write_latency.py`; run `env EXOMEM_STATE_ROOT="$PWD/.task/state-perf" uv run python scripts/semantic_write_latency.py --sizes 2000 8000 --samples 5 --root "$PWD/.task/semantic-write-corpora" --attempts 2 --fts5-visibility --check` and verify warm public-write p95 below 4.0 seconds, exact immediate keyword/hybrid read p95 below 1.5 seconds, paired p95 below 5.0 seconds, and every canonical-commit-to-response sample inside the shared 2.0-second bound; preserve its content-free result artifact.
- [ ] 5.5 Run `env EXOMEM_STATE_ROOT="$PWD/.task/state-lane5" uv run pytest --basetemp="$PWD/.task/pytest-lane5" -q tests/test_derived_batch_receipts.py tests/test_pending_recall_delta.py tests/test_fast_write_ack.py tests/test_deferred_write_advisory.py tests/test_fast_write_integration.py` across pre-prepare, prepared-before-replacement, partial canonical state, committed-before-terminal, worker death before/after publication, newer supersession, restart hydration, old-queue drain, new-reader/old-writer, and feature-flag rollback; verify no stale publication, lost custody, duplicate canonical mutation, or permanent false-pending state occurs.
- [ ] 5.6 On a quiesced machine with no service using this lane's isolated state root, run `env EXOMEM_STATE_ROOT="$PWD/.task/state-full" uv run pytest --basetemp="$PWD/.task/pytest-full" -q`, `uv run ruff check`, `uv run pytest -q tests/test_scaffold_no_leak.py`, `git diff --check`, and `openspec validate --all --strict`; record commands, exit codes, base/head SHAs, and environment flags, and explain any non-product skip without weakening a gate.
- [ ] 5.7 Add `scripts/live_write_acceptance.py` with exact direct/connector transports and content-free output; on native Windows run `.venv\Scripts\python.exe scripts\live_write_acceptance.py --transport direct --samples-per-operation 30 --check` and `.venv\Scripts\python.exe scripts\live_write_acceptance.py --transport connector --samples-per-operation 30 --check`, requiring at least 30 default remembers and 30 edits per transport, p50 at or below 3.0 seconds, p90 at or below 5.0 seconds, exact read-your-write or explicit warming, zero uncovered full receipts from covered writes, and bounded post-burst convergence.
- [ ] 5.8 Stage rollout with `EXOMEM_FAST_DURABLE_ACK=0` for additive-reader/worker deployment and dual-record evidence, enable pending recall, then set it to `1` for a bounded command tranche and finally all governed writes; run the exact direct/connector acceptance command after each stage and apply the design's objective pause/rollback triggers, disabling immediately on any custody, stale-read, or authorization invariant violation.
- [ ] 5.9 Run the full guard→mutant→named-test matrix from Design §10 against the aggregate Lane 5 source copy and verify every node turns red under its mutant and green after restoration; preserve the matrix results and `git diff --check` output.
- [ ] 5.10 Assign a fresh Lane 5 reviewer who authored no prior lane to inspect the actual aggregate diff, instrumentation, benchmark code, and evidence against every requirement; require the reviewer to run adversarial crash/privacy/deadline probes and report ACCEPT or exact correction tasks, then apply corrections and obtain recheck before acceptance.
- [ ] 5.11 After all non-optional tasks and live gates are proven on the merged implementation, update task checkboxes from evidence, sync delta specs without deleting later requirements, run `openspec validate --all --strict`, archive with `openspec archive accelerate-durable-write-acknowledgement`, rerun strict validation, and deliver/merge through the single serialized integration owner.
