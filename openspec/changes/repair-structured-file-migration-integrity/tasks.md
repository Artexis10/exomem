## 1. Lifecycle Parity

- [x] 1.1 Add failing Planning and Records tests for revise after rebaseline, permanent discontinuity preservation, and restart inspection.
- [x] 1.2 Recognise Planning rebaseline checkpoints in shared audit reconstruction and permit valid revision from either profile's acknowledged checkpoint.

## 2. Portable Filename Projection

- [x] 2.1 Add failing filename and migration tests for compatibility-equivalent Unicode projection, collision suffixes, exact preview paths, and an empty second preview.
- [x] 2.2 Canonicalise structured filename projections to one NFKC portable spelling before sanitisation, collision detection, and publication.

## 3. Audited Representation Migration

- [x] 3.1 Add failing healthy-collection tests proving moved/re-rendered items extend the existing event chain and post-apply inspection stays `ok` or `acknowledged_gap`.
- [x] 3.2 Build deterministic per-item representation events, final item markers, intermediate container hashes, and the final manifest head into the exact preview plan.
- [x] 3.3 Publish final items, manifest, ordered audit events, inbound links, and strengthened inverse receipt in one guarded batch; refuse pre-existing structural audit gaps.
- [x] 3.4 Add failure-injection tests proving every original path, byte, audit head, activity byte, and receipt state is restored after publication failure.

## 4. Acceptance and Delivery

- [x] 4.1 Run focused lifecycle, structured-file, Planning, Records, governance, and Unicode suites plus Ruff and strict OpenSpec validation.
- [x] 4.2 Repeat the human-readable migration on a fresh Markdown-only vault copy and verify post-apply audit status, zero UUID projection drift for migrated collections, meaningful bodies, valid links, and empty second previews.
- [ ] 4.3 Update canonical specs, archive the completed change, and deliver the implementation through a draft pull request with verification evidence.
