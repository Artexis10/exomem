## 1. Lock the behavior with failing tests

- [x] 1.1 Add route-resolution, URL-validation, all-scope Claude/Codex HTTP rendering, shadow-safe replacement/rollback, and inert-plugin tests.
- [x] 1.2 Add merge-safe intended-profile persistence, locked runtime parity, and exact-ABI Darwin physical-footprint tests, including mixed-metric and RSS-fallback cases.
- [x] 1.3 Add reviewed-none response/replacement aliases, exact bootstrap/remediation, compatibility spelling, idempotent replay, integrity, and canonical-persistence tests.

## 2. Implement service-aware client setup

- [x] 2.1 Add one validated stdio-or-HTTP route representation and render it for Claude CLI, Codex CLI, and the existing Codex TOML fallback.
- [x] 2.2 Resolve `--mcp-url`, cwd-`.env` then inherited `EXOMEM_BASE_URL`, and `--stdio` in the specified precedence order.
- [x] 2.3 Replace health-checking Claude registration probes and implement all-scope, shadow-safe, idempotent convergence with confirmation, transactional rollback, and `--replace-client-registration`.
- [x] 2.4 Replace plugin stdio with the optional canonical HTTP placeholder, validate it with Claude Code 2.1.208+, and block setup on enabled legacy stdio plugin state.
- [x] 2.5 Print OAuth, update, and rollback follow-ups and document the shared-service route for existing service users.

## 3. Implement truthful diagnostics

- [x] 3.1 Persist/resolve the intended profile, expose a validated editable project root, and add a bounded, offline, locked, runtime-only active-environment check.
- [x] 3.2 Add a dependency-free process-memory helper that selects Darwin physical footprint or labelled RSS and preserves compatibility details.
- [x] 3.3 Use the shared memory helper in Doctor and the persistent-core resource verifier, including mixed-safe aggregates, separate RSS/physical limits, and remediations.

## 4. Implement the reviewed-none contract repair

- [x] 4.1 Return `relation_review_hash` from creation and predecessor-bound replacement validation exactly as specified while preserving existing hash fields.
- [x] 4.2 Canonicalize `reviewed-none` to `reviewed_none` before writer-lease digesting, validation, replay, and persistence without weakening the handshake.
- [x] 4.3 Update bootstrap, semantic remediation, schemas, generated capability artifacts, and public docs to teach the executable two-step call.

## 5. Verify and deliver

- [x] 5.1 Run focused setup, diagnostics, semantic-contract, schema, plugin, and generated-document tests plus lint and strict OpenSpec validation.
- [x] 5.2 Run the repository privacy/public-artifact gates and the proportionate full test suite with writable temporary and uv-cache directories.
- [x] 5.3 Complete independent code review and verification, resolve attributable findings, and record any Apple-Silicon-only acceptance item explicitly.
- [x] 5.4 Commit the intended scope, integrate the current remote default branch, push, and open a ready pull request with rationale and verification evidence.
