## 1. Private agent contract and cell route

- [x] 1.1 Define `contracts/hosted-agent-command-binding-v1.json` and its compatibility feature marker with exact route, header bounds, digest semantics and error mapping; verify producer/consumer fixture tests agree before either repository implements forwarding.
- [x] 1.2 Add red-first tests in hosted agent/private-route/security suites for missing/duplicate/malformed/mismatched expected tuple, forged cell/principal, wrong credential and no vault/leaf access on denial; implement the additive v2 route in `server_hosted.py` and canonical context helpers without duplicating command semantics, then make those tests pass.
- [x] 1.3 Preserve v1 behavior and cross-version idempotency/lifecycle safety; verify an acknowledged or ambiguously acknowledged mutation cannot execute twice across the two routes, cancellation releases owned admission, and an old gateway remains compatible in hosted gateway/lifecycle tests.
- [x] 1.4 Publish the feature in signed candidate compatibility artifacts; verify candidate digest/signature, fixture synchronization and strict runtime identity tests, then produce the pinned release required by the Substrate companion without changing live state during acceptance.

## 2. Shared gateway infrastructure

- [ ] 2.1 Preflight the dedicated Cloudflare origin/tunnel ownership, gateway database region/access and secret provenance using read-only checks; verify the Substrate-owned exact-path rewrite can target that origin and confirm any previously exposed provisioning credential has a rotated/revoked receipt before live expansion, reporting missing authority without altering unrelated systems.
- [ ] 2.2 Add the pinned Substrate gateway deployment/service/probes/drain and minimal secret references to `infra/helm/platform`; red-first extend Helm/infrastructure contract tests for no mounted Kubernetes admin token, bounded pools/resources, no billing/provider-admin credentials and required network isolation.
- [ ] 2.2a Before enabling the dormant gateway, register its two governed secret destinations with their actual ciphertexts, exact active selection, handoff tests and signed registry in one reviewed deployment change; preserve the valid existing matrix until all inputs exist and verify the same production wrapping key is used without hand-created Kubernetes Secrets.
- [ ] 2.3 Add only the dedicated origin DNS/tunnel route under foundation ownership, consumed by the Substrate-owned native Vercel rewrite; verify streaming/cancellation and canonical audience preservation, unexpected-path rejection, ingress-only trusted source metadata and unchanged website/OAuth/transfer routing with fixture tests, Terraform validation/plan inspection and rendered chart checks. No Worker or whole-site DNS change is required.
- [ ] 2.4 Verify the fixed private ingress origin and NetworkPolicy against actual gateway and cell pods before public cutover; show the intended local cell call succeeds while unrelated workload/direct access and endpoint/Host/redirect injection fail, without exposing credentials in evidence.

## 3. Resumable unattended acceptance

- [ ] 3.1 Add `infra/scripts/accept_hosted_service.py` (or a thin canonical command over the same runner) and unit tests for run manifests, exact release identity, resumable stage state, secret redaction, precise blocked actions and owned-fixture cleanup; verify interruption/resume does not reset tenants or repeat committed writes.
- [ ] 3.2 Add deterministic synthetic corpus generation and the normal-path OAuth/MCP client integration with the Substrate companion; verify at least 1,000 notes/10 MiB varied text, public authorization, canonical discovery, small durable capture and paraphrased semantic recall with resolvable citations and retrieval dependencies enabled.
- [ ] 3.3 Automate fresh-client, refresh-expiry/replay, fleet renewal, revocation/suspension, wrong audience/client, database/cell outages, concurrent two-tenant sentinels and ambiguous-ack idempotency checks; verify explicit pass/fail/blocked reports and unattended continuation across the >15-minute and >1-hour live windows.
- [ ] 3.4 Integrate the existing governed backup/isolated-restore workflow into acceptance; verify source tenant preservation, restored fixture integrity and run-owned cleanup without modifying unrelated vaults or cloud resources.
- [ ] 3.5 Add comparable old/new-path cold/warm benchmarks with five concurrent clients across explicitly reserved synthetic tenants; verify the sample counts, corpus, vantage point, p50/p95/errors and stage attribution in the performance spec, with no implicit paid preview/database branch creation.
- [ ] 3.6 Automate genuine host-client acceptance where authorized browser/host tools permit it; verify host certification evidence is tied to the exact artifact/runtime and generic protocol results cannot satisfy the host-specific gate. Checkpoint any irreducible consent once and continue independent stages.

## 4. Integrated verification and launch

- [ ] 4.0a Complete the hosted custody-aware v3-to-v4 governance migration coordinator using the existing migration engine and authenticated provisioner operation boundary; preserve provisioner signing authority, exact activation identity, verified backup and all-replica fencing without calling standalone custody initialization/enrollment on hosted cells.
- [ ] 4.0b Add red-first integration tests for actual schema/enrollment readiness, interruption at cutover and custody publication, ambiguous acknowledgement, stale control/membership and same-operation resume; prove phase-safe abort/recovery never overwrites acknowledged v4 writes and runtime attestation does not claim schema v4 for a v3 store.
- [ ] 4.0c Verify the coordinator through independent review and an agent-run disposable migration/recovery drill before applying it through governed operations to the existing hosted cell; retain content-safe evidence of actual schema, enrollment, activation tuple, serving membership and canonical authorization-session readiness. A runtime release activation or successful core health probe alone cannot close this task.
- [ ] 4.1 During implementation run `uv run pytest -q tests/test_hosted_agent_surface.py tests/test_hosted_private_routes.py tests/test_hosted_gateway.py tests/test_hosted_security.py` and exact affected infra/runner tests with task-local state; at the tranche boundary run the documented full lean suite, relevant infra suite, `ruff check`, public-artifact validation and `openspec validate --all --strict --no-interactive`, retaining actual output and naming unrelated baseline failures.
- [ ] 4.2 Obtain author-independent review of the actual paired transport/admission diff and independently reproduced negative/race tests; verify canonical fixture agreement before publishing ready PRs and retain worktrees through review.
- [ ] 4.3 Deploy the additive runtime and privately tested gateway in dependency order through existing governed release operations, complete and verify hosted governance activation (4.0a–4.0c), separately activate the runtime using the Substrate companion, then switch only the public MCP edge path; verify URL/audience continuity and exact-origin rollback without deleting grants.
- [ ] 4.4 Run the live service report through safety, usefulness, latency, restore and continuity gates with the runtime frozen; verify all required stages before expanding access and report supported clients separately from pending marketplace certification.
- [ ] 4.5 Update launch/reconnect/rollback runbooks and reconcile affected active release/client-planning artifacts; verify strict OpenSpec validation, synchronize deltas preserving newer requirements and archive only with actual non-optional task and delivery evidence.

## Execution dependencies

The runtime binding (section 1) and Substrate admission can be implemented concurrently after the fixture contract is agreed. Infrastructure work starts against the gateway image/environment contract, not an invented duplicate handler. Acceptance runner scaffolding is independent; its live execution waits for the reviewed runtime, hosted governance activation, admission and gateway. The hosted coordinator fills a confirmed implementation gap in the canonical migration contracts; archived migration task checkboxes are not proof that this hosted path exists. Edge authority, missing credentials and host consent are distinct blocked nodes with exact next actions; they do not block ready local work. Start the unattended continuity window as soon as a fixed candidate is deployed and perform other checks while it elapses.

## Delivery evidence

- Task 1.4: runtime `0.74.0` was published from `bd95fc9826069ec66f142c821abfda4b2f1d0912` by successful release run [34121947678](https://github.com/Artexis10/exomem/actions/runs/34121947678). The immutable image is `ghcr.io/artexis10/exomem@sha256:4fc9c71d051196bad097f7cfd5a0d3c25969abf53c7be2d767218b467586b083`; candidate SHA-256 is `60e19a9611e967ff7ea23d0576ce6122415b68193ae728bc9885d0276734665c`. The governed `hosted_image_candidate.py verify` command reverified both image and candidate subjects against the retained release bundles on 2026-09-07. Substrate's synchronized fixture/trust adoption shipped in [PR167](https://github.com/substrate-systems/substrate/pull/167), with successful main CI [34128387294](https://github.com/substrate-systems/substrate/actions/runs/34128387294). Publication and fixture adoption do not claim live cell upgrade, runtime activation, gateway cutover or service acceptance; tasks 4.3 and 4.4 remain open.

## Hosted migration implementation slices

### Task 1: Custody-neutral migration engine (part of 4.0a–4.0b)

Implement the library seam before the rollforward adapter. Ownership is `src/exomem/governance/schema_migration.py`, `src/exomem/governance/store.py`, and the existing migration tests or a new focused `tests/test_hosted_governance_migration.py`. Do not change runtime attestation, provisioner code, public wire contracts or live state in this slice.

- Replace the internal standalone-named planning identity with a neutral migration identity while retaining standalone caller behavior. Accept Hosted identity only from authenticated existing custody under the fixed hosted ownership boundary, wholly DRAINING at actual schema 3 with issuance stopped and no-in-flight; do not require epoch 1 or create custody on a missing/mismatched Hosted path.
- Extract `prepare_forward_migration_backup` from the existing commit preparation: exact plan, staged projection and source-store recheck, immutable plan-bound backup, verified returned backup. Preserve all current whole-tree/schema/writer fences and no-follow ownership checks.
- Bind Hosted plans/backups to the authenticated source custody and stable attachment/keyring/replica identity. Normalize only the four enrollment/activation fields when comparing the pre-enrollment source with the enrolled successor; a renewed membership, changed lifetime, attachment or keyring must not reuse the reviewed schema-3 plan. Verify every member's control/keyring digests and key set against current custody. Recheck exact custody at the database effect boundary. Post-commit replay may accept the provisioner's schema-4 membership successor only with the same bound identity and active target. Keep standalone plan/backup serialization unchanged.
- Extract `commit_enrolled_forward_migration` and the underlying `commit_enrolled_v3_store` transaction so an already-enrolled Hosted target commits/replays without any standalone enrollment or membership publication. Enrollment and exact tuple parity remain mandatory; ordinary openers never migrate. A stale plan, source, tuple or membership must fail closed.
- Compose the existing standalone `commit_forward_migration` and `migrate_enrolled_v3_store` entrypoints over the extracted functions and existing standalone enrollment/completion. Preserve their signatures, default behavior, crash barriers and recovery tests.
- Add an explicit Hosted backup-root input bound to private plan-addressed machine-local state outside the vault, rather than attempting to write beside a read-only custody Secret. Preserve the standalone default backup location. Thread the same explicit root through verification/replay so a retry cannot discover a different artifact implicitly.
- Hosted backup preparation requires unchanged unenrolled custody at entry, before publication and after durable verification; recovery after enrollment only verifies the existing artifact. The provisioner serializes enrollment after the successful preparation terminal. Bind the source membership epoch/digest into the plan and backup: database replay accepts the exact prepared draining custody or a schema-4 successor at precisely source epoch + 1 with the prepared membership digest as predecessor, never a same-epoch replacement or unrelated/skipped successor.
- Add red-first tests for drained unenrolled Hosted preparation with no standalone calls; missing/unsafe custody refusal; plan/source/tuple drift; enrollment-before-commit; exact v4 replay; unchanged custody bytes; private backup placement; and existing standalone/crash recovery compatibility. Test files must use disposable local state only.

Run focused development tests for the changed migration functions. Before reporting this slice complete, run `uv run --frozen python -m pytest -q tests/test_governance_schema_migration_plan.py tests/test_governance_v3_store_migration.py tests/test_governance_schema_downmigration.py tests/test_hosted_governance_migration.py`, applicable lint and the repository privacy gate. Report actual red/green output and any missing capability separately. This slice alone does not complete task 4.0a, activate a Hosted cell, or authorize publication/deployment.

### Task 2: Provisioner-owned enrollment primitive (part of 4.0a–4.0b)

Implement the pure bundle transformation independently of the engine. Ownership is `infra/provisioner/src/exomem_provisioner/authorization_membership.py`, its focused tests and, only if a distinct stable reason is required, `conflict_reason.py`. Do not change the live adapter, lifecycle coordinator, Kubernetes publisher, runtime engine, public wire or live state in this slice.

- Extend the authenticated bundle observation with actual signed membership schema and enrollment/activation fields, populated consistently by bootstrap, inspect and ordinary transition. These are internal observations, not new wire fields. Ordinary renewal/drain/resume preserves the source schema and enrollment tuple.
- Add a separate `enroll_hosted_governance_bundle` pure function using the existing exact identity, recovery-envelope and signature verifier. It accepts only fresh, fully DRAINING schema-3 custody with issuance stopped and no-in-flight. Target inputs are a bounded nonempty activation-store identity, an exact positive integer activation epoch and a lowercase SHA-256 activation-state digest, bound to the expected logical vault by existing inspection.
- The only new enrollment transition is false/null to true/exact target. Preserve keyring and membership bytes, membership epoch/digest, control identity and validity window byte-for-byte; change only the enrollment fields and control MAC, then re-inspect the result. Exact already-enrolled replay returns the same bytes/revision. A different enrolled target, invalid tuple, stale/tampered/foreign custody, serving membership or unproven drain refuses. There is no unenroll/reset option and no arbitrary mutation callback.
- This pure function does not grant publication authority: the subsequent coordinator must bind preparation, backup, stopped pods, operation/fence/PVC and target image, then publish through the existing expected-revision Secret CAS. Do not add a second publisher or simulate that integration as complete.
- Add red-first tests for the accepted transition, non-epoch-1 custody, exact replay, all refusal classes, byte preservation, and verification by the real runtime custody parser. Existing bootstrap/renew/drain/resume tests stay green, including schema-3 preservation.

Run the focused provisioner authorization-membership suite and applicable lint; at completion run the provisioner suite and privacy gate using the repository's existing dependency environment. Record scope and actual results. Do not synchronize unrelated environments while another agent is testing the engine. This slice alone does not complete task 4.0a or authorize publication/deployment.

### Task 3: Schema-aware runtime/provisioner bridge (part of 4.0a–4.0b)

Keep actual runtime attestation and its provisioner consumer coordinated. Ownership is `authorization_session_lifecycle.py`, `authorization_membership.py`, `live.py`, and their existing test suites. Do not add migration Jobs, new public health fields or live deployment in this slice.

- Runtime attestations report the read-only observed governance-store schema, admitting only actual supported schema 3 or 4 and refusing unknown/malformed observations without migrating the store.
- Let the authenticated provisioner bundle inspector discover signed membership schema when no exact schema is requested; this discovery admits only supported schemas 3 and 4 and keeps all identity, signature, custody and freshness checks. Exact-schema callers, including enrollment, retain their existing strict expectation.
- Ordinary renewal/drain/resume uses that authenticated source schema and preserves it, rather than imposing the target schema-4 constant. A runtime attestation with a different schema refuses; discovery is not permission to repair or downgrade membership.
- New-cell bootstrap uses the predecessor schema pinned by a compatibility test to the runtime store initializer's canonical version; the separately shipped provisioner must not import or depend on the runtime package. Expired enrolled custody is never reset to a new unenrolled genesis, even when routes are closed and no pod is serving. Preserve the existing retry for genuinely unenrolled, never-served bootstrap.
- Add red-first source-schema discovery/refusal, truthful schema-3 runtime renewal, schema preservation through transitions and enrolled-expiry no-remint tests. Existing schema-4 renewal, bootstrap/CAS and runtime-attestation tests remain green. Run the affected runtime suite and full provisioner suite before delivery; hold live activation until the migration coordinator and legacy schema-claim reconciliation are verified.

### Task 4: Target-image migration runner (part of 4.0a–4.0b)

Implement the bounded runtime entrypoint consumed by the planned Kubernetes Job adapter. Ownership is `src/exomem/hosted_governance_job.py` and `tests/test_hosted_governance_job.py`; reuse the canonical hosted root binding, custody parser, state placement and migration engine. This slice adds no live Job submission, second publisher, deployment-mode switch or public API.

- Accept a closed, bounded request binding phase, operation, fence, PVC UID, pinned target image, cell/vault/replica identity and expected custody revision. Inspection has no plan/source input; preparation requires the inspected source digest; commit requires that source digest and the prepared plan digest. Emit only a bounded content-free terminal tied to the request digest.
- Validate the fixed hosted root markers and private custody files before store work. Require singleton fully drained authenticated membership, fixed replica identity and unchanged custody throughout. Inspect actual schema 3 without migrating; a legacy signed schema-4 claim may be observed but never repaired by the runtime. Preparation requires corrected schema-3 membership and unenrolled custody.
- Preparation reuses canonical plan, inert projection staging and verified backup under the fixed private machine-state root outside the vault. It cannot enroll. Commit verifies the existing plan-bound backup and exact enrolled target before invoking the custody-neutral engine; exact replay returns the same target without a second cutover. Neither phase writes custody, creates a policy, resets enrollment or chooses an arbitrary backup path.
- Exercise real disposable SQLite/custody fixtures, source/plan/identity drift, missing enrollment/backup, legacy claim refusal at prepare, read-only inspection, byte-preserved custody and replay. Keep raw inputs, policy content and exceptions out of the terminal/log output. The Job adapter and existing rollforward coordinator still own proof of stopped pods, Kubernetes UID/fence/image binding, Secret CAS and admission reopening.

### Task 5: Provisioner migration Job adapter and exact admission (part of 4.0a–4.0b)

Add the caller-side adapter for Task 4, without wiring it into live rollforward yet. Ownership is a focused `infra/provisioner/src/exomem_provisioner/governance_migration_job.py`, its provisioner tests and closed conflict reason, narrow fixed-slot preservation hardening in `adapters.py`, narrow additions to both existing tenant/provisioner admission policies, and their existing Helm/exact-K3s tests. Keep `V2HealthResponse`, existing migration modes and live deployment unchanged in this slice.

- Build the closed runtime request from authenticated operation metadata and explicit expected vault, PVC UID, pinned runtime image and custody revision. Use the original stopped StatefulSet replica identity, never the Job pod's generated name. Validate the phase-specific source/plan fields and terminal with strict integer/boolean types, exact keys and byte bounds. Test producer/consumer parity without introducing a runtime-package dependency in the separately shipped provisioner.
- Reuse the signed fixed `{resource}-init` Job slot and its existing provider recovery envelope. Bind the request digest, operation/fence, phase, PVC UID and image in the Job and pod template. Before submission and before accepting a result, verify the expected PVC UID and the existing desired-zero/no-runtime-pods proof. Preserve a foreign, active or ambiguous slot; never delete it merely because it is terminal. Pin the observed Job UID and require a single pod owned by that UID, the exact expected container/template, successful termination and the matching bounded terminal. Reject missing, duplicate, stale, malformed, drifted and oversized evidence with closed content-free errors.
- Pin `app.kubernetes.io/name=exomem-governance-migration` on the Job and pod in both admission layers, retaining the exact `exomem.io/cell` label. Keep the existing runtime stop-proof selector unchanged: using `exomem-cell` would count the migration Job itself, while adding a generic migration-label exclusion would create a caller-controlled stopped-proof bypass.
- Require the vault identity to equal the authenticated tenant identity, matching existing cell bootstrap. Require the Job controller principal for migration Pod creation, not merely caller-supplied owner references. Pin Job parallelism/completions to one and reject alternate controller modes. Harden the existing fingerprint adapter to preserve foreign terminal slots too; terminal status alone never authorizes deletion. Migration Jobs have no TTL cleaner: validated evidence and UID-preconditioned removal own slot release.
- Use the target runtime image for both the canonical `authorization_hosted_mount` init container and `python -m exomem.hosted_governance_job`; the main container sees only the private copied custody read-only. No custody refresher, service credential, API token, network service, arbitrary command/path/image, or Secret publisher is added. Mount only the fixed cell PVC subpaths, authorization source/custody volumes and bounded ephemeral scratch. Inspection mounts all persisted roots read-only; prepare/commit may write vault/state but not logs or custody. Use existing non-root identity, dropped capabilities, read-only image filesystem and bounded resources/deadline/retries.
- Extend both admission layers together for this exact Job shape. Preserve all existing serving, storage-init, fingerprint and restore branches, including controller-only Job-finalizer removal. A migration label alone cannot bypass the ordinary tenant restrictions: require the fixed owned Job, pinned command/image, bounded scalar environment, exact volume/mount/identity/resource shape, and reject mixed lifecycle labels or additional executable surfaces. Exercise actual CEL admission in a disposable local K3s cluster, not the live service.
- Repeated same-request observation is resumable. After validated successful evidence, remove only the observed Job using UID/resource-version preconditions and wait for foreground removal before releasing the fixed slot; no broad cleanup or adoption of a replacement UID. API and terminal failures remain content-free. The later coordinator still owns fresh operation/fence authorization around effects, irreversible enrollment publication through the existing Secret CAS, schema-claim reconciliation, checkpoint persistence, readiness agreement and admission reopening; this adapter alone does not close 4.0a–4.0c.

Run red-first adapter/refusal and rendered/admission tests, then the full provisioner suite, existing Helm/infrastructure scopes, exact-K3s admission, privacy, pinned strict OpenSpec validation and independent security review. Do not claim a generated manifest is deployable without the admission proof.

### Task 6: Wire governed migration into rollforward and private readiness (part of 4.0a–4.0b)

Implement the existing design section 3a using Tasks 1–5. Ownership is the provisioner lifecycle/live plane, authorization membership transformations, private readiness producer/parser, current deployment-lock/cell-chart mode validators and their tests. A focused migration coordinator module is allowed; no new service, public action, database table or public v2 health field is introduced. Published lock/candidate bytes remain immutable. Do not deploy or activate a live cell in this implementation slice.

- Add the explicit `governance-v3-to-v4` mode consistently to the current lock/schema/config and chart validators, without allowing an old storage-init command to perform it. Instantiate the Job adapter with the existing Kubernetes clients and select only the exact target image from the authenticated deployment lock.
- Reuse the existing maintenance, route-closure, runtime drain, desired-zero/no-pods and canonical vault-fingerprint sequence. Authoritative operation/claim/fence checks must remain fresh around each external effect. A checkpoint is a replay hint, never authorization to skip actual custody/PVC/schema checks. Retryable and lost-acknowledgement paths use the existing worker mechanisms; unknown or foreign state stays fenced and never becomes a generic successful retry.
- Keep durable progress within the existing 256-character checkpoint boundary where possible: phase plus vault fingerprint, source digest and plan digest fit without persisting private activation tuples in public progress. The canonical verified backup remains the target authority. Before enrollment, replay preparation to recover and reverify its exact target if needed rather than inventing another durable store. Test the encoded maximum length and every restart boundary before adopting this representation.
- Inspect schema 3 using the pinned target Job. Only an authenticated unenrolled singleton with stopped issuance/no-in-flight and actual schema-3 proof may reconcile a legacy signed schema-4 claim to a schema-3 DRAINING successor. This migration-only transform must not become an ordinary schema downgrade or change the signing key/attachment/enrollment tuple. Once repaired, no automatic fallback to an incompatible predecessor is permitted.
- Prepare and verify the source/plan/backup while custody remains schema-3 DRAINING and unenrolled; persist the source and plan before publication. Publish the exact enrollment successor through the existing expected-revision Secret CAS. Keyring and membership bytes remain unchanged. A lost publication acknowledgement must reconcile the exact prepared backup/activation tuple and signed successor; observing `governance_enrolled=true` alone is not success. Changed lifetime/membership/key/attachment or a different target must block before an irreversible database effect.
- Run the exact plan-bound commit Job only under enrolled custody. Lost database acknowledgement repeats that commit's canonical replay, never preparation or backup restoration over v4. After verified actual schema 4 and exact activation agreement, publish the schema-4 DRAINING membership successor, preserving monotonic enrollment and lineage. Reuse the ordinary target upgrade/resume only after this transition. Every failure after enrollment remains fenced; the existing no-old-image-rollback rule remains intact.
- Add only closed private readiness evidence for actual schema, authenticated enrollment/activation tuple and store agreement. Cross-check it against the authoritative signed Secret in the provisioner and require the exact selected runtime before routes reopen. Keep `V2HealthResponse` unchanged. Exercise the fresh-provision path too: a new target cell without governance enrollment must not acquire a false ready/admitted result; any required bootstrap activation integration must be resolved before claiming 4.0a complete.
- Cover interruption before/after preparation, repair CAS, enrollment CAS, database commit, membership completion, target start and route reopening. Include same-operation resume, changed operation/fence/PVC, stale custody, different successors, malformed private proof and legacy mode compatibility. Run a disposable real SQLite/custody integration through the coordinator adapters, plus the existing worker/lifecycle and private-route suites. Independent review and the live migration/recovery drill remain required before 4.0c or launch admission closes.

Critique the checkpoint/recovery sequence and the fresh-provision admission boundary before implementation; simplify within these existing authorities rather than adding a second orchestrator.

Task 6 starts with a bounded membership-helper tranche, not coordinator completion. The repair and completion helpers consume the exact typed migration request and validated Job evidence, recheck its closed terminal against the authenticated custody revision, and publish nothing. They preserve the signing key, attachment, enrollment target, software identity and validity window while advancing only schema/membership lineage. Repair requires fresh unenrolled drained custody and actual schema-3 inspection. Completion requires enrolled exact-target schema-4 commit evidence; it may preserve an already-expired signed window but cannot renew it or make it serving. A current-request proof of the already-correct schema returns identical bytes; old-revision evidence cannot authorize a different successor. Test the returned bytes against the runtime verifier.

The critique identified an activation blocker: a crash after enrollment that outlasts custody expiry cannot currently resume the strict runtime commit, and ordinary renewal would invalidate the backup's source binding. Before live activation, implement and verify a migration-only expired-custody recovery rule bound to the same operation/fence/PVC, immutable backup and exact activation target; do not relax ordinary custody readers or reset enrollment. The helper tranche alone does not resolve that blocker, fresh-provision activation, effect-claim fencing or private readiness admission.

Implement the remaining coordinator work with these concrete constraints from the critique:

- Add a worker-supplied repository-backed current-claim/cell-lock/fence guard before external effects, including effects within one driver call. A background heartbeat flag checked only after `execute` returns is insufficient. Retain provider UID/resource-version/request binding for races after that guard.
- Preserve the full migration checkpoint on transient failure. The existing generic retry path replaces it with `retry-backoff`, which would discard the source/plan digests. Distinguish ambiguous possibly-committed provider calls from definite foreign or malformed evidence; an uncertain acknowledgement must not restart the protocol or reset its checkpoint.
- For fresh provisioning in governance mode, finish storage initialization with routes closed before ever starting the StatefulSet. Release only the authenticated old init Job using UID/resource-version-bound foreground cleanup, prove the bound PVC and no runtime pods, publish a schema-3 fully DRAINING bootstrap successor, and use the same migration coordinator. The Job adapter already supports an absent StatefulSet as desired zero while retaining PVC/no-pod checks; do not weaken that proof. Render/start schema-4 serving mode only after verified completion, then require private governance readiness before admission and route publication.
- Completion's same-window expired successor covers a delay after commit evidence exists; it does not fix expiry before an acknowledged commit. The eventual target runner/engine must provide the narrowly governed expired-custody commit/replay path. Publish a new runtime image/candidate and authenticated deployment lock containing the runner and recovery implementation: the previously published 0.74 image predates the runner. Never edit that image's published candidate or lock bytes in place.

### Task 6 recovery boundary: expired enrollment remains recoverable, not serving

Add an explicit hosted-only commit/replay entrypoint over the existing engine and database transaction. Its custody reader authenticates the fixed hosted files at the actual current time, requires an already-enrolled singleton with the fixed local replica fully DRAINING at schema 3 or 4, and retains attachment, key, MAC, lifetime-shape and identity checks. Only the elapsed control/membership/replica authorization window may be accepted: future-issued records, expired signing keys, malformed windows, missing membership, serving replicas and unenrolled custody still refuse. Keep ordinary parser/loader signatures and expiry behavior unchanged; share private parsing internals rather than copying verifiers or backdating the clock.

The target-image runner uses this reader and commit entrypoint only for the existing `commit` phase, with its exact request/custody-revision/root/lifetime-lock checks intact. Before a database effect, verify the immutable plan-addressed backup, source-store digest, normalized prepared custody identity, enrolled activation target and inert projection; re-read the same custody at the transaction boundary and after completion. Use the existing live-source and v4 target verification for replay. Preparation/inspection remain fresh-only and cannot create a recovery authorization. The provisioner's current operation/fence, PVC UID, image and stopped-pod checks remain the outer authority; runtime recovery is not a new effect authority or a public mode switch.

Exercise real disposable SQLite and signed custody across expiry before commit, lost commit acknowledgement, unchanged ordinary-reader refusal, key expiry, future or malformed windows, missing/tampered backup, target/source/custody drift and serving/unenrolled refusal. No custody bytes may change, no enrollment reset or v3 restore is allowed, and a verified v4 replay must not perform another cutover. Review the distinct expiry exception before implementation and independently reproduce its refusal tests before delivery. This closes the runtime expiry dead end only; coordinator wiring, release publication and live activation remain separate unfinished work.

The recovery critique requires an actually elapsed control window; a fresh bundle stays on the normal strict commit path. Close recovery to the hosted publisher's one-key/all-signers-equal bundle. For expired replay after a direct schema-4 successor, additionally reconstruct the prepared control basis using the backup's source membership epoch/digest and normalized enrollment fields, then require the original custody digest. This binds the validity window and all other prepared control fields. Preserve coherent successor renewal on normal fresh replay; the tighter window binding belongs only to the expiry exception.

### Task 6 worker authority and checkpoint preservation

The next bounded tranche adds the worker-to-driver authority seam and prevents transient retry from discarding an existing checkpoint. Reuse the repository's active-claim, current tenant fence and shared cell-lock validation in a read-only guard. Supply that guard through `EffectContext`; an effect that explicitly requires it must refuse when it is absent, lost, expired or superseded. Recheck provider fence and heartbeat loss around the guard. Do not serialize the callback, expose claim credentials or replace provider CAS. Existing drivers are not automatically described as per-effect fenced merely because the seam exists: the migration coordinator must call it before its external effects. Pending retries keep the prior durable checkpoint while preserving bounded attempts, backoff, lease release and terminal exhaustion behavior. Test fresh database reads, changed claim owner/token/generation, expired/missing/foreign cell leases, newer tenant/provider fences and repeated failures after a nontrivial checkpoint. The migration coordinator still needs explicit terminal-failure recovery and checkpoint grammar before activation.

### Task 6 coordinator recovery constraints

Use the existing checkpoint column for a closed `gm1` representation: one phase code, the vault fingerprint, a canonical unpadded base64url SHA-256 binding, source-store digest and plan digest. The maximum ordinary complete representation is 244 characters; the target-start recovery extension below is at most 247. The binding covers the current internal/provider operation IDs, tenant/cell, fence, wire protocol, freshly authenticated PVC UID and selected digest-pinned image. Persist the initial inspection checkpoint before the first migration Job. Recompute and compare this binding on every retry; a checkpoint does not authorize an effect or adopt a replacement volume.

Advance inspection/repair to preparation, then persist source and plan before enrollment. An unenrolled enrollment retry repeats preparation and requires the same source/plan before publishing its exact target. If enrollment is already observed, do not prepare or renew: the exact plan-bound commit verifies the immutable backup, prepared custody basis and enrolled target before any database effect. Keep the binding and source/plan in migration-complete (`d`) and private-target-confirmed (`t`) phases through target start and route reopening; do not collapse into the older fingerprint-only checkpoint forms. Discard this progress only after route publication, upgrade commit and maintenance release are proven.

Keep Job and Secret provider identities distinct. Jobs use the current operation's authenticated `initJob` envelope. Custody inspection and Secret CAS use the recorded original owner and its `authorizationSessionSecret` envelope. Recheck the worker guard immediately before each Job create/delete and Secret patch, after predecessor reads, while retaining object UID/resource-version preconditions. Propagate claim/fence loss without disguising it as malformed provider state.

Treat Job creation/deletion or Secret patch timeout, transient API failure and CAS conflict as uncertain acknowledgements: preserve progress and reread the exact state. A conflict alone is not proof of a foreign successor. An enrolled or later-phase transient failure must not exhaust into checkpoint loss; use same-checkpoint delayed pending recovery. Definite foreign or invalid evidence remains terminal and fenced, but terminal failure and retry exhaustion must preserve governance checkpoints. Provide an explicit, verified same-operation/current-fence recovery path before live activation; preserving a terminal checkpoint by itself does not make it resumable. Exercise interruption and recovery at every effect boundary, including final route/upgrade/maintenance effects.

A guard cannot revoke a Job already running. Treat the fixed governance-migration Job as an external per-cell barrier: a different or newer operation must not perform PVC, runtime or route effects while it remains, even if the database cell-lock lease has expired. Only exact same-operation/fence adoption or explicit verified cleanup/recovery may release that barrier. A terminating exact Job is deletion-in-progress, not automatically foreign; provider observation must let its owning coordinator reconcile it. A failed commit Job can hide a committed SQLite transaction and requires retained progress plus exact replay after verified cleanup. Test higher-fence submission mid-commit, commit followed by container failure, and deletion acknowledgement loss before integrating live enrollment. At an enrolled `e` checkpoint, verified commit evidence advances to `c`; defer membership completion to the `c` replay so the durable phase boundary precedes that additional effect.

Use retained `gm1` progress as the durable denial barrier as well: it commits before any Job attempt and prevents a delayed old Job create from racing a successor that just observed an empty provider slot. Exclude foreign internal operation IDs at candidate selection, cell-lock acquisition and each active-claim guard. Pending, claimed and terminal-error v2 provision/rollforward markers block the same cell; they block tenant-wide destruction across the tenant. Preserve unrelated-cell throughput and same-operation retries. Atomically refuse the first non-`gm1` to `gm1` transition while a non-final tenant destruction or a foreign claimed same-cell operation exists, including reused provider operation IDs. The existing tenant-fence-first transaction order supplies serialization; do not hold a database transaction across Kubernetes calls or add a second marker store.

Provider observation must authenticate migration-labelled fixed Jobs using their own recovered operation annotations and exact Job recovery envelope. Expose absent/current/foreign classification without treating it as coordinator effect authority. Only an authenticated current operation/fence may bypass generic terminating rejection; foreign authenticated state remains pending and malformed or unauthenticated state remains terminal. Before any lifecycle dispatch, retain the barrier unless the current governance-mode operation has its durable migration checkpoint. The coordinator still must verify the exact body, UID/resource version, phase and result before reconciliation.

Expected authenticated Job contention returns same-checkpoint delayed pending, not a retryable error that consumes the worker failure budget. Preserve `gm1` through the worker's intermediate effect-applied/resource-recording steps; clear it only when the final result and `FINAL` state commit atomically. Exercise a crash after driver success but before that final database commit, then prove same-operation replay remains possible and a successor remains blocked until completion.

### Task 6 target-start expiry recovery

Persist the pre-CAS recovery commitment in closed eight-field `gm1:r1d:<fingerprint43>:<binding43>:<source43>:<plan43>:<successor43>:<issued_at>` or `r1t` form. The five digests are canonical unpadded base64url; `issued_at` is a positive bounded decimal integer. Preserve ordinary six-field encoding and the `gm1:` denial prefix. Compute the exact DRAINING successor and return pending without publishing; only the next claim-locked pass may CAS. If current custody equals the committed successor, reconcile to ordinary `d`. Otherwise reconstruct the successor from current custody using the committed issuance time and require its revision to equal the commitment before CAS against that exact current revision. Validate source/key freshness exceptions at actual current time separately from deterministic successor issuance. Never replace an uncertain commitment when its draining window expires: repeat the same exact CAS or reconcile it while the key remains current, then use normal DRAINING-to-SERVING to mint the fresh serving window. Test old encoding compatibility, strict bounds/canonical forms, same-target different-time/key successors, lost CAS acknowledgement across restart, expiry while pending and current-key expiry. This extends the existing worker checkpoint transaction; do not add a table, receipt writer or historical-time authentication shortcut.

The approved target-start recovery rule also covers an expired schema-4/enrolled SERVING successor at retained `d`/`t` progress. Keep this implementation migration-only and do not change the ordinary membership transition or renewal authority. Re-authenticate the original-owner Secret, current operation/claim/fence, bound PVC UID and selected image on every pass. Under maintenance ownership, close both routes, prove external rejection, stop the workload and prove absence of all PVC-using pods before exact-revision guarded custody reissuance. Preserve keys, attachment, activation tuple, schema, software and membership predecessor lineage; reject future/malformed windows and expired signing keys. Resume the unchanged target and require fresh closed private proof against the same Secret before admission or reopening routes. Retain source/plan/binding through lost target-start, route-publication and maintenance-release acknowledgements. No second cutover, plan replacement or backup restore is authorized. Pre-enrollment expiry remains a fenced explicit abort/recovery case. Test expiry after each serving-publication/start/route effect and claim loss between every predecessor read and write before enabling the mode.

Publish an intermediate schema-4 fully DRAINING successor; never renew expired SERVING directly to SERVING. An expired `t` retains `t` until the exact DRAINING CAS is confirmed or reconciled, then persists `d`. The `d` path must likewise detect and recover expired SERVING before any ordinary same-state transition or target start. A key valid at the current instant is insufficient: cap both recovered and subsequent serving windows at its `not_after`, and refuse insufficient remaining lifetime. Use namespace-wide PVC/runtime pod absence, not the existing label-selected `runtime_stopped` alone. Repeat route closure, absence, binding and current-claim checks after predecessor reads and immediately before CAS/start. Cover uncertain CAS at `t`, replacement or wrong-state successors, near-expiry keys and an unlabelled PVC-using pod in the recovery tests.

### Task 6 exact Job cleanup and replay

Reconcile an exact terminating Job by waiting under fresh claim guards, without issuing another delete. A failed Job is eligible for automatic cleanup only with terminal `Failed=True`, zero active/terminating counts and authenticated exact request/spec/UID. A failed counter, `FailureTarget`, timeout or deletion timestamp is not that proof. Observe pods across the whole cell namespace: candidates include any pod mounting the bound PVC or naming the fixed Job slot/current UID, even without its expected labels. Authenticate every candidate's exact metadata, owner and execution spec; foreign or malformed candidates remain untouched, and nonterminal candidates remain pending. Immediately before failed cleanup, re-read the same Job, revalidate terminal failure and delete with its UID/resource-version preconditions under the worker guard.

Both successful and interrupted cleanup must prove Job absence and absence of all candidate pods. Repeat the namespace-wide absence check immediately before creating a new Job; API/list failures are not absence. After failed or interrupted cleanup, return a retry of the same request and phase, never synthesized migration evidence or phase advancement. Keep the durable barrier and existing coordinator responsibility for checkpoint retention. Kubernetes 1.35's [terminal Job conditions](https://v1-35.docs.kubernetes.io/docs/concepts/workloads/controllers/job/#terminal-job-conditions) and [foreground garbage collection](https://v1-35.docs.kubernetes.io/docs/concepts/architecture/garbage-collection/#foreground-cascading-deletion) require these separate stop and absence checks.

A provisioner killed mid `helm upgrade --wait` leaves the release at `pending-upgrade` or `pending-install`, and Helm refuses every later attempt at the same target while that record stands. Before the guarded `helm upgrade`, read the release history with status and, when the latest revision is pending, authenticate that record as this call's own: its saved values must equal the exact values about to be applied, its chart must carry the pinned chart version, and it must be the latest revision. Anything else is foreign, stays fenced, and fails terminally without a delete or an upgrade. An authenticated own record is cleared by running the caller's effect guard immediately before deleting exactly that `sh.helm.release.v1.<namespace>.v<N>` ConfigMap under its UID and resource-version preconditions, then re-reading the history to prove it is gone — the prior deployed revision is latest again for `pending-upgrade`, and no revision remains for `pending-install` — before the exact `helm upgrade --install` proceeds in the same call. An unacknowledged delete, a precondition conflict or an unproven removal returns the same-checkpoint retryable outcome; never a second delete in one pass, and never a rollback. Ungoverned Helm calls keep their current behaviour and never touch a release record.

### Task 7: Private governance readiness producer and verifier (part of 4.0a–4.0b)

Own `src/exomem/governance/authorization_serving_membership.py`, `authorization_session_lifecycle.py`, `hosted_runtime.py`, `server_hosted.py`, a focused provisioner `governance_readiness.py`, and focused new runtime/provisioner tests. The coordinator owns `live.py`, `lifecycle.py`, deployment-mode selection and its tests; do not edit those in this task. Do not change public health models, ordinary readiness serialization, custody publication or lifecycle state.

Extend the existing readiness result with an optional frozen private governance proof, absent by default. Produce it only from the existing fixed-hosted-path custody validation and `_ready_custody` database check in `hosted_serving_membership_readiness`; reuse those canonical checks rather than a second custody or store verifier. Verify actual schema 4, current enrolled custody, active store/tuple agreement and canonical serving membership on the same read connection. Re-read the exact custody generation before returning the proof; changed custody must return unavailable, never evidence assembled from two generations. Standalone readiness and unavailable results carry no private proof.

Expose this only as `governance` on the authenticated private `/private/exomem/v1/ready` response, preserving the existing `authorization_session` shape and public v2 health wire. The closed proof has exactly `schemaVersion` (integer 1), `actualSchema` (integer 4), `cellId`, `vaultId`, `replicaId`, `softwareVersion`, `governanceEnrolled` (true), `activationStoreId`, `activationEpoch`, `activationStateDigest`, `custodyRevision`, `membershipEpoch`, `membershipDigest`, and `storeAgreement` (true). Derive revision as SHA-256 of the exact keyring, control and membership bytes in that order, matching the existing publisher. Identity, digests and integers retain the canonical bounds; booleans never satisfy integer fields. Do not emit file paths, credentials, key bytes, policy content or raw exceptions. The ordinary non-private readiness serialization must not gain the activation tuple.

Add `verify_governance_readiness(proof, *, bundle, cell_id, vault_id, replica_id, software_version)` in the provisioner module. It accepts only the exact private object and an already authenticated `HostedAuthorizationBundle`, requiring schema 4, enrolled SERVING custody and exact identity, software, activation tuple, custody revision and membership epoch/digest agreement. Return no public fields; refuse missing, extra, malformed, false or mismatched evidence with a content-free `MetadataConflict` using the existing readiness conflict vocabulary. The caller remains responsible for freshly reading/authenticating the authoritative Secret and selecting the pinned runtime; a typed bundle alone grants no publication authority. Do not import the runtime package into provisioner production code.

Write red-first tests with real disposable SQLite and signed hosted custody for schema 3, unenrolled, wrong actual target, expired/changed custody, successful schema-4 proof and unchanged standalone/public serialization. Exercise the authenticated private route and missing/wrong authentication. Add exhaustive closed-object and exact-bundle mismatch tests for the provisioner verifier, plus a cross-package producer/consumer test using the actual runtime proof. Test-only imports may install both editable packages. Run focused runtime readiness/private-route tests and the focused provisioner verifier tests during development; the integrated completion boundary runs the full affected suites and independent review. This task alone does not reopen routes or close launch acceptance.
