# Tasks

## 1. Registry and contracts
- [x] 1.1 Family registry module (benchmarks/membench/families.py: 7 active v0.1 + 8 planned v0.2 + tacit_polanyi out-of-scope; generation-time refusal at the single choke point; taxonomy-membership + integrity guards) — reviewed APPROVE, 16 tests, release-manifest byte-identity unchanged
- [x] 1.2 Coverage table published in docs/memory-proof-benchmark.md with verbatim no-drift gate
- [ ] 1.3 Provider onboarding doc + third-party adapter conformance suite (faked runtime seam)

## 2. Governance wiring (closes the v0.1 gap)
- [x] 2.1 PolicySet → `_Governance/` translation via public surfaces (validated by exomem's own policy compiler), persona→principal threading, GOVERNED_VIEWS iff wired; wired-translation report with dropped-rule accounting — delegated lane + correction round; targeted recheck: RECHECK PASS
- [x] 2.2 Three-state reporting (wired / default-open-labelled / unsupported) with family-tagged rows and full comparative exclusion of default-open governance-family rows; malformed policy → INVALID run; `--governance` CLI switch
- [x] 2.3 Wired vs default-open rerun recorded in the findings doc (tables, vacuity caveat, title-probe enforcement matrix, dropped-rule unsupported markings, repro commands) + "no time-conditioned rules" product finding with STOP disposition

## 3. New families (fable lanes after 1.1; red-first per family)
- [x] 3.1 Procedural/how-to chains — t17 + membench/procedural.py; delegated lane, independent review APPROVE (no HIGH/MEDIUM); integrated 0f1e4d3
- [x] 3.2 Quantitative reasoning — t18 + membench/quant.py (Decimal-only, unit table, tolerance); delegated lane, review APPROVE (no blocking); integrated with this commit
- [x] 3.3 Negation & counterfactuals — t19 over existing DISPROVED/REVOKED; delegated lane, review APPROVE (no HIGH/MEDIUM); integrated ff2d324
- [x] 3.4 Cross-lingual facts — t20 + paired Cyrillic/Latin syllabary; Codex lane, review APPROVE (conditional follow-up applied as the orchestrator correction: basic-memory renderer identity for non-Latin names); integrated b3b17eb. Profile-side unsupported-not-zero reporting stays with the harness (task 1.3/4.x)
- [x] 3.5 Preference attribution + source reliability — t21/t22 + oracle-derived builders; Codex lane, review APPROVE (no blocking); integrated with this commit
- [ ] 3.6 Long-horizon 52-week entropy release + quarterly health snapshots
- [ ] 3.7 Multimodal depth under the media profile (real PDF, OCR image, audio transcript; degradation without extras)

## 4. Industry-standard packaging
- [x] 4.5 **BEFORE 4.1/4.2 pin release bytes:** sub-day temporality family — **done in `1a6363b`**, verified 2026-08-07: `templates/t23_sub_day_temporality.py` carries both required shapes — `same_day_latest` (six same-day entries with distinct instants, so intra-day order decides the answer) and `same_instant_indeterminate` (`expect_indeterminate_order`, `abstain=True`, because coinciding instants make the order genuinely underivable rather than merely unknown). A day-granularity store cannot order the first and must abstain on the second; `test_control_query_is_answerable_at_day_granularity` pins the discriminating control, so abstaining on everything is not the same as knowing when to abstain. 54 tests green. The ordering constraint on 4.1/4.2 is released — but see 4b.32 and 4b.7, which now hold that gate instead. Original text: sub-day temporality family. Corpus knowledge time was `recorded_week` (0..11), so the benchmark was structurally blind to intra-day ordering — it could not detect that the product records its own knowledge time to the day. Product-side handoff: `docs/handoff-note-timestamps.md` Corpus knowledge time is `recorded_week` (0..11), so the benchmark is structurally blind to intra-day ordering — it could not detect that the product records its own knowledge time to the day. Needs finer recorded time on `Assertion`/`StatusSpan` plus a family where two records land on the same day and their order decides the answer, so a day-granularity system must abstain rather than guess. Changes generated bytes, hence the ordering constraint. Product-side handoff: `docs/handoff-note-timestamps.md`
- [ ] 4.1 Replication kit (pinned seeds, one-command regeneration, hash verification) proven on a clean checkout
- [ ] 4.2 Versioned corpus releases + changelog; held-out seed documented
- [ ] 4.3 Publication gate implementation in reporting (judged-dimension block without agreement data; profile/version/replication labels on every figure)
- [x] 4.4 Judge–human agreement — **stood down as moot, not abandoned.** Protocol and blind sample were built and are retained (`membench/agreement.py`, 26 tests; stratified 40 judged + 10 control; Cohen's κ; labeller/judge input parity pinned by test). 17 human labels collected and kept at `benchmarks/judge-agreement/labels-human.json` as evidence for the record. Measuring human agreement with `semantic_match` stopped being worth a person's attention once 4b.19 disqualified that dimension on structural grounds. Reinstate this machinery unchanged if any judged dimension is ever reintroduced

## 4b. Review follow-ups (raised by independent reviews; none blocking at integration)
- [x] 4b.1 BEFORE packaging pins release bytes: t22 `s_fresh` declares `supersedes_source`/`version=4` over an edition whose claim is NOT superseded and which still backs the current answer — decide the topology (t13 uses the field with the opposite meaning) since fixing it later changes generated bytes — **decided + implemented**: a source-supersession edge means the replaced edition's claims are retired (t13's meaning); `s_fresh` declares no edge and stays `version=1`; `expect_value_with_correction_history` now refuses at generation if a fresh source declares supersession while carrying a live claim. Two independent reviews confirmed the refusals bite
- [x] 4b.2 BEFORE cross-profile comparison: t20's variant discriminator appends a marker instead of redrawing, so 158/1000 seeds yield near-duplicate entity names (a retrieval confound scored as cross-lingual failure); use the redraw-until-distinct idiom from t19 — **done**: digest-partition redraw (variant *v* accepts only slot-*v* names, disjoint by construction rather than probabilistically). Measured near-duplicates 158/1000 → **0**; mutation-verified red against the old marker scheme; template isolation re-verified empirically
- [x] 4b.3 Discriminating calibration axis for source-reliability: `hedged=False` on the clean system-of-record metric (today every t22 query except one is `hedged=None`, so a provider that hedges everything is never penalised) — **done**: `expect_settled_value` refuses unless the corpus warrants a flat answer. Discrimination proven two-sided — hedge-everything fails `clean_metric`, hedge-nothing fails `fresh_unconfirmed`; no constant hedging policy passes t22
- [x] 4b.4 Declared-`gates` accuracy in expected records (t21 as-of query declares `current_state`; `holder_opinion` declares `calibration` while it evaluates not-applicable) — `expected.jsonl` is a published artifact — **done for t21/t22** via a `_state_gate` helper mirroring the scorer; all 32 records guarded by a declared-vs-evaluated test. Generalisation to the other six templates is 4b.15
- [x] 4b.5 Test-honesty cleanups: t20's 1,000-seed uniqueness assertion is tautological (the genuine guard is the single-seed test); add the stronger wrong-source citation negative and pin the same-script control's value/citation equality; add the both-values stale-echo variant to the procedural golden — **done**: tautological 1,000-seed assertion replaced with one driving `build()`; same-script control now pins value/citation/ask equality; procedural golden gained the both-values stale-echo case (proven to slip `gate_value` and be caught by `gate_state`). Residual weakness in the wrong-source negative is 4b.17
- [ ] 4b.23 **Confirm judge discrimination on real response text, not synthetic sentences.** The 19/19 result used clean one-sentence candidates where direction is unambiguous; the real rows are multi-document dumps. Repeat the direction swap on the actual response text — that is an upper-bound result until it does. Report the scoped dimension with this caveat attached rather than as settled capability
- [x] 4b.24 **Cause: `EXOMEM_DISABLE_CLIP=1` zeroes text retrieval without product fix `91b016f`.** A disabled lane never fails, so the BM25-only fallback at `find_candidates.py:242` never triggers and the strict retention seam vetoes every candidate lacking ALL query stems — which no interrogative phrasing satisfies. Verified by overlaying that commit's `find.py`: **0 hits without it, 52 with it**, same 20 queries and vault where the August baseline recorded 52 — an exact reproduction. Invisible for five withdrawn root causes because the adapter applies profile settings temporarily while `capture_environment` reads ambient `os.environ`, so artifacts recorded only `EXOMEM_DISABLE_EMBEDDINGS`, and every control faithfully reproduced the manifest profile — carrying the bug inside itself
- [ ] 4b.26 **Capture effective profile settings where they are applied.** The environment gate reads `env_knobs` from ambient `os.environ`, outside the adapter's `_set_env`/`cleanup()` window, so it records only what the caller exported and would miss the 4b.24 class again. Capture what the adapter actually applied
- [x] 4b.27 **Merge product fix `91b016f` to `origin/main`** — **done**, verified 2026-08-07: landed as **`888eaab`** ("fix(find): recover degraded-profile retrieval with content-anchored majority-coverage retention (#378)"). Note for successors: it was **squash-merged**, so `git merge-base --is-ancestor 91b016f origin/main` reports false and the box looks open to a naive check — verify by commit message or by the behaviour, not by ancestry. Original text: (branch `fix/lexical-degraded-retention`, authored 2026-08-01, unmerged; `origin/main` was at `77ebc01`) (branch `fix/lexical-degraded-retention`, authored 2026-08-01, unmerged; `origin/main` is at `77ebc01`). It is both cause and cure of the zero-hit failure, and its own commit message cites this benchmark (`factual_qa 0/180`). `EXOMEM_DISABLE_CLIP=1` is a correct determinism pin and stays — it merely requires this fix. **Action is a merge, not a patch.** Until it lands the retrieval floor keeps the failure loud
- [ ] 4b.28 **Track D J1 anchor check disagrees with the product contract.** `test_j1_longitudinal_evolution_green` asserts `topic_anchor == v1_path` (oldest), and `journeys.py:17` documents "`topic_anchor` = oldest path". The product does not do that: `evolution_for_path` sets `canonical_path = get_page(vault_root, path=path).path`, and `get_page` follows supersession to the active head, so requesting v1 returns v3. Surfaced when the retrieval fix (#378) was merged in and real retrieval resumed. **Not established whether the fix changed this or merely exposed it** — determine that before deciding whether the benchmark's documented contract is wrong (likely) or the product's anchor resolution is (possible). Do not re-calibrate the test to whatever currently happens without settling which side is correct
- [ ] 4b.30 **A profile that asks for the semantic lane must get it or refuse.** exomem soft-degrades when sentence-transformers is missing ("embeddings disabled (import failed) ... keyword-mode find() still works"), which is right for a product and catastrophic for a measurement: the run completes, reports `invalid=False`, and stamps the manifest `profile: recommended-embeddings` over numbers **bit-identical to the lexical run**. A false label on a real number is worse than a wrong number. This has now silently produced mislabelled runs three times (two superseded in the v0.1 findings, once again 2026-08-05). Guard added in `exomem_local.setup`: when the profile does not disable embeddings, `get_model()` must load or the run is an environment fault — never a contender loss. Extend the same shape to any future capability-tier profile (see 4b.29)
- [ ] 4b.31 **Provenance measures the shared answerer and inversely rewards retrieval quality.** `scoring/extractive.py` cites its top-3 hits unconditionally (`_TOP_HITS = 3`), so a contender that retrieves on every query emits ~3 citations per answer against a permitted set averaging ~1.6-2.7 and fails precision everywhere. Measured: exomem 2.98 citations/answer over 236/236 queries with hits -> **0/208**; basic-memory 2.14 over 188/236 -> 24/172. Retrieving better scores worse. Neither number is a product statement. Either the answerer must cite only what it actually used, or provenance must be scored against a contender's own citations rather than a harness-constructed answer — decide which, because today the column is unpublishable. **First candidate rule measured and REJECTED (2026-08-07).** An uncommitted max-query-term-coverage `select_hits` was found in this worktree (preserved at `.task/4b31-max-coverage-select-hits.patch`) and tested: applied to a clean HEAD it breaks 12 judge-wiring tests, and on the t00 fixture it converts the four honestly-undecidable `current_state` rows into **four false FAILs** — evidence `required CLM-5EFCEBA2 value '2025-03-28' absent`, one citation each, the *superseded* source every time. The query asks for the **current** deadline; the stale document repeats more query nouns than the terse revision memo, so a lexical argmax prefers it. A contender with perfect retrieval scores those 4/4 FAIL, and it penalises every contender identically, which is why nothing flagged it as asymmetric. The lesson generalises past this one rule: the old `_TOP_HITS = 3` was recovering the current value at rank 2–3 purely by casting a wider net, so **narrowing for citation precision necessarily costs answer recall** — one shared extractive answerer cannot serve both. That is a structural argument for the second option (score against the contender's own citations), not merely a defect in this rule. The gates.py half of that same uncommitted work was independent and IS adopted (`ecf6bde`)
- [ ] 4b.35 **Track B ingests at raw-source altitude, so the provenance chain it scores cannot exist.** Measured on the 2026-08-07 full-strength run's vault: **205 pages under `Sources/`, ZERO compiled notes** (`Notes/` holds only `index.md`), `ingested_into: []` on **204 of 204** sources, **0** occurrences of `derived_from` anywhere, and no compile step in the adapter at all. Exomem's provenance apparatus — a compiled conclusion declaring the `sources:` it drew from, with `ingested_into:` written back on each source, plus `derived_from`/`evidenced_by` and `provenance_report` over that graph — is therefore **structurally inert in Track B**. That matters twice over: (1) the provenance dimension can never show exomem's actual advantage over a search-only competitor no matter which answerer scores it, because the structure the advantage consists of is never created; and (2) at raw-source altitude "provenance" degenerates to *which documents did you return*, which is the same shallow thing for every contender — so the column is comparable but uninformative rather than unfair. Track D **does** exercise the real chain (`capture_source` → `remember` with `sources=<path>` → correction propagation, `trackd/journeys.py:481,510,575`), which is where the claim currently lives and where it should be published from. Two required actions: **(a)** make ingestion altitude a declared, recorded run property and put it in the fairness gate — falsification-register item 2 mitigates altitude asymmetry with an ingested-doc-COUNT parity check, which is blind to altitude by construction, and `graybox-local` already declares "raw-inbox altitude" in prose while `exomem-local` runs at the same altitude silently; every published provenance figure must carry it. **(b)** Decide whether Track B grows a deterministic compile step — the oracle already knows every claim and its sources, so the harness can author compiled notes citing known sources with no model in the loop, which would create the chain and make provenance measure chain preservation rather than result-list membership. That changes the ingestion protocol and therefore release bytes, so it belongs before 4.1/4.2 if it happens at all
- [ ] 4b.34 **Track D journeys break at midnight — three tests changed verdict on a date rollover with no code change.** Observed 2026-08-07→08: `test_j2_correction_propagation_green`, `test_j1_wrong_order_variant_fails_chain_check` and `test_j3_weekly_review_live_green` were green on the 7th and red on the 8th, and the same three are red at the *previous* commit, so nothing in the tree caused it. Symptoms are wall-clock dependent: `remember` operations report empty-detail failures and the review queues surface a source path stamped with today's date (`Knowledge Base/Sources/Sessions/2026-08-08-session-cap-brief.md`) that the stale/unprocessed/attention expectations do not account for. `trackd/runner.py:67` stamps `datetime.now(dt.UTC)` and the journeys plant date-named artifacts, so the fixtures and the assertions disagree about "today" whenever a run straddles a UTC day boundary or the planted dates age. **This is a replication-kit blocker, not a flake:** 4.1 promises one-command regeneration proven on a clean checkout, and a suite whose verdict depends on the calendar cannot be reproduced by anyone tomorrow, including us. Fix by injecting the clock (the corpus side already does this — see `membench/clock.py`) so journeys run against a pinned instant, and make any date-named fixture derive from that instant rather than from `now()`
- [ ] 4b.33 **`contradiction_uncertainty` cannot be passed by anything — ceiling 0, floor 0.** Found by the oracle-retrieval ceiling on its first run (`20260807T180456Z`, seed-1): a contender returning exactly the oracle-permitted sources scores **0/20**, identical to the null floor, so the dimension has zero discriminative range. Cause: `gate_calibration` requires **hedged language in the answer text**, and the shared extractive answerer only quotes stored source text verbatim — it has no generative step and cannot produce hedging under any retrieval. This is the 4b.31 class exactly (a dimension measuring the shared answerer rather than the products), and it is currently misreported: the v0.1 findings doc reads the row as a shared product capability gap — "Neither system does disputed state… it discriminates nothing, because nobody passes it" — when the truth is that nobody *can*. Decide between (a) scoring calibration against something an extractive answerer can express (e.g. co-presence of a documented dispute in the quoted text, which is a retrieval property), or (b) reporting the dimension as not-measurable until a generative answerer exists, the way unsupported capabilities are reported everywhere else. Do **not** leave it published as a product finding. The bounds table now renders it `VOID` so it cannot silently return
- [ ] 4b.32 **BEFORE packaging pins release bytes: canonical entity names collide suite-wide, and three prompt pairs are byte-identical with mutually exclusive expected answers.** Seed-1 draws 108 entities under 89 distinct canonical names: **18 names are shared by 37 entities** (`Vanta Partners` ×3; `Kelva Labs`, `Lumo Partners`, `Vanta Works`, `Project Mossfield`, `Norva Dynamics`, `Project Mossrun`, `Kelva Systems`, `Kelva Works`, `Coril Group`, `Project Sablegate`, `Project Sablefall` ×2 each, and six more). 74 of 240 queries name a colliding entity, across all seven families. The sharpest consequence is in `t07_authority_conflict`: three prompts are byte-identical across two scenario instances while their expected values are mutually exclusive (173 vs 149, distinct claims `CLM-A03C7D32`/`CLM-4E3A8893` and distinct sources `SRC-63F693C2`/`SRC-CA79C170`), so the corpus asks one question and grades two different answers. This is the 4b.2 class, which was fixed **for t20 only** by digest-partition redraw; the wordbank itself never got the treatment. **Currently masked, not harmless:** measured on the 2026-08-05 embeddings run, colliding queries score the same as clean ones (factual_qa 81% vs 83%, temporal 68% vs 70%, abstention 78% vs 75%) and 4 of the 6 colliding t07 queries pass — because the extractive answerer dumps whole documents and `gate_value` matches by substring, so both values reach the answer text and each query finds its own. The defect is invisible only because the shotgun answerer and 4b.14's substring matching cancel it. That cancellation is fragile in exactly the direction the suite is heading: any narrowing of the answerer (see 4b.31) converts these into real failures, and a published corpus containing one prompt with two contradictory graded answers is indefensible regardless of what today's score happens to be. Fix by extending t20's disjoint-by-construction name draw to the shared wordbank, plus a generation-time refusal for duplicate canonical names and a suite-wide test that no two queries share prompt text while declaring different expected values. Changes generated bytes, hence the ordering constraint
- [ ] 4b.29 **BLOCKS any published comparison: profiles degrade only exomem.** `neutral-lexical` is a bag of `EXOMEM_*` env vars, meaningless to a contender, so the profile switches off our semantic lane and leaves competitors at full strength. Confirmed from artifacts: basic-memory's workdir holds `fastembed_cache/models--qdrant--bge-small-en-v1.5-onnx-q` while exomem ran `EXOMEM_DISABLE_EMBEDDINGS=1`. Mirror image of the renderer defect (which rigged the harness in our favour) — same disease, opposite sign. A profile must express a **capability tier each adapter implements for itself**, with adapters declaring whether they can honour it, and a run that cannot apply the tier to a contender must refuse the comparison rather than silently compare unequal configurations. **Knob-by-knob audit (2026-08-08) — only three of the nine are the problem, so the fix is surgical rather than a rewrite.** *Capability amputations, which degrade exomem alone and must move behind a declared tier:* `EXOMEM_DISABLE_EMBEDDINGS` (removes the semantic lane outright — the whole defect), `EXOMEM_DISABLE_CLIP` (removes the image lane), `EXOMEM_DISABLE_MEDIA_EXTRACTION` (removes OCR/ASR/PDF extraction, which silently guts the `multimodal` family and the planned `multimodal_depth` one). *Legitimate determinism pins, which change no capability and should stay:* `EXOMEM_DISABLE_WARMUP`, `EXOMEM_DISABLE_FILE_WATCHER`, `EXOMEM_DISABLE_MODE_WATCH`, `EXOMEM_DISABLE_CORPUS_CACHE` (caches and background loops, not behaviour), `EXOMEM_DISABLE_RELEVANCE_CHECK` (gates only the usage-boost ranking multiplier at `usage.py:102`, and is a no-op on a fresh benchmark vault whose read/cite logs are empty — it does **not** suppress abstention, which was checked directly because it would otherwise have invalidated the abstention finding), and `EXOMEM_DISABLE_RANKING_CONFIG` (forces `DEFAULT_RANKING` over any committed `ranking_config.json` — actively *pro*-fairness, since a locally tuned ranking file would be a thumb on the scale). State the multimodal consequence when publishing: any multimodal figure measured under this profile measures the amputation, not the product
- [ ] 4b.25 **Five real-adapter tests are red by design** (`test_membench_adapter_exomem` leaf-run, four in `test_membench_governance_wiring`): they assert `not result.invalid` over runs that retrieve nothing, and the floor guard now catches that. Four are governance tests where returning zero documents trivially satisfies "the withheld content was not returned" — independently confirmed by a zero-hit run publishing `governance: {pass: 16, fail: 0}`. Left red as the loudest signal this machine cannot retrieve; a `pytest.skip(invalid_reason)` would mask a genuine product regression later. They go green when a Python 3.12 environment exists (the remaining half of 4b.24). Do not paper over them
- [x] 4b.21 **Self-defeating forbidden values — unwinnable queries, worst defect class (6th instance).** 4 `identity` queries name the old project in the prompt ("the project once called Project Driftreach") while `gate_state` forbids that exact string, so no possible correct answer can pass: a response reading "Project Driftreach is renamed to Project Emberfall" is failed for echoing the question. Guaranteed failure for every contender forever. Fix by excluding forbidden values that occur in the query text, or by scoping the absence check to assertion position rather than whole-text substring — **done in `ae0a6ff`**: a forbidden value the query itself supplies is excluded and named in the evidence (`forbidden value supplied by the query prompt, not measurable`). The 4 unwinnable identity queries now pass with a correct answer and still fail with a wrong one
- [x] 4b.22 **`gate_state` absence rule is mode-dependent and applied uniformly.** "Forbidden value absent from answer text" presumes an assertive QA answer; this run's extractive answerer returns retrieved documents, where returning a record *with* its supersession memo ("the earlier decision is fully reversed … is now Lumo Group") is reasonable retrieval behaviour. 23 of 120 state-gate failures (19%) are responses carrying every required current value — mini_smoke 4, temporal 8, maintenance 4, identity 4, multimodal 3. Every current_state/as_of figure in the lexical profile inherits this. Needs a mode-aware rule (assert-position for QA, resolved-vs-unresolved-conflict for retrieval) rather than one substring test for both — **done in `ae0a6ff`**: co-presence with a documented predecessor is UNSUPPORTED, not FAIL, via `oracle.superseded_toward` walked hop-by-hop from `truth_at().superseded_by`. Stated cost: a reversal asserted backwards is now UNSUPPORTED rather than FAIL
- [x] 4b.20 **Scope `semantic_match`, do not remove it** (supersedes the original 'remove' framing). Score the judged dimension only on rows where a deterministic gate reports UNSUPPORTED: non-redundant by construction, since where `gate_value` decides it is κ=0.989 redundant and where `gate_state` is UNSUPPORTED it is the only thing that can rule. Deterministic gates stay final — the judge never overrides one, which is what contains the measured fabrication risk. Wire the judge into `runner.py` (it is currently called from tests only) and label the dimension with the 4b.23 caveat until that measurement lands — **done in `25c37b0`**: judged verdicts are a separate type in a separate file under a separate dimension, resolvable only on `JUDGE_RESOLVABLE_GATES = {current_state, as_of}` UNSUPPORTED rows, judge phase runs last so `deterministic-scores.json` is byte-identical with or without it
- [ ] 4b.18 **Blinding is defeated by structural fingerprint — affects the real judge pipeline, not just the agreement sheet.** `leakage_scan` matches known tokens (product names, sentinels, vault paths) and returns 0 hits on text whose YAML frontmatter key set (`type:`, `[system]_id:`, `source_type:`, `captured:`, `tags:`, `ingested_into:`) identifies the product outright; `[system]_id` additionally advertises that a product-named field was scrubbed. Found by a human recognising the vendor at a glance across 84 documents. Since `normalize_for_judge`/`leakage_scan` gate real judge requests, a judge in a multi-system comparison can identify systems by schema shape — the fairness property blinding exists to guarantee. Needs schema-shape normalisation (canonical key names/order, or strip frontmatter entirely) plus a leakage check that tests *structure*, not just tokens
- [x] 4b.19 **Resolved — the judge earns its place, scoped.** Third and final position. With `gate_state` corrected (`ae0a6ff`), 19 rows report UNSUPPORTED — the only territory a judge can add to. Controlled probe over those 19, one variable changed (supersession direction reversed), two prompts: **19/19 discriminated correctly under both**, 0 false positives on the backwards variant. The judge reads which value is asserted as *current*; no deterministic rule here can (assert-position was examined and rejected because `expect_change` requires both values present). Prompt framing is not the variable — plain and strict identical, as in the earlier 44/44 zero-flip rerun. Redundancy result (κ=+0.989 vs `gate_value`) and the single fabrication both still stand and shape the scoping
- [x] 4b.14 **Systemic**: `gate_state` and `gate_value` match values by bare substring (`gates.py` — `value in text`, `any(v in text for v in values)`), so a forbidden `10` fires on `105`/`2010`/`SRC-10` and an expected `10` passes on `105`. Both directions are wrong: it punishes correct answers and passes wrong ones. Found via t20 (archive 105 aliasing routing 10, 5/1600 pairs — the range was capped to two digits as an immediate stopgap, but the scorer is the real defect). Needs token/numeric-boundary-aware matching, reusing the `quant` Decimal path where the value is numeric — **done in `ae0a6ff`**: `states_value()` is now the single matching rule for both `gate_value` and `gate_state` — numbers through the quant Decimal path against standalone tokens, other values on word boundaries
- [ ] 4b.15 Generalise the 4b.4 declared-gate fix beyond t21/t22: `base.expect_value` still hardcodes `gates=["current_state","citations"]` and `expect_superseded_history` hardcodes `["as_of", …]`, so **20 records with `world_week` set still declare `current_state`** (t03, t04, t06, t10, t19) and **28 records with `uncertainty.hedged` set still omit `calibration`** (t05, t08, t09, t19) — the identical defect, unfixed in six templates. `expect_settled_value` also inherits `current_state` rather than using `_state_gate`, so it misdeclares the moment t22 gains an as-of ask
- [ ] 4b.13 Fold into 4b.10 (entangled, do not patch alone): `expect_derived_quantity` (`builders_ext.py:302-330`) sets `required_citations` from two claims but leaves `required_claims=[]`, so those 12 t18 records have no claim basis and citation precision is unverifiable — the only records in the suite where a shotgun still passes. The one-line fix makes `gate_state` newly applicable there, which is exactly what 4b.10's declared-gates validator must settle
- [ ] 4b.16 `ExpectedRecord.transitive_citations_ok` is published in `expected.jsonl` and read by nothing; `permitted_citations` implements exactly the transitive-citation policy the flag was meant to gate, and ignores it. Same class as 4b.10 — fold into that lane
- [ ] 4b.10 `ExpectedRecord.gates` is unvalidated fiction in a **published** artifact: nothing outside the builders reads it (only `builders_ext.py:353,408,736` append to it); it declares 7 names the scorer never emits (`change_explanation`, `conflict_awareness`, `disproof_awareness`, `retraction_awareness`, `unit_conversion`, `declassification`, `derived_value`); and it never declares `value`, a real gate that scores most records. Split into validated deterministic `gates` (checked against the scorer's registry at generation time) vs `judged_dimensions`, and make the runner consume the declaration rather than ignore it
- [ ] 4b.11 Suite-wide source-supersession invariant guard (every source with `supersedes_source` ⇒ all claims asserted by the superseded edition are retired) in `tests/test_membench_families.py`; today only t22 is guarded by its own test + a generation-time refusal, so a future template can silently re-introduce the 4b.1 contradiction
- [ ] 4b.12 Pin the undocumented declaration conventions the t21/t22 audit exposed: `value` is declared nowhere suite-wide (treated as subsumed by the state gate); `gate_abstention` is applicable on nearly every record but declared only on abstain-required ones; `correction_history` runs under a gate *named* `current_state` while requiring superseded values present. Document or fix — currently each is convention-by-accident
- [x] 4b.8 Citation gate measures recall only (`gate_citations`, `scoring/gates.py:138` — required ids ⊆ answer.citations, extras never penalised), so a contender that shotguns every source id passes provenance on every citation query. Provenance is currently free for all contenders, Exomem included. Decide and implement the precision rule + report precision alongside recall — **done after three iterations and two independent reviews**. Precision measured against an oracle-justifiable permitted set = `required_citations ∪ evidence_neighbourhood`, closed over bidirectional `derived_from`, supersession partners, and *reference-resolving* same-entity claims (object value names an entity in the closure — structural, not a predicate allowlist). Iteration 1 was too narrow (punished `cite_both_sides` and multi-hop provenance); iteration 2 too wide (entity edge supplied 40% of permitted sources, cite-everything-about-the-entity passed 83%); iteration 3 measured 1% / 4 records. Whole-corpus shotgun: 280 FAIL / 12 UNSUPPORTED / 0 PASS. Precision now computed whenever a claim basis exists (measurable 252 → 280). Unverifiable precision reports UNSUPPORTED, never PASS. `runner.py` wires `entities_by_id` — without it 128/240 records report UNSUPPORTED instead of measuring (verified before/after)
- [ ] 4b.17 `test_citing_the_wrong_source_fails_even_with_the_right_value` (crosslingual) claims to prove failure is "not merely a missing citation", but the answer it builds cites *only* the wrong source, so it fails on recall and never exercises precision — it survives the `evidence_neighbourhood → ()` mutant untouched. Rebuild it as required-citations *plus* the wrong source, so precision is what decides
- [ ] 4b.9 Cleanups from the t20 lane: `wordbank.org_name_cyr(discriminator=)` and `_CYR_DISCRIMINATOR` are dead API with no caller; consider a Latin-script decoy source in t20 so the wrong-language citation negative becomes expressible (today no Latin source exists by construction, so the lane substituted sibling-ledger negatives)
- [ ] 4b.7 BEFORE packaging pins release bytes: the release-manifest check is environment-pinned — it byte-compares the whole `manifest.json`, which embeds `renderer_versions`, so a Pillow patch bump reports corpus drift while all 28 artifact hashes (incl. PNG) are provably identical; split into corpus-identity (hard fail) vs environment-provenance (reported), and make `pillow_version()` degrade like pymupdf so text-only generation stops hard-failing without the media extra. Measured + dispositioned in the findings doc (2026-08-04 addendum)
- [x] 4b.6 Latent edges: `quant._canonical` emits E-notation below ~1e-6; `ExpectedAnswer.tolerance` is carried but no gate consumes it; `_correction_chain` uses `current_truth` instead of the as-of-aware view; t20 `archive_count`/`routing_count_new` ranges overlap (12/2000 variants) — **done** (all four): `_canonical` forces fixed-point notation; `ExpectedAnswer.tolerance` now consumed by `gate_value` through `quant.within_tolerance`; `_correction_chain` split into an as-of-aware reported view and a knowledge-week structural view; t20 ranges disjoint by construction (and later capped to two digits, see 4b.14)

- [x] 4b.36 **`gate_state` scored 28 absence-only records it was never asked to judge — 24 of the ceiling's 44 temporal failures.** Found by the oracle-retrieval ceiling (`20260808T093458Z`, seed-1, raw altitude). Seed-1 carries 28 records whose whole expectation is absence: no required claims, one forbidden claim, `abstain=True`, `answer.kind="none"`. Their own declared `gates` list names `abstention` and the awareness gates and **never `current_state`**, but the runner ignores the declaration (4b.10), so `gate_state` ran anyway. The shared answerer is extractive, so it quotes the retrieved source, so the forbidden value is necessarily present, and with no required claim there is no successor for a supersession to excuse it — a guaranteed FAIL for **any** contender including one that retrieves perfectly, which makes it a harness defect by the ceiling's own definition. Fixed in `32b9932`: no required claim means no state question, so the gate returns NOT_APPLICABLE. This **reverses** `test_absence_only_records_still_fail_on_the_forbidden_value`, whose argument (surfacing the value *is* the failure) holds against a native answerer but not an extractive one — and the gate already conceded exactly that in its co-presence branch, where quoting a retired value is UNSUPPORTED because assertion-versus-report "is a fact about rhetoric" no deterministic rule can read. The two branches disagreed; the ceiling broke the tie. The test is rewritten to record the reversal, not deleted. NOT_APPLICABLE rather than UNSUPPORTED on the corpus's own evidence — these records do not declare the gate, and holding them as UNSUPPORTED would keep them in the temporal denominator while `gate_abstention` scored them again, pricing one judgement twice. Measured, one variable changed: temporal **192/44 (u=52, n/a=64) -> 188/20 (u=52, n/a=92)**, every other dimension byte-identical, abstention included. The four lost passes were absence records that happened not to quote the value and banked a free temporal pass; removing those is the same correction in the other direction
- [ ] 4b.37 **The remaining 8 ceiling temporal failures are `negation_counterfactual` co-presence, and the obvious fix would also excuse a genuinely wrong answer.** After 4b.36 the ceiling's 20 temporal failures are fully attributed: 12 are numeric values held in PNG/PDF artifacts the text-only ceiling documents itself as unable to read (cited, not quotable — not a defect), and 8 are t19 records that **do** name a required claim, where the forbidden value is a *counterfactual alternative* rather than a retired predecessor. The extractive answerer quotes both, `oracle.superseded_toward` finds no chain because none should exist, and the row fails. This is the same quoting-is-not-asserting problem the UNSUPPORTED branch already concedes, one level down — but extending the co-presence excuse to forbidden claims with no supersession relation would also excuse the t15 prior-burn-score case, a separate current claim where putting the wrong reading forward is provably wrong and which `test_a_forbidden_value_with_no_documented_supersession_still_fails` exists to guard. Needs a distinction between *counterfactual alternative* and *competing current claim* that the oracle does not currently carry. Design-sensitive: do not recalibrate the test to whatever currently happens
- [ ] 4b.38 **Compiled altitude changes what is written and what one gate reads, but not what is retrieved — so there is no compiled-altitude ceiling.** `OracleRetrievalAdapter.search` serves raw source artifacts and never consults `self.altitude`; the retrieval artifacts of the raw and compiled ceiling runs (`20260808T051905Z` / `20260808T051851Z`) are byte-identical at 528 hits. The contradiction_uncertainty jump from 4/36 to 40/0 therefore came from `gate_calibration` gaining the corpus's declared dispute structure at compiled altitude, **not** from the ceiling retrieving compiled conclusions — and temporal being byte-identical across the two runs is the necessary consequence, not a coincidence. The ceiling proof recorded in `949c2eb` is sound as far as it goes but must be described as proving the gate is *satisfiable* at compiled altitude, not that the altitude is realised end-to-end. Real contenders differ: their `search` hits the product, so compiled conclusions written by the renderer genuinely come back. That asymmetry means contender runs at compiled altitude currently have **no denominator** — the exact gap the ceiling was built to close. Blocks publishing any compiled-altitude figure

## 5. Validation
- [ ] 5.1 `openspec validate expand-memory-proof-benchmark --strict`
- [ ] 5.2 Lean membench suite green incl. new family tests (<60s each, offline)
- [ ] 5.3 Determinism: double-generation equality incl. new families; template isolation unchanged for v0.1 templates
- [ ] 5.4 Full-suite corpus regeneration matches an updated committed release manifest
