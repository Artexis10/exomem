## 1. Request budget primitive

- [x] 1.1 Red first: `tests/test_request_budget.py` asserts that `mcp_request_context` creates a budget whose deadline is entry plus the default 50 s, that `request_budget.current()` is `None` outside an MCP call, that `EXOMEM_MCP_REQUEST_BUDGET_SECONDS` overrides the default, and that a malformed override falls back to the default with one warning.
  - Red: `ImportError: cannot import name 'request_budget' from 'exomem'` (collection error, 1 error in 1.25s).
  - Green: `tests/test_request_budget.py` 14 passed in 0.61s.
  - Files: `tests/test_request_budget.py`.
- [x] 1.2 Add `src/exomem/request_budget.py` (PROVISIONAL constants in one place: `MCP_REQUEST_BUDGET_SECONDS = 50.0`, `DELIVERY_RESERVE_SECONDS = 5.0`, `RERANK_RESERVE_SECONDS = 8.0`, `RERANK_COLD_RESERVE_SECONDS = 25.0`, `PACK_RESERVE_SECONDS = 10.0`, `GRAPH_ENRICH_RESERVE_SECONDS = 5.0`), the `RequestBudget` object (`deadline`, `remaining()`, `can_afford(reserve)`, `note_skipped(stage)`, `note_truncated(stage)`, `as_response_block()`, `as_ledger_block()`), the context variable, and the reconcile-class exemption list.
  - Files: `src/exomem/request_budget.py` (leaf module, stdlib only, like `call_spans`). Shipped values are exactly the six named above. The malformed-override warning is emitted once per distinct raw value via a memoized resolution, so a typo in a deployed variable is visible without flooding the log.
  - Green: `tests/test_request_budget.py` 14 passed in 0.61s.
- [x] 1.3 Create the budget in `command_surface.mcp_request_context` beside the call token; exempt reconcile-class tool names at the dispatch site; red-first test that `reconcile` and `maintain_memory(mode="reconcile")` see no budget.
  - Red: same collection error as 1.1 (`test_reconcile_class_calls_are_exempt`, `test_maintain_memory_outside_reconcile_mode_is_budgeted` unrunnable).
  - Green: `tests/test_request_budget.py` 14 passed in 0.61s.
  - Files: `src/exomem/command_surface.py:698-729` (`mcp_request_context` gains keyword-only `tool` / `arguments` and binds the budget), `src/exomem/server.py:191-198` (dispatch passes the tool name and arguments). The exemption predicate mirrors `writer_lease.py:4000-4001` (`joins_unbounded_graph`) so the two cannot drift.

## 2. Heavy recall stages yield

- [x] 2.1 Red first: `tests/test_find_budget.py` drives `op_find` under a budget with too little remaining for rerank and asserts rerank is skipped, `budget.skipped == ["rerank"]`, and the hit order equals the unbudgeted call's order; same for `graph_enrich`; same for the deep pack with `budget.truncated == ["pack"]` and the pack a prefix of the unbudgeted pack.
  - Red: `tests/test_find_budget.py` 6 failed, 5 passed in 11.13s — the six budget assertions failed (`assert [] == ['rerank']` and the pack/graph_enrich equivalents); the five that passed are the controls that must not change (a reranker that reorders when afforded, a warm reranker inside the same budget, an unchanged in-budget call, a tight budget with nothing to skip, a local call with no budget).
  - Green: `tests/test_find_budget.py` 11 passed in 9.77s.
  - Files: `tests/test_find_budget.py`. "The unbudgeted call's order" is tested as the `rerank=False` order with a stubbed reranker that visibly reorders, so a skipped rerank is distinguishable from a half-applied one.
- [x] 2.2 Implement the checks at stage entry in `find.py` (rerank, with the cold reserve when the reranker singleton is not resident) and `commands.op_find` (pack, graph_enrich); thread the deadline into `context_pack.assemble_pack` so it stops adding items at the deadline.
  - Green: `tests/test_find_budget.py` 11 passed; `tests/test_find*.py` + `tests/test_request_budget.py` 217 passed in 143.51s (no regression in the recall suite).
  - Files: `src/exomem/find.py:157-174` (`_rerank_reserve_seconds`, reading `embeddings._RERANKER` — the same fact `model_reaper.default_slots()` reads) and `src/exomem/find.py:4515-4529` (the check, placed last so a rerank already off for another reason is never reported as a budget cost); `src/exomem/commands.py:2438-2474` (pack and graph_enrich); `src/exomem/context_pack.py:48-55, 656-671, 727-730` (`deadline`, `_monotonic`, `DEADLINE_TRUNCATION`).
  - `graph_enrich` runs only inside `assemble_pack`, so it is checked against `PACK_RESERVE_SECONDS + GRAPH_ENRICH_RESERVE_SECONDS`: its own reserve alone would admit an enrichment the pack it rides in cannot pay for. A pack that cannot be afforded names `graph_enrich` skipped too, because it cannot run without one.
  - `assemble_pack` stays a pure function: it appends `DEADLINE_TRUNCATION` to the pack's own `truncation` list and `op_find` reads that back to call `note_truncated`, rather than the pack mutating a budget.
- [x] 2.3 Red first: a call that fits the budget returns a response byte-identical in shape to today's (no `budget` key); a call with nothing to skip under a tight budget also has no `budget` key.
  - Both assertions were green from the first run (they assert the absence the change must preserve) and stayed green after 2.2/2.4: `test_a_call_that_fits_the_budget_is_unchanged`, `test_a_tight_budget_with_nothing_to_skip_carries_no_budget_key`, `test_a_local_call_with_no_budget_skips_nothing`.
  - Green: `tests/test_find_budget.py` 11 passed in 9.77s.
- [x] 2.4 Emit the `budget` block per design D3 on `op_find` and `op_ask_memory` responses, including compact detail; one sentence in the scaffold recall reference and its plugin copy (no tool-description change).
  - Red: `test_the_compact_recall_product_carries_the_block` failed with `assert [] == ['rerank']`.
  - Green: `tests/test_find_budget.py` 11 passed; `tests/test_plugin_sync.py` + `tests/test_tool_surface_fingerprint.py` + `tests/test_scaffold_no_leak.py` 22 passed in 7.56s.
  - Files: `src/exomem/commands.py:2513-2524` (the block is computed at return and forces the envelope form) and `:2578-2580`; `src/exomem/_scaffold/_Schema/references/recall.md` + `plugins/claude-code/skills/exomem/references/recall.md` (one sentence). No tool docstring touched.

## 3. Preservation fan-out leaves the critical section

- [x] 3.1 Red first: `tests/test_client_artifacts.py` instruments `index_sync.upsert_after_write` and `epistemic_graph.refresh_paths` and asserts neither runs while the `preserve_artifacts_media` guard is held nor before the batch terminal is persisted; the batch response returns with `derived_sync` reporting the deferred work.
  - Red: `tests/test_client_artifacts.py -k "media_fanout or fast_acknowledgement_session"` 2 failed, 2 passed in 5.50s — both lanes failed on `{'after_terminal': False, 'inside_media_guard': True, 'name': 'post_commit_batch_fanout', 'paths': ['proof.png.md']}`, the media sidecar's fan-out running inside the held guard and before the terminal.
  - Green: same selection 4 passed in 7.45s.
  - Files: `tests/test_client_artifacts.py` (`_FanoutWatch`, `_watch_media_fanout`, `test_media_fanout_never_runs_inside_the_guard_or_before_the_terminal[batch|adoption]`, `test_a_fast_acknowledgement_session_carries_the_media_sidecar[batch|adoption]`).
  - The watch judges the media sidecar's own fan-out only. The canonical artifact commit fans out too, from its own `preserve_artifacts_commit` boundary; that one belongs to `shorten-mutation-critical-section` and asserting on it would fail this test for a reason it does not describe.
  - The two fast-acknowledgement tests were green before the fix as well: with a session active, `batch_atomic_write` already suppressed the inline fan-out (`vault.py:5281`, `post_commit_fanout and fast_receipt is None`). They are regression guards for the deferral, not red-first drivers.
- [x] 3.2 Pass a `commit_guard` to `media_processing.reconcile_media` from `client_artifacts.preserve_artifacts` and `_preserve_evidence_adoption`, built as `commands._process_media` builds its own; record in this file which mechanism (fast-acknowledgement derived batch, or post-terminal fan-out) carries the media sidecar on each lane and why.
  - Files: `src/exomem/client_artifacts.py:642-661` (`_media_commit_guard`), `:1206-1213` (adoption lane), `:1487-1494` (`preserve_artifacts` batch lane); `src/exomem/media_processing.py:210-221` (`_derived_batch_carries_fanout`, `_defer_until_terminal`), `:226-256` (the `defer_fanout_to_terminal` flag), `:352-388` (`_run_fanout` and the three-way `_commit_scope`); `src/exomem/writer_lease.py:266-311` (`defer_until_terminal_persisted`, `_drain_post_terminal_fanout`), `:3852-3856` + `:3958` + `:3990` (the per-mutation queue, bound for the leaf), `:4707-4715` (the drain).
  - **Which mechanism carries each lane, measured on this tree** (instrumented run over the real `preserve_artifacts` command through `writer_lease.invoke_command`, both lanes, both flag states):
    - **With a fast-acknowledgement session (`EXOMEM_FAST_DURABLE_ACK=1`), both lanes: the derived batch.** `batch_atomic_write` prepares a derived batch for the sidecar (the `.md` sidecar beside the binary is a governed receipt path, so `prepare_active_derived_batch` returns a receipt), `complete_active_derived_batch` registers it, and `vault.py:5281` suppresses the inline fan-out because `fast_receipt is not None`. Nothing index- or graph-shaped runs during the request at all, and the terminal reports `derived_sync`. `reconcile_media` must therefore *drop* its own `deferred_fanout` here — with `post_commit_fanout=False` the writer still hands the paths back, and running them would put the work straight back on the request it was moved off. That is what `_derived_batch_carries_fanout` guards.
    - **With no session (`EXOMEM_FAST_DURABLE_ACK=0`, the shipped default), both lanes: post-terminal fan-out.** No derived path exists, so `reconcile_media` queues its fan-out through `writer_lease.defer_until_terminal_persisted` and `invoke` drains it once `idempotency.run` has returned — strictly after the terminal is persisted and acknowledged. Measured trace: `reconcile(commit_guard=True)`, `guard:enter`, `guard:exit`, `TERMINAL`, `fanout(guard=0,terminal=True,replaced=['proof.png.md'])`. Before the change the same trace read `guard:enter`, `reconcile(commit_guard=False)`, `fanout(guard=1,terminal=False,...)`, `guard:exit`, `TERMINAL`.
    - **With no mutation at all** (CLI, watcher, direct test), the fan-out runs inline as before, after the guard has been released. Dropping it there would leave the index behind with nothing to recover it.
  - `defer_fanout_to_terminal` is opt-in, so `commands._process_media` keeps exactly today's timing: it already passes a `commit_guard`, and moving *its* fan-out would change when its own `index_refresh` report is observable. The spec delta is scoped to `client-artifact-preservation`, and so is the new behaviour.
- [x] 3.3 Existing preservation, adoption and media tests stay green (`tests/test_client_artifacts.py`, `tests/artifact_adoption/`, `tests/test_media_processing.py`).
  - Green: 252 passed in 291.39s (0:04:51).

## 4. Recall spans and budget reach the ledger

- [x] 4.1 Red first: `tests/test_call_ledger.py` asserts an `ask_memory` row inside an MCP call carries `recall.*` spans with names and milliseconds only (no query text, path or excerpt appears anywhere in the row), and a `budget` block when a budget applied; a CLI `find` records no spans.
  - Red: `tests/test_call_ledger.py -k "recall_row or cli_find or budgeted_row or budget_block"` 3 failed, 1 passed in 6.75s — `AssertionError: []` (no `recall.*` span reached the row) and `KeyError: 'budget'` twice. `test_a_cli_find_records_no_spans` passed before and after: it asserts the absence the change must preserve.
  - Green: same selection 4 passed in 5.29s.
  - Files: `tests/test_call_ledger.py`.
- [x] 4.2 Bridge `find_types.timing_span` to `call_spans.record_span` when a call token is active; create `FindTimings` for every MCP `find` regardless of `include_timings`; keep response inclusion opt-in (existing find-recall-efficiency tests stay green).
  - Green: `tests/test_find*.py` 217 passed in 143.51s; ledger selection 4 passed.
  - Files: `src/exomem/find_types.py:16` and `:702-714` (the mirror, in `FindTimings.span`'s exit — the one place that sees every stage's elapsed ms, and what `timing_span` delegates to); `src/exomem/commands.py:2295-2302` (`collect_timings`) and `:2518-2520` (`timings_dict` still gated on `include_timings`, so response inclusion is unchanged).
  - Every stage, including nested BM25/vector/rerank and graph enrichment, is mirrored with inclusive timings. The ledger retains its 64-name bound and aggregates repeated names. `record_span` remains a no-op off the MCP path.
- [x] 4.3 Add the `budget` field to `call_ledger.record_call` and the row, bounded like `spans`.
  - Green: ledger selection 4 passed in 5.29s.
  - Files: `src/exomem/call_ledger.py:56`, `:271`, `:314-319`, `:399-430` (`_clip_budget`, `_MAX_BUDGET_STAGES = 16`, rebuilt field by field because the row is hash-chained), `:502` and `:541`; `src/exomem/server.py:376-384` + `:397-400` (read once, at the one recorder every exit passes through, while the request context is still open).

## 5. Writer lease, comment, docs

- [x] 5.1 Red first: `tests/test_writer_lease.py` asserts the acknowledgement deadline is the earlier of the entry-based value and the request deadline minus the delivery reserve when a request budget exists, and unchanged when none does; a canonical commit that started before the deadline still commits and persists its terminal.
  - Red: `tests/test_writer_lease.py -k "acknowledgement_deadline or looser_request_deadline or spent_request_budget"` 3 failed, 1 passed in 2.47s — `AttributeError: module 'exomem.writer_lease' has no attribute 'acknowledgement_budget_deadline'`. `test_a_spent_request_budget_still_commits_and_persists_its_terminal` passed before the change too: it pins the invariant the derivation must not break.
  - Green: with `tests/test_call_ledger.py`'s selection, 8 passed in 5.83s.
  - Files: `tests/test_writer_lease.py` (four tests, after `test_derived_acknowledgement_budget_bounds_the_waiting_only`).
- [x] 5.2 Derive the deadline in `writer_lease.invoke`; correct the `_CORPUS_CONTEXT_JOIN_TIMEOUT_SECONDS` comment in `semantic_contract.py` (value unchanged); document `EXOMEM_MCP_REQUEST_BUDGET_SECONDS` in `docs/remote-quickstart.md` next to the edge's `MCP_TOOL_TIMEOUT_MS` with the origin budget below both edge and client timeouts; recommend a configurable edge timeout below the client timeout.
  - Green: 8 passed in 5.83s.
  - Files: `src/exomem/writer_lease.py:219-242` (`acknowledgement_budget_deadline`, the earlier of the two bounds; the local at `:3806` now calls it and `:4222` consumes it unchanged, so the receipts repair's "bounds waits, never commits" shape is untouched); `src/exomem/semantic_contract.py:1307-1314` (the comment now names the 50-second origin budget, the 100-second edge and the 60-second client, and says the 15-second connector timeout it cited no longer exists — the 2.0 value is unchanged, it belongs to `bound-corpus-context-flight-join`); `docs/remote-quickstart.md` ("Request deadlines" table under step 2).
- [x] 5.3 Regenerate in order and commit together only if scaffold bytes changed: `exomem package-skills --plugin-root plugins/claude-code`; `scripts/hosted-plugin.py render` and the v5 candidate render plus `check`; `scripts/generate-capabilities.py` then `--check`. Confirm the tool-surface fingerprint did not move (D3/D7).
  - `scripts/refresh-skill-contract.py` → `refreshed skill contract 56dae028bc4b5dbfb681f245a83aa1f60d926cec67809bad22a9dd3112861e7b` (the stamp is the only change in every `SKILL.md`).
  - `exomem package-skills --plugin-root plugins/claude-code` → `Synced plugin v0.77.0 at plugins/claude-code`. The incidental `plugin.json` version bump (0.76.0 → 0.77.0) was reverted.
  - `scripts/hosted-plugin.py render` and the v5 candidate render (`--candidate hosted-alpha-agent-v5 --platform all --openai-app-id plugin_asdk_app_6a5e3d26f2b08191a04424d1c1b33fc0`) produced no diff under `plugins/hosted/`; `check` → `Hosted generated artifacts are current`.
  - `scripts/generate-capabilities.py` → `wrote docs/capabilities.md`, no diff; `--check` → `docs/capabilities.md is current`.
  - **Fingerprint did not move**: `tests/test_tool_surface_fingerprint.py tests/test_plugin_sync.py tests/test_scaffold_no_leak.py` 22 passed in 7.56s. No tool docstring was edited in this change.

## 6. Verification

- [x] 6.1 Scoped gates during iteration, each with `XDG_STATE_HOME` at a scratch directory and `-o timeout=180`: `tests/test_request_budget.py tests/test_find_budget.py tests/test_find*.py tests/test_client_artifacts*.py tests/artifact_adoption/ tests/test_media_processing.py tests/test_call_ledger*.py tests/test_call_spans.py tests/test_writer_lease*.py tests/test_fast_write_ack.py tests/test_fast_write_integration.py tests/test_latency_gate.py tests/test_plugin_sync.py tests/test_personal_baseline_contract.py tests/test_tool_surface_fingerprint.py`.
  - Initial scoped gate: 890 passed in 1092.06s. Review corrections: recall/context/ledger suite 292 passed in 372.03s; preservation/writer/media suite 378 passed in 705.18s; final media recovery and signed-receipt selection 20 passed in 45.60s. Graph receipt and idempotency recovery neighbors are included in the final verification pass.
- [ ] 6.2 Completion boundary: full suite in the CI shard layout (12 core + 4 harness), failure-name diff against the branch base, `uvx ruff check . --select F`, CI full-ruff list, CI mypy list, `openspec validate --all --strict` with the CI pin, `scripts/check_openspec_archive_discipline.py`, `scripts/validate-public-artifacts.py --repository`, `scripts/generate-capabilities.py --check`; record counts here, then complete PR CI and squash merge before archiving the change.
- Post-deploy operational follow-up, not a task of this change: read the first live week's `recall.*` spans and `budget` rows from the personal cell's ledger and tune the PROVISIONAL reserves in `request_budget.py` if they skip stages that would have finished.

### Independent review corrections

- Budget-skipped reranking does not publish an incomplete result to the normal request cache; a later call with sufficient budget reranks and can cache its complete result.
- Graph enrichment rechecks its reserve after parent loading, at the actual stage entry. Nested retrieval and graph-enrichment timings reach the call ledger.
- Regression evidence: cache and nested spans failed before correction (2 failed), then passed (2 passed); graph-entry reserve cases failed before correction (2 failed). The corrected budget module plus nested-ledger selection passed 15 tests; an independent recheck passed all 4 new regression cases.
- Media fan-out runs only after successful terminal persistence and reports observed component outcomes. Durable full-refresh demand and a pending canonical receipt survive terminal failure and exact retry. The foreground leaves refresh retirement to the background drain, preserving newer same-path work after a clear-and-readd interleaving.
- The receipt projection accepts only the existing three `derived_sync` outcomes and still accepts legacy receipts without the optional field. The clear-and-readd regression failed before correction, then passed; the independent reviewer rechecked it and approved the final correction with no remaining findings.
