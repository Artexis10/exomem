## 1. Freeze The Operator Contract

- [x] 1.1 Check in and golden-test `contracts/hosted-operator-v1.json`: exact per-command argv/request transport, bounded request schemas/conditionals, correlated success/error envelopes, exit mapping, stable codes, redaction, operation IDs, and retry identity.
- [x] 1.2 Add the hosted-only CLI dispatcher and common bounded strict request decoder. For offline init/restore, implement a one-FD parser for the fixed root-owned, no-write-bit, single-link regular `subPath` file and test absolute path, owner/mode/link/type, symlink/parent escape, size, replacement race, and one-generation read. For live credential/probe, implement exact `--request-file -` stdin-only parsing and test direct argv/no shell, non-TTY EOF, incomplete/trailing/oversize input, and rejection of alternate sources. Cover duplicate JSON, canonical envelope, redaction, modeled exits, and empty stderr for both without changing ordinary CLI startup.

## 2. Bind And Restore Cells

- [x] 2.1 Add failing pure tests for binding v2 cell/vault/root/UID/GID identity, exact release/protocol proof, non-root bounds, actual no-follow owner/mode checks, private marker modes, idempotent convergence, and foreign/unowned conflicts.
- [x] 2.2 Add failing privileged migration tests for recognized v1 roots, bounded descriptor-relative no-follow chown, partial retry, and symlink/hardlink/device/FIFO/socket/race rejection; implement `hosted init` plus binding migration/bootstrap.
- [x] 2.3 Add failing restore tests for pinned artifact/archive digest, unsigned-manifest behavior, source cell/vault verification, target-cell distinction/logical-vault preservation, runtime-state rejection, lifetime locking, empty roots, and sibling-staging constraints.
- [x] 2.4 Add crash injection after each state/log marker, journal phase, canonical rename, derived rebuild, and proof write; prove exact-request resume, changed-request conflict, post-rename inference, canonical-byte preservation, cleanup ownership, and concurrent server/restore exclusion.
- [x] 2.5 Implement `hosted restore-candidate` with the durable phase journal, target lifetime lock, canonical rename commit point, fresh bindings/security bootstrap, and degraded derived rebuild result.

## 3. Rotate And Probe Credentials

- [x] 3.1 Add failing Secret-bundle tests for fixed native AtomicWriter path, read-only mount, root:root 0444 resolved file, accepted confined `..data` generation swap, one-FD bounded read, no pod fsGroup, duplicate/unknown/malformed/weak values, exact 32-byte base64url entropy shape, escape/type/owner/mode rejection, and at-most-two versions.
- [x] 3.2 Add failing security-authority/state-machine tests for bootstrap, stage, fresh bound proof, promote, abort from staged/promoted, finalize, operation replay/conflict, CAS races, transaction crash points, restart overlap, digest-only persistence, and immediate finalized old-token rejection on MCP plus every private route.
- [x] 3.3 Implement the process-safe `hosted-security.sqlite` authority, injected bundle loader, dynamic common service authenticator, transition table, proof freshness/invalidation, bounded retry/capacity behavior, and `hosted credential` command.
- [x] 3.4 Add failing probe tests for literal IPv4 loopback/fixed path, validated port, disabled proxy/netrc/DNS, fresh UUID/principal on every operation retry, selected version, no redirects, 1/2/3-second bounds, 16 KiB/exact-media/exact-schema response, current security revision, complete readiness/worker proof, redacted failure, no cached-health replay, and no proof write on mismatch.
- [x] 3.5 Implement the hardened authenticated `hosted probe`; include authenticated credential version, logical vault identity, current security revision, and worker-policy digest in private readiness, always re-run transport, and idempotently persist only a still-matching pending proof.

## 4. Make Browser Transfers Direct And One-Time

- [x] 4.1 Golden-test the complete grant/request/response/error `contracts/hosted-transfer-v2.json`; add failing grant tests for 8192-byte bound, exact claims/canonical encoding, signing version, canonical HTTPS origin/host/route/method, cell/principal, exact `iat/nbf/exp/now` equations and boundaries, normalized download path/disposition, bounded upload metadata/digest, operation limits, and finalized-old-version rejection.
- [x] 4.2 Add failing restart-safe JTI tests for transactionally unique consume-before-use, concurrent consumption, crash/restart replay, expiry-plus-24-hour cleanup without resurrection, far/just-expired replay after cleanup, 10,000-row fail-closed capacity, cell/schema binding, and unavailable/busy durability.
- [x] 4.3 Add failing route tests for exact public PUT/GET/OPTIONS host/routes, forbidden bearer/cookie/query/range/selectors, signed-size validation, optional exact Content-Length, absent-length/chunked success and post-consumption mismatch/overflow burn, framing conflict, strict ordering, lifecycle drain/seal/cancel/restart, one active v2 upload, 90 MiB whole-body ceiling, download grant size, metadata/hash/missing file, expiry during admitted stream, abort/new-ticket, and v1 isolation.
- [x] 4.4 Add failing response/CORS tests for exact upload 201 envelope, download 200 bytes/headers and bounded RFC 8187 filename, every stable error status/code/retry/new-grant tuple, content/existence neutrality, configured/hostile/null origins, exact route-specific bodyless OPTIONS, no credentials/wildcard, `Vary: Origin`, 300-second max-age, private/no-store caching, bounded exposed headers, and no preflight consumption.
- [x] 4.5 Implement grant v2, security-store JTI consumption, exact response/error contract, strict CORS/public raw upload/download/OPTIONS routes, active-transfer lifetime, exact target verification, and hosted 90 MiB upload default without exposing long-lived credentials/private addresses.
- [x] 4.6 Add default-off v1 compatibility config/tests: immutable deadline at most seven days after signed build time, per-request expiry, private auth, 4 MiB/one-upload cap, runtime-temp accounting, and rejection for absent/malformed/expired/overlong windows.

## 5. Package And Verify The Runtime

- [ ] 5.1 Add hosted-image/real-K3s manifest tests for fixed non-root UID/GID, no pod fsGroup, unchanged 0700/0600 PVC modes after kubelet mount, native read-only AtomicWriter Secret root:root 0444 readability, read-only image `/tmp`, only vault/state/log plus Secret writable/readable declarations, `state/tmp/runtime` global TMPDIR 16 MiB cleanup, explicit `state/tmp/transfers-v2` 96 MiB/one-upload bounds, diarizer/v1 coexistence gates, stale-temp cleanup, immutable release inputs, and JSON operator availability.
- [x] 5.2 Run real large/chunked/aborted raw uploads against the read-only hosted image and prove bounded v2 temp, cleanup, JTI burning, canonical commit, and no image-root writes; crash/restart with v1 multipart and a diarizer temp artifact to prove separate runtime-temp cleanup/quota and disabled-worker gates cannot poison v2 readiness.
- [ ] 5.3 Regenerate the hosted gateway/operator contract fixtures and update release/protocol/grant digests as one reviewed unit; update Substrate issuance/consumption fixtures from those exact artifacts.
- [x] 5.4 Run focused hosted security/isolation/portability/CLI/route tests, complete lean Python suite, Ruff correctness/scoped checks, package/container checks, strict OpenSpec validation, and product E2E/latency gates.
- [ ] 5.5 Obtain independent implementation review, resolve every blocking finding, record exact verification evidence, and refresh PR #227 title/body/checklist.
