# Verification evidence

Evidence is appended as implementation gates close. A checked task means the result below was reproduced or explicitly inherited from the reviewed branch baseline; known red baselines remain named until their later task closes them.

## 2026-07-14 — trustworthy baseline

### Exomem `main`

- Frontmatter cache freshness fix merged through PR #230 at `80eb668` after independent review.
- Fresh lean suite: 2,233 passed, 19 optional platform/model skips.
- The fix hashes current page bytes before cache reuse, so same-size/same-mtime external edits cannot serve stale frontmatter.

### Exomem PR #227

- Refreshed branch head: `d925642` (`feat/hosted-multi-tenant-service`), mergeable and pushed.
- Semantic merge preserves current-main content-hash freshness, operation-scoped writer fencing, hosted mutation serialization, idempotency isolation, privacy, transfers, and workers.
- Independent review found and then approved the invocation-level hosted admission fix: read-only `connect_memory` modes remain usable during mutation-authority outage/quiescence; write and unknown modes fail closed.
- Fresh full lean suite: 2,392 passed, 19 optional platform/model skips. Focused hosted/lifecycle/lease suite, Ruff correctness/scoped checks, latency gate, installed-wheel product E2E, capability generation, package/image checks, and strict OpenSpec validation passed.
- Every GitHub check on the recorded head passed, including Python 3.11/3.13, retrieval golden gate, Docker smoke, installed-wheel E2E, package, lint/types, onboarding, capabilities, and OpenSpec.

### Substrate PR #32

- Refreshed branch head: `ffc499c` (`feat/exomem-hosted-service`), mergeable and pushed.
- Reproduced failed Vercel deployment: three minute/hour Exomem jobs in `vercel.json` violated the Vercel Hobby once-per-day cron limit.
- Fixed by keeping the authenticated handlers on Vercel while moving cadence ownership to the versioned K3s schedule contract. The contract pins origin, paths/cadence, dedicated least-privilege bearer and two-version receiver overlap, redirect denial, timeouts/deadlines, non-overlap, retries/history/TTL, content-free metrics, and alerts. Global `CRON_SECRET` is not exposed to K3s.
- Fresh unit/contract suite: 451 passed across 102 suites; TypeScript (`npx tsc --noEmit`), changed-file Prettier, strict OpenSpec (16 items), production Next build, and diff checks passed. Independent code and cross-repository artifact reviews approved the hosted implementation.
- Replacement Vercel deployment check passed on `21bb0f1`, proving the original deployment failure is closed.
- Real PostgreSQL integration/migration baseline inherited unchanged from reviewed `52dabed`: 22 tests across five suites through migration 0021. This session had no `DATABASE_URL`, so the local production-only migration launcher correctly skipped; static migration serialization/transaction tests and the production build passed.
- Next 16 lint baseline repaired at `ffc499c`: `npm run lint` now uses ESLint 9 flat config and exits with 0 errors plus 23 visible pre-existing warnings. The new Hooks rule remains error-level for new/hosted code and is disabled only for eight exact legacy files. A clean scratch `npm ci --ignore-scripts`, lint, changed-code zero-warning lint, TypeScript, focused 14-test suite, build, and independent review all passed.

## 2026-07-14 — Exomem runtime deployment contract frozen

- Exomem PR #227 head `b38bea8` adds `complete-hosted-runtime-deployment-contract`, including normative operator v1 and direct-transfer v2 JSON artifacts.
- The contract fixes exact init/restore delivery, binding v2 identity, offline crash-safe candidate restore, Secret-backed active/pending credential rotation with abort/finalize, fresh literal-loopback authenticated probe, durable one-time JTI consumption, direct raw browser transfer, route-specific CORS, response/error tuples, temp quotas, and real-K3s image/mount gates.
- Strict OpenSpec validation and JSON/structural assertions passed. Final independent adversarial review approved the frozen artifact hashes with P0 0, P1 0, P2 0.

## 2026-07-14 — IaC scaffold and saved-plan guard

- Added explicit ownership boundaries for split Terraform foundation/durability/bootstrap roots, Ansible, platform/cell Helm charts, the provisioner, scripts, and hosted runbooks.
- Pinned Terraform 1.15.8, HCloud 1.66.0, Cloudflare 5.22.0, B2 0.12.1, K3s `v1.35.6+k3s1`, Hetzner CSI 2.21.1, and every validation tool. Provider locks include Linux amd64/arm64 checksums.
- Git ignores state, saved plans/JSON, tfvars, generated inventory, decrypted SOPS material, and age keys. Reproducible validation/plan/apply commands are documented.
- The non-printing JSON plan inspector rejects unapproved delete/replacement, unknown plan actions, duplicate approval, and secret-like output lacking Terraform sensitivity. Six focused tests pass; ShellCheck, Terraform formatting, Ruff, and diff checks pass.
- Apply accepts only a mode-0600 saved plan, re-inspects that exact plan, and never recomputes it. Destructive approval is per exact Terraform address.

## 2026-07-14 — split-state Terraform implementation

- Foundation now declares the dedicated CX33, stable IPv4, private network, restricted SSH-only firewall, Cloudflare Tunnel, exact control/transfer DNS ingress, and a service-token-only Access application. Destructive provider controls and Terraform `prevent_destroy` protect the node, address, network, SSH key, firewall, and tunnel.
- Durability now owns only the recovery and complete-database B2 buckets. Both use B2 server-side encryption, seven-day governance Object Lock, 30-day current-object retention, and split upload/restore/delete identities; the database backup identity cannot delete.
- The one-time local bootstrap owns only the encrypted/versioned B2 state bucket and two prefix-scoped backend identities. Foundation and durability use distinct state keys plus native S3 lockfiles. Its wrapper accepts reviewed mode-0600 plans, seals local state with SOPS/age, verifies decryption, and removes plaintext only after successful atomic replacement; interrupted applies retain recoverable local state rather than losing it.
- Normal plan/apply wrappers require a mode-0600 backend config and prefix-scoped B2 credentials from environment variables. Backend examples contain no credentials. State, backend config, plan, JSON, and tfvars artifacts remain ignored.
- Red-to-green contract tests cover lifecycle-domain separation, fixed cost-safe defaults, explicit non-global SSH CIDRs, absence of public 80/443/6443, destroy protection, exact two-host Tunnel ingress, Access service authentication, B2 retention/capability splits, sensitive outputs, backend bootstrap, sealed-state workflow, and backend-config permissions.
- Terraform 1.15.8 formatted and validated foundation, durability, and bootstrap against the committed provider locks. Focused infrastructure suites pass: 14 tests; Ruff, ShellCheck, and `git diff --check` pass.
- Real B2 concurrent lock contention and prior-version recovery remain deliberately open as task 6.6; no live apply is permitted until that evidence is recorded.

## 2026-07-14 — hardened host and K3s bootstrap

- Added idempotent Ansible roles for safe package updates, a locked dedicated administrator, key-only SSH, explicit-CIDR UFW, Kubernetes forwarding rules, fail2ban, systemd time sync, bounded journal storage, unattended security updates, and `cryptsetup`.
- The K3s role downloads only the pinned `v1.35.6+k3s1` amd64 binary with the official SHA-256 `2b52a2c1ca6eb502e2a0ffa1a4cf79eef94875926577c1e43347ed292cc92432`. The downloaded 75.9 MiB release binary independently matched that checksum, and its own help output confirmed the configured embedded-etcd, secrets-encryption, kubeconfig-group, kernel-default, S3 retention, and S3 timeout flags.
- The single-server configuration enables embedded etcd and secrets encryption, restricts the kubeconfig to the operator group, disables bundled Traefik/ServiceLB/local storage, caps service-account token lifetime, enables metadata-safe audit rotation, bounds image/container logs, and schedules 30-minute B2 S3 snapshots with seven-day retention. S3 credentials and the cluster token are written only by a `no_log` mode-0600 task.
- No task fetches or slurps the cluster-admin kubeconfig. A mode-0600 atomic inventory generator consumes only two explicitly non-sensitive Terraform outputs and ignores all sensitive output values.
- Four focused structural/behavior tests pass with one opt-in Ansible syntax test; the opt-in test passes under ansible-core 2.21.2. The playbook also passes direct syntax validation and ansible-lint 26.6.0's production profile with pinned community collections, while Ruff, ShellCheck, and diff checks pass.
- Encrypted variable handoff/break-glass escrow and a live double-run convergence proof remain open under tasks 7.4, 7.5, and 15.1; the role is not yet authorized for a live node.

## 2026-07-14 — platform and cell Helm boundary

- Pinned the official Hetzner CSI chart at 2.21.1 and both driver components to the immutable `v2.21.1` multi-platform image digest. Provenance records upstream tag commit `1dd5776c2810f80f038454c9333a3814a2319b1b`, its exact `encryption-passphrase` node-publish Secret contract, and `crypto_LUKS` behavior. The real Hetzner attach/remount drill remains a deployment gate.
- Added the encrypted `Retain`/`WaitForFirstConsumer` StorageClass, pinned Traefik and cloudflared, and rendered the three content-free scheduler CronJobs only from the byte-identical Substrate schedule contract (`02c1b9fa8d031ed77df9015f8555cfc95545b266767474aa52a8ccf08f4b40fc`). Helm now computes that SHA from the embedded bytes, rejects a configured mismatch, derives the sender environment name/auth scheme/success status from the checked contract, and fails on unsupported receiver or retry semantics.
- Split a cell release into a narrowly admitted root initialization Job and a normal restricted non-root StatefulSet. The serving pod has a read-only root, no API token or plaintext legacy credential environment variable, the complete hosted-v2 identity/runtime environment seam, one 10 GiB retained PVC, no generic writable image or `emptyDir` mount, state-backed bounded runtime/transfer temporary roots, fixed 5 GiB/90 MiB/zero-worker entitlements, one ClusterIP Service, quota/limits, and default-deny networking.
- K3s Pod Security defaults to baseline with restricted audit/warn; every tenant namespace explicitly enforces/audits/warns at restricted v1.35, while only the named storage-init RuntimeClass bypasses Pod Security. The immutable runtime image allowlist now comes only from the operator-owned platform release, never a routine-provisioner Namespace annotation. A second platform policy covers Namespace creation and update: a tenant marker is admitted only with the complete exact restricted-v1.35 identity/resource/PSS contract, the contract cannot be removed from an existing tenant namespace, and all protected fields (including the retired image annotation) remain frozen against routine updates while allowing the named break-glass operator identity. The global pod policy denies the exempt RuntimeClass outside a governed tenant namespace, admits the one immutable root init shape and restricted serving shape inside it, leaves ordinary non-tenant pods alone, and rejects a foreign image, capability, compute envelope, PVC, Secret, ConfigMap, unbounded temporary volume, sidecar/init container, command/argument/environment drift, lifecycle hook, exec/HTTP/TCP/gRPC probe, port/hostPort, stdin/TTY, working directory, unmasked proc surface, or container-level seccomp override. Every tenant container is fixed to `/dev/termination-log` with policy `File`, preventing the root initializer from publishing mounted credential bytes through a forged termination-message path. Namespaced ConfigMap parameters were tested first but exact K3s 1.35.6 returned stale/missing parameter data until an API-server restart, so that unsafe dynamic path was replaced by the protected Namespace contract plus platform image source.
- Per-cell Traefik routing exposes only the Access-protected private `/v1` segment (exact path or descendants under `/v1/`) and the two exact public v2 transfer paths, strips the opaque cell prefix, and has no NodePort or LoadBalancer Service. One top-level transfer hostname now drives both the runtime `EXOMEM_HOSTED_TRANSFER_HOST` and Traefik host matcher; the nested duplicate was removed from values and schema. The Terraform Tunnel target is cross-checked against the actual production Helm release Service `exomem-platform-traefik.exomem-platform.svc.cluster.local:80`. Routes remain disabled until the provisioner completes authenticated readiness.
- Helm strict lint passes for platform, serve, and initialize modes. Kubeconform 0.8.0 reports zero invalid resources across 50 platform, ten serving-cell, and nine initialization-cell resources. The complete pinned validation gate passes Terraform validation, Ansible syntax/production lint, Ruff, ShellCheck, strict OpenSpec, all 29 hosted infrastructure tests, and an ephemeral privileged container running exact K3s `v1.35.6+k3s1`; both live policies report no CEL warnings before the first adversarial mutation, insecure tenant creation and routine contract mutations deny, an operator-authored legacy image annotation cannot admit a wrong image, and all positive/adversarial pod shapes above behave as specified. The live fixture requires three consecutive API readiness responses and retains container logs in pytest's temporary artifact tree on setup or test failure.
- SOPS static-secret application, executable network denial probes, log-pressure alerting, and the real Hetzner/Cloudflare checks remain open; the ephemeral exact-K3s API proof is not presented as production deployment proof.

## 2026-07-14 — durable provisioner core

- Added a standalone, locked `infra/provisioner` package with FastAPI, strict Pydantic settings, SQLAlchemy 2 async repositories, asyncpg production support, a SQLite test path, Alembic, AES-256-GCM envelope encryption, redacting operational logs, and deployable liveness/readiness endpoints. `uv sync --frozen` and `uv lock --check` pass.
- The initial Alembic migration creates the operation, monotonic tenant-fence, resource, credential-metadata, export, and backup records in a validated dedicated production role/schema. An empty SQLite database upgrades to head in the test suite. Application repositories use the ORM rather than raw SQL.
- Repository tests prove canonical request hashing, exact action/idempotency replay, changed-body conflict, monotonic concurrent fences, immutable provider operation/fence metadata, encrypted recoverable references, pending/final/error state, atomic claims, expired-claim recovery, and PostgreSQL `FOR UPDATE SKIP LOCKED` compilation with a SQLite-safe atomic fallback.
- The API exposes the real client's exact 14 POST paths and strict current request/final-response shapes. It enforces HTTPS, bearer authentication, exact protocol/content type, bounded idempotency keys and request/response bodies, identifier/field constraints, unknown-field rejection, and no redirects. Pending responses are durable `202` responses with `Retry-After`; final responses and content-free errors are schema-validated.
- The database-backed worker fences every driver effect against provider-observed state, checkpoints before and after effects, resumes expired claims, survives a lost acknowledgement without duplicating the fake-provider effect, and leaves operations pending beyond six polls. The deterministic driver is an injected seam and performs no live provider work.
- Adversarial tests prove bearer values, credentials, restore/export references, URLs, database passwords, content, and injected exception strings do not appear in logs, stored plaintext rows, exception representations, or API responses.
- Fresh provisioner verification: 37 tests passed; Ruff check and format check passed over 21 files. Existing hosted Terraform/Ansible/Helm/scaffold contracts remain green at 19 passed and six intentionally opt-in tool skips. Strict OpenSpec validation and diff checks pass.
- The TypeScript client still needs its pending-response parser and shared cross-language fixtures; those remain explicitly open under tasks 3.1–3.2 and 9.6 rather than being claimed by this Python-core gate.

## 2026-07-14 — provisioner independent-review hardening

- Tenant fencing is now a same-transaction database invariant for submission replay and worker claim. An exact idempotency replay carrying an obsolete fence is rejected, and queued work becomes unclaimable as soon as a higher durable tenant fence exists.
- Every post-effect checkpoint, resource, retry, failure, pending, and completion write now requires the current claim token and generation plus an unexpired owner lease. Claim generations rotate on reacquisition, and the worker renews long-running effects with a bounded heartbeat so an expired or superseded worker cannot commit provider results.
- Provider-pending outcomes remain non-attempt-consuming. Retryable provider failures increment a distinct durable failure counter and terminate with a content-free exhaustion result at the configured ceiling.
- Export and backup replay now compares every immutable identity and integrity field, including tenant, cell, reference digest, provider operation, fence, hashes, manifest, and size. Pending API responses preserve the caller checkpoint while internal worker progress remains separate.
- Production ingress and readiness fail closed: request bodies are bounded while streaming, trusted proxies are explicit private/loopback addresses, HTTPS is enforced, root/Uvicorn logging uses the content-free formatter with access logging disabled, and startup/readiness reject the public schema, a shared or mismatched database role, and an unexpected Alembic revision.
- Adversarial red-to-green coverage includes stale replay/claim races, same-owner claim-token rotation, expired-lease writes, long-effect renewal, immutable export/backup mutation, retry exhaustion versus repeated pending, chunked oversize bodies, forged proxy headers, log formatter installation, and unsafe production database identity.
- Fresh acceptance: `uv sync --frozen` and `uv lock --check` pass; 55 provisioner tests pass; Ruff check and format check pass over 21 files; existing hosted Terraform/Ansible/Helm/scaffold contracts remain green at 19 passed and six intentionally opt-in tool skips; strict OpenSpec validation and diff checks pass.

## 2026-07-14 — provisioner PostgreSQL and claim-safety review closure

- Fresh PostgreSQL 17 bootstrap now creates the validated dedicated schema before Alembic attempts its version table, inside the same migration transaction. Migration rejects a pre-existing schema owned by another role without changing its owner. Readiness requires the configured current role/schema, matching schema owner, and exactly one expected Alembic revision.
- Production environment startup and the API launcher reject SQLite before constructing the application; explicit dependency injection retains SQLite only for fast tests. Trusted proxy inputs are normalized to canonical, de-duplicated networks before reaching Uvicorn.
- PostgreSQL's `clock_timestamp()` is authoritative whenever callers do not inject test time. Claim acquisition locks the current tenant-fence row before the operation, then rechecks claimability and generation under lock. Every active-claim checkpoint, terminal transition, resource, credential, export, and backup write follows the same fence-first ordering and rejects a superseded fence.
- Durable side-effect inserts are bound to the active claim and the operation's tenant, cell, provider operation, and fence identity. Credential metadata permits staged false-to-true promotion, transactionally deactivates the previous version, enforces one active version per cell with a partial unique index, and rejects reversal or identity drift.
- Permanent opt-in PostgreSQL 17 tests prove fresh bootstrap and owner rejection, a blocked claim concurrent with a higher fence commit, rejection of stale post-claim completion, and database-clock authority under a deliberately skewed process clock.
- Fresh final acceptance: `RUN_POSTGRESQL17_TEST=1 uv run --frozen pytest -q` passed 66 tests; provisioner Ruff check and format check passed over 22 files; `uv sync --frozen`, `uv lock --check`, and an sdist/wheel build passed; root hosted infrastructure contracts plus the latency gate passed 21 tests with six documented tool-opt-in skips; strict OpenSpec validation and `git diff --check` passed.

## 2026-07-14 — exact K3s hosted runtime gate

- Added a checked runtime-gate input manifest pinning Exomem `0.22.0` at exact source commit `c255ffb2dfcd7bc470372d4efa0e8a11b00f0640` (the reviewed hosted runtime plus the browser preflight fix), hosted Docker target, hosted protocol `1`, fixed release-build time, operator-v1 contract SHA-256 `407799e723e9d996e5ab15ca76c071c3ae497041a1096f106690712ce6fe4ca6`, and the exact K3s OCI image `rancher/k3s@sha256:9d6b9c15e8031c1aea7dd7f0cdc019f5e74a23c53b9eada564b7a8dc94efc14c`. The live fixture now runs that immutable digest rather than a mutable version tag.
- The opt-in gate checks out that exact clean commit, verifies the package version and operator artifact hash, builds the `hosted` target, and rejects any image that is not fixed to `10001:10001`, the `exomem` entrypoint, no declared Docker volumes, and the expected hosted/release environment. It streams the image into containerd, records the resulting manifest digest, adds the digest reference to the CRI image store, and deploys only that immutable reference. This closes local image loading, not the still-open byte-reproducible published release manifest in task 4.2.
- A privileged ephemeral container runs exact K3s `v1.35.6+k3s1` by OCI digest with local storage disabled. A statically prebound 10 GiB PV backs a real PVC so kubelet performs the production PVC and subPath mounts; the underlying test PV uses `hostPath`, so this does not claim Hetzner CSI attach, LUKS, or retained-volume recovery proof.
- The real root init Job now invokes operator contract version `1`, writes unchanged `0700` runtime roots and `0600` binding markers for UID/GID 10001 without `fsGroup`, and completes under the tenant admission policy. Exact K3s identified the finalizer-removal actor as `system:serviceaccount:kube-system:job-controller`. The `nodeName` exception is limited to that identity, an already scheduled storage-init pod, removal of the sole `batch.kubernetes.io/job-tracking` finalizer, and exact equality of the complete old/new Pod spec. A routine pod editor cannot add a finalizer to a scheduled serving pod or remove the Job finalizer. A freshly fetched scheduled Pod owned by a real initialization Job proves the exact unchanged controller transition succeeds, while simultaneous `activeDeadlineSeconds` or toleration drift is denied.
- Serving admission now fixes the image entrypoint/arguments, all 19 environment names and values, the one TCP port, all three TCP probes and their timings, lifecycle/interactive/working-directory surfaces, and pod/container restart policies. Per-cell non-secret values are bound to protected Namespace annotations that routine provisioners cannot mutate. Live adversarial requests prove command, argument, environment-set, environment-value, port, exec/HTTP probe, lifecycle, stdin/TTY, working-directory, and restart-policy drift all deny.
- The serving StatefulSet runs as UID/GID 10001 with a read-only image root and only four mounts: three fixed PVC subpaths plus the credential Secret. `/tmp` remains read-only; `TMPDIR` is the private state-backed runtime root. Kubelet's native Secret AtomicWriter projection is proven through its `..data` topology, root-owned `0444` resolved credential file, and a failed write on the read-only mount.
- Authenticated hosted probe and `/private/exomem/v1/contract` calls succeed against the real process, including release/protocol, credential revision, admission, worker-policy, and canonical contract-digest checks. Replacing the pod removes an approved runtime sentinel and recognized v2 upload remnant, returns to readiness, and passes the authenticated probe again.
- The fresh focused adversarial admission drill passes in 44.26 seconds, and the fresh unmodified exact-runtime drill passes in 61.84 seconds after the full-spec equality hardening. The complete pinned validation gate then passes all three Terraform roots, Ansible syntax and production lint, Helm strict lint, Kubeconform with zero invalid resources across 50 platform, ten serving-cell, and nine initialization-cell resources, Ruff, ShellCheck, and all 31 hosted infrastructure tests including the exact-K3s admission and real-runtime drills in 63.58 seconds.
