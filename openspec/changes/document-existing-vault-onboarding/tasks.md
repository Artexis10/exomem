# document-existing-vault-onboarding — tasks

## 1. Docs

- [ ] 1.1 SETUP-LOCAL.md: add "Already have a vault full of notes?" after the
      "One command" section (write contract, searchability, same-vs-separate
      vault, daily-notes bullet, `overview` worked example).
- [ ] 1.2 README quickstart: link the existing-vault paragraph to the new
      SETUP-LOCAL section.
- [ ] 1.3 Cross-check scaffold SKILL.md "Assessing a vault you didn't build"
      wording for consistency (no scaffold edit expected).

## 2. Verification

- [ ] 2.1 `uv run pytest -q` still green (docs-only); read both sections
      end-to-end for coherence.
