## 1. Red tests

- [ ] 1.1 Add a failing test that saves `off` through the real `configure_memory` path and asserts both hook copies resolve `off` on their next run, and that saving `balanced` after `maximal` moves the hook presets accordingly.
- [ ] 1.2 Add a failing test that serves `bootstrap` through a descriptor without `configure_memory` and asserts `change_with` names the custom-instructions route, never the CLI string.
- [ ] 1.3 Add a failing test that corrupts the preference record for an identity that saved `off` and asserts the resolved level is `balanced` with source `preference:unavailable`, and that `capture_gate()` does not grant proactive writes.

## 2. Implementation

- [ ] 2.1 On every successful `configure_memory` set or clear, mirror the level that the local owner identity now resolves for the `coding` context into the shared configuration file through `write_prominence`, preserving the compute-mode key, and report `mirrored_to_hooks` in the inspect and set results.
- [ ] 2.2 Seed `change_with` from the served command set: the `configure_memory` route when present, otherwise the custom-instructions path documented in `docs/prominence.md`.
- [ ] 2.3 Resolve an unreadable preference record to `balanced`, report `preference:unavailable`, and keep the `unavailable` diagnostic.
- [ ] 2.4 Update `docs/prominence.md` and the scaffold engagement reference; regenerate the plugin tree, hosted renders, the tool-surface contract and `docs/capabilities.md` if any served text changed.

## 3. Verification and delivery

- [ ] 3.1 Run the prominence, hook, bootstrap-contract, byte-budget and hosted-fidelity suites scoped to the change, then the full CI shard suite on the pull request head.
- [ ] 3.2 Obtain an author-independent review of the diff covering the mirror's identity choice on multi-identity machines, the write ordering between the preference record and the shared file, and the hosted `change_with` text.
- [ ] 3.3 Deliver under the standing merge authority, verify on the personal cell that saving `off` from Claude Code silences the capture hook within one turn, then synchronize and archive this change.
