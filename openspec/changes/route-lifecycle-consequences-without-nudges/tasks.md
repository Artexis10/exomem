# Tasks — route lifecycle consequences without nudges

Every test lands red first (verbatim failing output recorded before the
implementation, then green). Measured budgets are written into this file when the
task is ticked, not estimated.

## 1. Contract — capture classes (design D1, D2, D8)

- [ ] 1.1 `src/exomem/prominence.py`: add the two lifecycle capture classes and the pairing rule to the `balanced` and `maximal` capture axis; `light` and `off` unchanged. Red-first test in `tests/test_prominence.py` (or the existing contract test) asserting both levels name intent→Planning, outcome→Records and the pairing rule, and that `light` does not.
- [ ] 1.2 `src/exomem/_scaffold/_Schema/SKILL.md` and the plugin copy (md5-identical): stepping-stone block gains the two classes; the Planning section's "never infer it from prose" becomes "never infer it from elapsed time — a stated outcome is evidence, the clock is not"; the implicit-proactive block lists `record_memory` and `plan_memory` beside `ask_memory`, `capture_source`, `remember`; one paired worked example in domain language. `tests/test_scaffold_no_leak.py` and the skill-sync test stay green.
- [ ] 1.3 `commands.op_bootstrap`: `intent_boundary` gains the two classes and the pairing rule; `capture_examples` gains one paired example; `_SIMPLE_ACTIONS` gains `plan`. Extend `tests/test_epistemic_bootstrap_contract.py` (intent-boundary pin) and the simple-action pin red-first. Measure the compact payload before and after and record both sizes here; raise `COMPACT_BYTE_CEILING` by the minimum needed, not past 61,400. Measured: before ______ B, after ______ B, ceiling ______.
- [ ] 1.4 Workflow skills `continue` and `capture`: one lifecycle paragraph each (session start checks the Planning inventory and the due-state block; capture routes intent and outcomes). Hosted skill renders and generated locks regenerated by the existing scripts.
- [ ] 1.5 `_hooks/exomem_capture_nudge.py`: `_KB_WRITE` counts `record_memory`, `plan_memory`, `observe_memory`; the reminder names the lifecycle classes in one line. Red-first test: a transcript whose only KB write is a `record_memory` append is not nudged; the reminder text contains the lifecycle line.

## 2. Substrate — identity (design D3)

- [ ] 2.1 `records.append_record`: derive the item key from the declared natural key when `item_key` is omitted and every natural-key field is present, via `natural_key_serialization` + `inferred_item_key`; `uuid4` otherwise; explicit key still wins. Red-first tests in `tests/test_record_mutation.py`: omitted key → derived key equals the read-path inferred key; identical re-append → `replayed`; same natural key, different payload → `RECORD_ID_CONFLICT`; missing natural-key field → random key as before.
- [ ] 2.2 `RECORD_NATURAL_KEY_CONFLICT`: an append whose serialised natural key equals an existing item's under a different key refuses naming the existing item(s); two pre-existing duplicates are both named. Red-first test with a `uuid4`-keyed seeded item. Error code added to the allowlisted codes wherever codes are enumerated (`review_state.STORE_ERROR_CODES` pattern, docs).
- [ ] 2.3 Planning inherits: `plan_memory(action="add")` without `plan_id` on a `[title]`-keyed collection derives the key; adding the same title twice with different fields refuses. Red-first test in the Planning suite.

## 3. Substrate — inventory and binding (design D4, D5)

- [ ] 3.1 `plan_memory(action="inspect")` with `collection` omitted returns the Planning inventory in the Records inventory shape, disclosure-filtered; with a collection unchanged. Validation matrix updated (`collection` optional for `inspect` only). Red-first tests: inventory lists exactly the planning-profile manifests; a withheld manifest is absent; every other action still requires `collection`.
- [ ] 3.2 `structured_collections.PlanLink` gains optional `join`; manifest validation accepts 1–4 pairs, record-side names must be declared fields, plan-side names bounded non-empty text; `record_governance._project_plan_link` round-trips it; `describe` documents it. Red-first tests: valid join round-trips unchanged through inspect; unknown record field refuses; five pairs refuse; Records inspect performs no Planning lookup (assert no resolver call).

## 4. Substrate — the family and the carriers (design D6, D7)

- [ ] 4.1 `audit.py`: `_check_unreflected_outcomes` over join-bearing Records manifests → resolved Planning manifests → open items (lifecycle `active`, status ∉ {completed, cancelled}) with ≥ 1 joined record; finding fields per design D6; `signal_version` from authored state; registered in `ALL_CATEGORIES`. Red-first tests: fires on the touched items only; a twin Records collection without `join` produces nothing; a completed item produces nothing; an unresolvable reference produces nothing and is reported as such in the audit's own unevaluated set, never silently.
- [ ] 4.2 Fingerprint: item reference + sorted joined record keys; a new joined record changes it and a dismissed item resurfaces; removing the record restores the old fingerprint and the dismissal holds. Red-first tests through `review_state`.
- [ ] 4.3 `attention.DEFAULT_ATTENTION_CATEGORIES` gains the family; `registered_families()` therefore includes it (assert, no restatement); `triage_memory(ref="exomem://review/family/unreflected_outcomes", action=quiet)` works through the existing surface. Red-first test.
- [ ] 4.4 `due_state.PROJECTION_CATEGORIES` and `DELTA_CATEGORIES` gain the family; `apply_write_delta` handles the two structured write shapes with the bounded counterpart read; `reconcile` recomputes. Red-first tests: the delta after a record append equals a full recompute for the touched pair; the delta after a triage to `completed` clears it; reconcile after an out-of-band manifest edit heals.
- [ ] 4.5 Carriers: `record_memory` append/update and `plan_memory` add/update/triage responses carry the due-state block via the shared helpers under emission governance. Red-first tests: the append that opens a gap carries a block naming the family with count 1; twelve appends inside one `batch_scope` deliver once; `quiet` disposition removes it from the block; an unreadable review state yields no block and the write still commits.
- [ ] 4.6 Disclosure: a withheld record or plan item contributes nothing to the finding, the count or the reference list; the served view equals the vault with it absent. Red-first test through the release plane fixture used by the other categories.
- [ ] 4.7 Cost: measure the write-time delta for a record append against a bound Planning collection of 100 items and a plan triage against a bound Records collection of 1,000 records on the development box; record the numbers here and keep both under 50 ms. Measured: append ______ ms, triage ______ ms.
- [ ] 4.8 `docs/epistemic-inbox.md` and the Records documentation: the family, the binding, what resolves it, what never happens (no runtime transition).

## 5. Acceptance (design D10)

- [ ] 5.1 `benchmarks/epistemic/projectors/exomem_vault.py`: `VaultProjector` emits a `collections` section (profile, manifest reference, items with key, lifecycle/status where present, natural-key values), additive and versioned. Red-first test: a seeded Planning + Records pair appears; the pre-existing snapshot fields are byte-identical for a vault without collections.
- [ ] 5.2 `tests/test_lifecycle_routing_journey.py`: generic batch-production fixture (Planning collection keyed on `[title]`, Records collection keyed on `[occurred_on, title, event_type]` with a `join` to it, plus one unbound twin collection). Journey: seed queued deliverables → append `produced` events for three → assert the family fires on exactly those three and the carrier block on the third append reports it → triage the three to `completed` → assert cleared by state change (no dismissal recorded) → append one more event on a fourth → dismiss → re-run passes → assert dismissal holds → append a second event on the fourth → assert resurfaced. Snapshot both collections through the projector at the end and assert the expected durable state.
- [ ] 5.3 Mechanism removals recorded in `.task/RESULT.md`: disable the natural-key derivation (journey duplicates), remove the join (family silent), remove the family from `DELTA_CATEGORIES` (carrier stale until reconcile) — each goes red.

## 6. Closure (design D9)

- [ ] 6.1 `add-planned-vs-recorded-review` task 7.1: `review_memory` `mode` docstring documents `plan-progress`; `plan_memory` description documents the inventory form; regenerate `tests/fixtures/mcp_tool_schemas.json`, `src/exomem/tool_surface_contract.json`, the v1 release-identities fixture, hosted generated locks/directory packets, and the ChatGPT plugin contract pending digest (`refresh_required: true`) via the scripts `docs/remote-quickstart.md` names. Record the old and new digests here: ______ → ______.
- [ ] 6.2 Tick 7.1 in that change, refresh its stale `MODIFIED` block against the current canonical `records` spec, `openspec validate --all --strict`, `openspec archive add-planned-vs-recorded-review --yes`, validate again, `scripts/check_openspec_archive_discipline.py` → `ok`.

## 7. Gates

- [ ] 7.1 Gate 1: the new and touched suites (`test_record_mutation`, Planning suite, `test_prominence`, `test_epistemic_bootstrap_contract`, `test_bootstrap_compact_budget`, the hook test, the due-state and review-state suites, `test_lifecycle_routing_journey`).
- [ ] 7.2 Gate 2: tool-surface / plugin sync / hosted marketplace release / scaffold no-leak / skill sync / attention / epistemic inbox suites.
- [ ] 7.3 Gate 3: `tests/test_latency_gate.py` + `tests/test_semantic_write_latency_gate.py`.
- [ ] 7.4 `uvx ruff check . --select F,I`; `openspec validate --all --strict`; archive-discipline script.
- [ ] 7.5 Gate 4: the lean suite once, single process, quiet box, no early stop.
