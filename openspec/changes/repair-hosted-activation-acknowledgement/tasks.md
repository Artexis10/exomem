## 1. Freeze the proof and compatibility boundaries

- [x] 1.1 Add the three-second mounted-image read-only-custody reproduction and its writable control to the maintained test suite. Preserve the after-failure note/DB/control evidence and the same-key replay that currently returns outcome unknown. Keep this distinct from the full cluster rehearsal.
- [x] 1.2 Inventory every supported hosted mutator/selector and direct catalog/policy publisher. Record each canonical effect boundary, prepared result source, required child set and derived work in this change's design. Refuse to dispatch a remember-only implementation as complete coverage.
- [x] 1.3 Freeze the versioned pending-proof, acknowledgement/current response, Unix-socket and private recovery-descriptor schemas. Specify field bounds, identity source, replay checks, full deadline allocation, reserved proof executor/admission capacity, writer-journal variant/phase ownership, recovery dispatch and retention pins, and stable refusal/pending codes. Execute codec fixtures and specify concrete callback lock-ordering/saturation and composite-crash integration cases before dependent implementation, including an intermediate-child acknowledgement before a complete-operation commit component exists; child acknowledgement never proves the whole mutation completed. Execute the real-path cases in tasks 2.5 and 3.1 as completion gates.
- [ ] 1.4 Obtain independent adversarial/security review of the authority, secret-disclosure, recovery and rollout design. Resolve findings before changing those boundaries.
  - [x] 1.4.1 Independent adversarial security review of the authority-bearing surface, 2026-09-20. One critical finding (the listener name mismatch, Decision 10), one high (the apply path did not know the TLS kind), three medium, four low. Fixed in the same pass: the name mismatch, the apply path, the open replica-state vocabulary and zero work budget in the classifier, non-DNS subject alternative names riding along, the exception chain retaining decrypted CA material, the missing certificate/key pair check, and a constant-time compare that could raise instead of refuse.
  - [x] 1.4.2 Deferred from the same review, each needing a siting decision rather than a patch. All three settled, as Decisions 11-13.
    - [x] Finding 3, the certificate was validated only where it was issued: `activation_ack_startup.preflight_activation_ack_listener` now runs in `production.py` before anything answers on 8443, reading the mounted leaf and the lock's pinned trust bundle through the adapter the lifecycle path uses. The rotation floor is a parameter rather than a refusal at startup: issuance keeps fourteen days, startup passes zero and logs `activation-ack-certificate-rotation-due`, because refusing a valid certificate with nine days left would take the fleet out of service now to prevent a handshake failure nine days away. Nine cases in `infra/provisioner/tests/test_activation_ack_startup.py`.
    - [x] Finding 4, the legacy-uncertain verdict was stateless: `classify_activation_ack_target` takes `legacy_uncertain_mark` and returns `mark_legacy_uncertain`, so the caller records the verdict and a recorded one survives both routes back to looking healthy -- the capability arriving, and a later write restoring tuple parity. A mark never masks `stranded`. Three cases added to `infra/provisioner/tests/test_activation_ack_preflight.py`. The production caller remains 5.4.2.
    - [x] Finding 5, the floor-2 refusal reads as configuration-time: settled by declining a second control. `_custody_floor` is called by `activate()`, so the direction that matters is already refused at the deliberate operator action with a message naming both halves. Of the three sites that could catch the reverse order, the mount refusal is content-free, the provisioner site adds a Secret read to every driver step of every capable cell forever, and lock composition cannot see a cell's floor. Alpha mints at floor 1 and the combination is reachable only by activating floor 2 first. Decoupling the generation from the epoch retires it; guarding it twice does not.

## 2. Bind exact mutation recovery to canonical publication

- [x] 2.1 Extend writer_lease.py with attempt-bound PreparedCanonicalMutationRecovery and durable private prepared payloads. Test tampering, substituted attempts, stale payloads, missing evidence and precommit crash without marking preparation as success. **The 2026-09-20 survey was wrong about the implementation and is corrected here.** It said nothing in `writer_lease.py` referenced the type. In fact `writer_lease.py:1276` builds the recovery through `prepare_canonical_mutation_recovery`, `persist_prepared_recovery` (line 2848) freezes it against the live attempt — checking `attempt_id`, `commit_token`, `command_digest` and the scoped idempotency digest, bounding it by `MAX_CANONICAL_BYTES`, and treating a byte-identical re-persist as a no-op — and `create_allocating_journal` records it before the first canonical effect. `catalog_publication.py:187` is the production caller. So the writer_lease side exists. What was genuinely missing were the three cases, now added to `tests/test_hosted_mutation_journal.py`: a second differing preparation for one attempt refused as stale with the first left binding, a preparation with no claimed attempt refused for want of evidence, and a precommit cut that reopens the store to find the attempt still `executing` with no terminal — preparation is not success.
- [x] 2.2 Extend catalog_publication.py and schema_v4.py to bind each hosted-mutation-child/v1 component atomically with its tuple publication and bind whole-command hosted-mutation-commit/v1 only after all required children verify (same transaction when possible, otherwise a later evidence-only transaction with no activation advance) using existing journal/component tables. Preserve exact v4 schema compatibility and bind all required child effects. Test generic governance recovery dispatch, unknown-variant refusal, projection/catalog garbage-collection pins, retention and downmigration.
- [x] 2.3 Extract prepare/commit/result seams at the shared semantic creation, edit, move and recovery boundaries and each inventoried direct publication caller. Preserve existing public result shapes and current authorization/egress; no effectful leaf replay is a recovery renderer. Inventory audited 2026-09-21 by grepping every module that calls `catalog_publication.publish_markdown_batch`: nine outside `catalog_publication` itself. Eight now wire `catalog_publication.prepare_hosted_catalog_recovery` -- `semantic_writes` (4 call sites, covering creation, edit and the shared commit boundary), `move_file` (2), `delete_file`, `delete_directory`, `recover_from_trash`, `preserve`, `scene_frames`, and `governance/tool.py`. **`governance/tool.py` was a real gap**: `_backfill_commit` publishes a catalog child the design matrix explicitly requires one for ("Companion commit has one catalog child") and had zero references to the seam, so a hosted attempt interrupted after that publication had no private payload to resume from -- `exact_commit_evidence` finds nothing and the caller is told outcome-unknown for a write that committed. Fixed: the freeze now sits immediately before `receipts.begin_event`, the first durable effect, carrying `{status, event_id, proposal_id, direction}`, every field derivable there. `test_companion_commit_freezes_its_hosted_recovery_before_any_effect` pins the call site, its position before any receipt exists, and that it carries the caller's exact result. The ninth, `governance/recovery.py:1044`, correctly has **no** freeze: `_recover_companion_catalog_publication` *is* the recovery pass, driven by the journal `_backfill_commit` writes, re-deriving the prepared batch and refusing unless its prior/prepared component digests match what was recorded. Freezing a plan there would be recording a plan to recover the recovery. Backfill/companion suites 143 passed, 10 skipped; the companion-backfill module 30 passed.
- [ ] 2.4 Extend exact_commit_evidence and canonical resume to verify the actual publication plus original private payload, recover its acknowledgement, render the same canonical outcome and resume derived work. Preserve uncertainty for legacy incomplete attempts. Surveyed 2026-09-21, and the machinery is present: `writer_lease.py:5082 exact_commit_evidence` loads the original private payload with `load_prepared_recovery`, recovers the acknowledgement through `adoption_proposals.recover_hosted_completion_receipt` when the descriptor carries a sidecar child, and reads the publication bound to `prepared.descriptor_sha256`; `resume_graph_sync` resumes derived work; and `prepared is None` falls through to the fail-closed `outcome_unknown` terminal, which is the legacy-uncertainty preservation. 39 tests over the canonical-resume, prepared-recovery and committed-evidence surface pass, including forged-candidate and incomplete-aggregate refusals. **Left unchecked deliberately**: nothing yet proves the *hosted* end of this end to end -- that a fresh process resolving a real hosted attempt renders the identical canonical outcome the lost response would have carried. That is the claim the box stands for, and the 6.3 rehearsal is where it is earned.
- [x] 2.5 Prove the crash cuts before commit, after publication/before acknowledgement, after authoritative acknowledgement/before response, and before outer terminal persistence.
  - [x] 2.5.1 Fresh-process cuts for the catalog barriers in `tests/test_hosted_activation_ack_crash_cuts.py`: a child interpreter dies at the barrier with `os._exit`, and this process reopens the vault. They pin the durability ordering — the canonical filesystem batch is durable **before** the catalog transaction opens, so both cuts leave the page already changed on disk and only a committed publication separates them. They also pin that repeating the request is refused rather than published twice.
  - [x] 2.5.2 The remaining cuts. **After authoritative acknowledgement before response: done.** Needed a new barrier -- `_acknowledge_registry` ran and returned with nothing between it and the result -- so `schema_v4` gained `catalog-publication-after-registry-before-result`, and `test_a_cut_after_the_acknowledgement_leaves_a_complete_mutation_and_a_usable_page` pins it. Measured, and it is the cut that justifies the whole acknowledgement design: after the cut *before* the registry the next ordinary write on that page is refused `GOVERNANCE_CATALOG_PUBLICATION_BLOCKED`, and after this one it simply succeeds (publications and activation epoch both advance again). The acknowledgement is the line between a stall that strands a page and a mutation that is complete with only its receipt lost; a retry is still refused `STALE_SEMANTIC_WRITE` rather than published twice. **The three Adoption Studio boundaries and the incomplete child set: done.** `adoption_proposals` gained `_adoption_crash_point` (a no-op, like `schema_v4._crash_point`) at the three places a completion can be lost: after the catalog transaction before the proposal is completed, after the atomic proposal/receipt replacement before the SQLite evidence, and after the aggregate commitment before terminal persistence. `tests/test_adoption_completion_crash_cuts.py` kills a child interpreter at each, and the parametrised expectations record what each cut actually leaves behind, so a change that reorders those effects fails there rather than in a rehearsal. Every cut is then finished by one real recovery pass that lands exactly one applied proposal, one receipt, and one `hosted-mutation-commit/v1` over its two children -- covering the incomplete child set, since the sidecar child is missing in the first two cuts. A second recovery pass changes nothing. **Before outer terminal persistence: done**, closing 2.5. `writer_lease` gained `_crash_point` (a no-op) immediately before `_persist_completed_from_canonical`, the window where the leaf has run and nothing records that it has. `tests/test_outer_terminal_crash_cut.py` takes the cut in a child interpreter, with the leaf appending to a file outside the lease's bookkeeping so a second run is visible. Measured result, stronger than the non-duplication the task asked for: the same-key retry returns the original `{"value": 7}` and the leaf never runs again. The terminal row is missing, so that is not a replay of it -- the canonical row written before the leaf returned is the retry anchor and survives the window alone, which is the property 2.4's canonical resume depends on. Include composite mutations with an incomplete child set and fresh-process recovery, not only caught exceptions. For Adoption Studio cover after catalog/before proposal completion, after atomic proposal/receipt replacement before SQLite evidence, and after aggregate commitment before terminal persistence; none may repeat _route_apply, duplicate a note, or advance activation to record completion.

## 3. Add control-plane-owned acknowledgement

- [x] 3.1 Add a bounded private pending-publication proof route in server_hosted.py and its transport descriptor. Verify exact committed publication/receipt and challenge, cell, attachment, store and tuple bindings while content parity is blocked. Prove it does not acquire a fence held by the waiting capture or return paths/content. Saturate the shared command worker pool and prove reserved bounded callback capacity remains available.
- [x] 3.2 Add provisioner acknowledgement/current services using current owned cell credentials, provider recovery identity, trusted callback selection and existing authorization Secret revision CAS. Preserve renewal fields; test replay, stale/foreign proof, expiry, wrong credentials and concurrent acknowledgement/renewal.
- [x] 3.3 Add activation_ack_api.py to the existing worker composition with a narrow injected capability set and coordinated listener/worker shutdown. Keep the general admission API's privileges and global bearer boundary unchanged. Return signed control/membership and keyring digest/revision only, never keyring bytes.
- [x] 3.4 **Withdrawn: the fuse it was written to defuse does not exist.** The task proposed letting an exact pending successor renew the attestation window, widening the readiness proof and its provisioner validator to do it. Its whole justification was Decision 7's claim that a pending acknowledgement blocks the hourly renewal and expiry is then unrecoverable. Two measurements retire it. First, by enumeration: `schema_v4.load_active_state` is the only comparison of the store's activation tuple against `control.json`, it has five callers in `src/exomem/`, and the only one on a readiness or attestation path is `authorization_session_lifecycle.py:153` inside `_ready_custody` -- which the renewal never reaches, because `renew-authorization` goes `lifecycle.py:2156` -> `live.py:1015` -> `adapters.py:2765 attest_authorization_session_membership`, posting `authorization-membership/attest` directly without `health()` or `verify_governance_readiness`, and `mint_hosted_replica_readiness_attestation` never loads active state. Second, by experiment: the one input a pod restart recomputes, `_mutation_authority_ready` from `probe_hosted_mutation_authority`, still returns `(True, "HOSTED_READY")` against a cell whose store genuinely leads custody by an epoch after a committed-but-unacknowledged write. Pinned by `test_a_committed_unacknowledged_write_does_not_close_the_attestation_path` in `tests/test_hosted_activation_ack_crash_cuts.py`. Note for anyone re-testing: synthesising the divergence with `activation_epoch=0` makes the probe refuse, but the schema's `CHECK(activation_epoch>0)` means that is a malformed record, not a cell that is behind -- it reads like a confirmation and is not one. What a pending acknowledgement does block is `_ready_custody`, and so `/ready`, session issuance and content serving: the defect this change repairs through the protocol itself. See Decision 7's amendment.

## 4. Deliver custody promptly and monotonically

- [x] 4.1 Extend the native custody helper with a bounded Unix-socket acknowledgement client/server path and fixed authenticated TLS control-plane client. Verify identities, signatures and installed parity before completing the healthy request.
- [x] 4.2 Serialize fast delivery and projected refresh. Test stale projected predecessors, conflicting equal epochs, incomparable renewal generations, sidecar restart, missing rotated verification key and interrupted multi-file publication. Reconcile from authority without locally combining generations.
- [x] 4.3 Wire hosted acknowledge_activation_tuple to the helper while preserving standalone behavior. Refuse missing capability before mutation and retain exact committed pending recovery after transport loss. Prove the healthy path fits the existing command deadline with the actual held mutation fences.

## 5. Wire deployment and credentials

- [x] 5.1 Add the worker's internal 8443 TLS listener and ClusterIP Service, trusted fixed endpoint configuration, dedicated certificate Secret and public trust ConfigMap distribution. Add the socket/credential/trust mounts without making runtime custody writable or sharing a Kubernetes/global bearer token. Built on both sides and asserted against real Helm renders in `tests/test_hosted_activation_helm.py`: the platform Service selects the worker on 8443 and the legacy render has no listener at all; `exomem-activation-ack-tls` mounts read-only at 0400 on the worker and nowhere on the admission API; the cell mounts the acknowledgement socket read-only and never the trust bundle, and publishes the CA as the immutable `exomem-ack-ca-<digest[:40]>` ConfigMap behind a fixed 8443 egress policy. The worker now also validates that certificate before serving (1.4.2 finding 3).
- [ ] 5.2 Update cell/platform values schemas, exact-shape tenant and provisioner admission policies, Secret/ConfigMap ownership verification, fixed destination egress/DNS and callback ingress. Render and exercise acceptance/refusal against a real API server. Written and rendering: both values schemas carry the binding, `tenant-admission.yaml` and `provisioner-scope-admission.yaml` branch on it, and `test_hosted_activation_helm.py` proves five refusal mutations fail the render. **The count half is now verified offline, 2026-09-21, contrary to what this task assumed.** The concern was that the policies pin exact sizes and CEL is evaluated by neither kubeconform nor conftest, so a count that drifted from the chart would pass every offline gate and be caught only by a real API server refusing every cell. But the counts do not need CEL: they are integers in the rendered policy and lengths in the rendered StatefulSet. `test_the_pinned_pod_counts_equal_what_the_cell_chart_actually_renders` renders the cell chart in both capability modes and compares. Measured, every pinned count matches: volumes 6/4, `containers[0].env` 30/28, `containers[0].volumeMounts` 6/5, `initContainers[0].volumeMounts` 3/2. The refresh sidecar's flat `initContainers[1].volumeMounts == 5` reads like a rule that would refuse every legacy cell and is not: it sits inside `(!variables.activationAck || ...)`, and the ack-disabled render produces 2, short-circuited. The test asserts the guard as well as the count, so that reading cannot be lost. It is mutation-checked -- pinning 7 instead of 6 fails it with "chart renders (6, 4), policy pins [(7, 4)]" -- so it is not passing vacuously. Reading the policy also required handling two syntactic shapes, `size(X) == (cond ? a : b)` for volumes and mounts and `(cond ? size(X) == a : size(X) == b)` for env, the latter appearing once per pod variant. **What genuinely still needs the desktop K3s**: CEL *semantics* -- that the expressions mean what we read them to mean -- and the acceptance/refusal run the task names. `tests/test_hosted_k3s_admission.py` still has no activation-acknowledgement case and is skipped without `RUN_K3S_RUNTIME_TEST=1`. That is a much smaller residue than the whole count surface, and it belongs with 6.3.- [ ] 5.3 Document and preflight production certificate creation, encrypted custody, SAN/expiry validation and overlap rotation. Generate a separate disposable CA for tests. Production transport credentials and target deployment remain separately verified actions. Decision 9 settles the shape; these are its parts.
  - [x] 5.3.1 Add the `sops_k8s_tls_secret` destination kind to `infra/scripts/secret_handoff.py` and its matrix validation, sealing `tls.crt` and `tls.key` into one `kubernetes.io/tls` artifact under one lock. Keep the existing `(namespace, kubernetes_secret)` uniqueness guard intact.
  - [ ] 5.3.2 Add the matrix entries: the server pair to `k3s.activation-ack-tls.active` targeting the TLS Secret in the platform namespace, and the CA private key to an escrow-only destination. **Prepared 2026-09-21; one operator action remains, and it is correctly the last step.** The stated dependency on 5.3.4 is satisfied (the issuance script exists and is tested), and everything that does not need key material is worked out and verified below. What cannot be done here is the issuance itself: `secret_handoff` reads `SOPS_AGE_RECIPIENTS` from the environment, which is Hugo's, and this WSL is the laptop.
    - *Why the entries are not committed ahead of it.* They break CI on their own, which is the constraint this task and `docs/runbooks/hosted/secrets.md` both already state: an active Kubernetes destination with no selection entry makes the signed registry incomplete. Confirmed by running it -- with the entries added, `test_active_secret_selection_is_complete_and_the_signer_publishes_a_verified_pair` fails `assert 35 == 34`. The `expected` set already admits both Kubernetes kinds, so only the count and the selection line are outstanding.
    - *The exact entries*, validated by loading the real matrix through `secret_handoff.load_matrix` (50 secrets, both destinations resolving to their intended kinds), and cross-checked against the script's own constants `_TLS_SECRET`, `_CA_SECRET`, `_TLS_DESTINATION`, `_CA_DESTINATION` and against the renewal command's `--authority` path in the runbook:
      - `activation_ack_tls_pair`, source kind `generated-activation-ack-tls`, destination `k3s.activation-ack-tls.active`: kind `sops_k8s_tls_secret`, slot `active`, target `infra/secrets/provisioner/activation-ack-tls.{version}.sops.json`, namespace `exomem-platform`, kubernetes_secret `exomem-activation-ack-tls` (the name the chart mounts; not the Service name).
      - `activation_ack_ca_private_key`, same source kind, destination `escrow.activation-ack-ca.active`: kind `sops_escrow`, slot `active`, target `infra/secrets/escrow/activation-ack-ca.{version}.sops.json` -- which resolves to exactly the path the runbook's renewal command later passes to `--authority` -- secret_key `authority_private_key_pem`.
    - *The operator step*, from the runbook's first-issuance section, as one line: `infra/scripts/activation_ack_certificate_handoff.py --matrix infra/contracts/secret-destinations-v1.json --repository-root . --version v1 --platform-namespace exomem-platform --trust-bundle infra/secrets/public/activation-ack-ca.pem --new-authority`. It prints only public facts, including the `trustBundleSha256` the composition lock needs.
    - *Then, in the same change*: the two entries above, the printed digest into the lock via `--activation-ack-trust-bundle` / `--activation-ack-platform-namespace`, a `k3s.activation-ack-tls.active` line in `infra/contracts/active-secret-selection-v1.json`, the count bumped 34 -> 35, and a re-signed active-secret registry pair.
  - [x] 5.3.3 Add `validate_activation_ack_server_certificate` to `activation_ack_configuration.py`: single leaf, not a CA, `serverAuth`, SAN exactly `exomem-activation-ack.<platformNamespace>.svc.cluster.local`, chains to a CA in the bundle, bounded total lifetime and a remaining-lifetime floor. Leave `validate_activation_ack_trust_pem` structural and time-free.
  - [x] 5.3.4 Add the issuance script in the shape of `provider_recovery_keypair_handoff.py`: generate or reuse the CA, issue the leaf with AKI and SKI so `x509.verification` accepts it, validate before publishing, hand off atomically.
  - [x] 5.3.5 Add the rotation runbook section covering both tiers, and a per-run disposable test CA marked so the preflight refuses it as live readiness.
- [x] 5.4 Add and test preflight classification of clean, protocol-qualified and legacy pending/uncertain targets, and of remaining attestation window; refuse to begin work that cannot complete inside the window. Closed 2026-09-21 across its three subtasks: the classifier exists and is correct for the caller that genuinely observes a store tuple (5.4.1), wiring it into the two production paths was **withdrawn with evidence** because each already implements its half better and the window half would have replaced a repair with a refusal (5.4.2), and forward recovery/rollback refusal is implemented and tested in pieces that compose (5.4.3).
  - [x] 5.4.1 Pure classifier `activation_ack_preflight.classify_activation_ack_target`: direction-blind tuple comparison, the three statuses plus `stranded`, remaining-window arithmetic, refuse-to-begin on an insufficient budget, refusal when the capability is not bound to compatible releases, and a legacy-uncertain target that is never reconciled into looking clean.
  - [x] 5.4.2 **Withdrawn 2026-09-21, with evidence.** The task assumed one classifier serving two callers. Traced, the two callers need disjoint halves of it, and each already implements its own half better than a shared classifier could. **The acknowledgement path** (`activation_ack_api.acknowledge` -> `_advance`) does not need the tuple comparison: `advance_hosted_activation_bundle` already validates the full predecessor -> successor transition against the real store tuple from `publication["successor"]` and refuses with `ACTIVATION_CONFLICT` 409. That is strictly stronger than a direction-blind equality check, so wiring the classifier there adds a weaker second copy of an authoritative control. **The migration coordinator** already implements the window check, twice, and better: `_SOURCE_WINDOW_FLOOR_SECONDS` (`MIGRATION_JOB_DEADLINE_SECONDS + 300`) returns to `prepare` and **reissues** the window through `refresh_drained_source_bundle` rather than refusing, and only the signing key ending inside any possible window (`refreshed.expires_at - current <= MIGRATION_JOB_DEADLINE_SECONDS`) is terminal. **Wiring the classifier there would replace a repair with a refusal on precisely the cells the repair exists for.** `refresh_drained_source_bundle`'s own docstring says a migration starting more than one window after the drain would otherwise strand its cell; `DRAINING` is in `_SERVED_STATES`, so `classify_activation_ack_target` returns `STRANDED`/`may_begin=False` on that exact cell. `test_a_long_drained_cell_is_stranded_here_and_repairable_on_the_migration_path` pins the conflict so it is seen before anyone wires this in. Per the standing rule, removing a control that costs more than it protects is the finding, and it carries its reproduction. 5.4.1 stays: the classifier is correct for an operator adjudicating a cell, which is the caller that genuinely observes a store tuple. Two defects in it were fixed rather than left in a withdrawn task -- the legacy-uncertain mark was suppressed on stranded cells (the cells a restore can revive into looking clean), and `legacy_uncertain_mark` lost its default so a future call site cannot silently reclassify from live inputs. **Not covered, and deliberately not built**: the durable legacy-uncertain mark has no store. It needs a per-cell row in the provisioner DB, since the verdict is the platform's adjudication and must survive the cell being unreachable. Its population is legacy cells -- ones that committed a mutation with no protocol deployed -- and the repair deploys the capability before the alpha cell is provisioned, so that population is empty. Sizing governance to blast radius: specified in Decision 12, built when there is a cell that needs it.
  - [x] 5.4.3 Describe forward recovery after an activation advance and refuse an incompatible rollback; regenerate signed target and final consumer evidence only for the released exact source. Preserve legacy unknown mutation outcomes even when exact activation-only reconciliation restores parity; never invent their missing receipts or replace their identity. Bind the capability to compatible runtime/provisioner/chart releases and deployment locks. **Surveyed and closed 2026-09-21: every clause is implemented and tested, in pieces built across tasks 5.1-5.5.**
    - *Incompatible rollback is refused, in two places that compose.* `config.selected_runtime` carries `activationAcknowledgement` on the `active` selection and **drops it on `rollback`** (`test_v3_runtime_selection_returns_one_consistent_rollback_unit` asserts `selected.activationAcknowledgement is None` against `active.activationAcknowledgement == lock.activationAcknowledgement`). A worker whose environment still says serve then meets `preflight_activation_ack_listener`'s `binding is None` refusal -- "configured without a lock binding" -- rather than putting a listener in front of cells that have no trust bundle for it (`test_activation_ack_startup.py:235`). The refusal costs the operator a worker that will not start, at deploy time, before any cell is harmed, and env-says-serve plus lock-says-no-protocol is a contradiction rather than an eventually-consistent state, so fail-closed is the right category here.
    - *Evidence only for the released exact source.* `load_rollforward_evidence` returns its content-free digest proof only when exactly one `ROLLFORWARD` operation matches the external id and it is `FINAL` with a cell and a result ciphertext; anything else raises `KeyError`.
    - *Capability bound to compatible releases.* `classify_activation_ack_target` refuses `capability_bound and not release_compatible`, and the lock only ever hands the binding to the active runtime, so a rollback cannot carry the capability forward by omission.
    - *Legacy unknown outcomes survive restored parity.* The tuple comparison is direction-blind by construction, so later parity is never read as evidence about the mutation that lost its receipt; the durable mark (Decision 12) holds the verdict against live inputs, and 6.2.4 fixed the case where a stranded cell never requested one. Receipts are never invented: `prepared is None` falls through to the fail-closed `outcome_unknown` terminal rather than synthesizing a result (surveyed under 2.4).
    - **Deferred to 6.3, deliberately**: rehearsing an actual rollback against a capability-bound cell holding a committed-unacknowledged write. Every component above is unit-proven; what no unit test can show is the sequence on a real cluster, and that is the rehearsal's job rather than a second set of doubles.
- [x] 5.5 Implement the closed deployment-lock capability/trust extension, both signed-source declarations, exact conditional trust/egress recovery envelopes, retained encrypted operation binding, and unchanged-public-request replay across trust rotation. Verify capable-forward refusal without binding, legacy shape preservation, active-versus-rollback selection, and original trust retention.
- [x] 5.6 Decouple the vocabulary-authority generation from the activation epoch, or refuse `vocabulary_authority_floor == 2` on a cell with acknowledgement capability. Taken by refusal, in `VocabularyAuthority._custody_floor`: the two are named incompatible at the single point where the floor is read, so the failure is an explicit message at configuration time rather than a cell silently losing its authority mid-capture. Decoupling the generation stays the better long-term fix and is now the only thing standing between floor 2 and the capability. Per-write activation advancement invalidates every grant and reservation stamped with the pinned generation. Alpha cells mint at floor 1, so this is latent; add a test that fails if floor 2 and acknowledgement capability are ever combined.

## 6. Verify and deliver the repair

- [ ] 6.1 Run scoped runtime, mutation, governance, custody, provisioner and chart tests during development. At completion run the required full corpora, build/static/privacy gates and strict OpenSpec validation, comparing any known baseline failures by name. **Scoped runs done, named:** backfill/companion 143 passed 10 skipped; activation-ack + certificate suites 137 passed; vocabulary/admission/mount/custody 634 passed 14 skipped; helm contract (pinned Helm 3.19.4) 8 passed. **Provisioner corpus done:** 1748 passed, 69 skipped, via the exact CI invocation `uv run --project infra/provisioner --frozen --with-editable . pytest -q --confcutdir=infra/provisioner infra/provisioner/tests` (`infra/scripts/validate.sh:109`). Note for anyone re-running it: the provisioner's `asyncio_mode = "auto"` lives in its own pyproject, so a root-level `pytest infra/provisioner/tests` fails ~892 async tests, and running from `infra/provisioner` without the root editable fails collection on `import exomem`. Neither is a real failure. **Privacy gate** clean on every commit (4409 files, 4521 text payloads). **Strict OpenSpec** valid for this change. Remaining: the root full corpus and the build/static gates at the delivery boundary.
- [x] 6.2 Obtain independent review of the implemented authority and recovery cut, including real race and forged-evidence probes. Deliver committed, pushed ready PRs and merge/publish only within the operator's authorized scope. **Review run 2026-09-21** in its own lane over `origin/main...HEAD`, with adversarial probes executed rather than reasoned: minted certificates driven through the real `validate_activation_ack_server_certificate`, the real classifier run on synthesized states, and a `cryptography` 48.0.0 probe of public-key equality. Six findings, all fixed:
  - [x] 6.2.1 **Two deployment blockers (HIGH).** The listener Secret was mounted as a directory, so `tls.crt` was kubelet's `..data/` symlink and `_read_certificate` refused it -- and the refusal propagates out of `_run_worker`, so the pod CrashLoopBackOffs and every cell lifecycle operation stops. The chart states this precedent three times for ConfigMaps; the worker takes every other secret through `secretKeyRef`, so its first mounted Secret volume never inherited it. Separately `defaultMode: 0400` on a pod with no `fsGroup` and no `runAsUser` leaves the projection root-owned, denying the worker its own certificate. Fixed to per-key `subPath` and `0444`, pinned by a helm assertion and a startup test that builds the real projection tree.
  - [x] 6.2.2 **The rotation window ignored the chain (MEDIUM).** A 90-day leaf from a CA with 30 days left passed issuance and startup and logged `verified`; on day 30 path validation began rejecting it fleet-wide, with the operator-visible message reading as a wrong certificate rather than an expired authority. The window is now taken from the chain `verify` returns -- the anchor that signed this leaf -- and the report carries `authority_not_valid_after`.
  - [x] 6.2.3 **The warning never repeated (MEDIUM).** It fired once per process start; uvicorn resolves the certificate once and this Deployment has no probes, so a worker that started with hours left would serve an expired certificate indefinitely. `watch_activation_ack_certificate` re-runs the preflight hourly for as long as the listener serves, and never refuses.
  - [x] 6.2.4 **The legacy-uncertain mark was suppressed on stranded cells (MEDIUM).** The mark was decided inside the branch that lost, so the cells in the worst state never got the durable verdict -- and a restore can hand such a cell a fresh window, after which it reads `protocol-qualified` and `may_begin=True`. Decided from the inputs now, and `legacy_uncertain_mark` lost its default so forgetting it is a TypeError.
  - [x] 6.2.5 **The floor-2 refusal cost the cell (MEDIUM).** See Decision 13's 2026-09-21 amendment; relocated to custody mount.
  - [x] 6.2.6 **The attestation-renewal claim was independently confirmed**, with two qualifications this change records rather than fixes: the *restart* path re-runs the probe, so a floor-2 cell with a diverged tuple fails it there, and the fuse exists on floor 2 in both directions. Both are unreachable on the alpha shape (control v1, floor 1, no activated authority), where `runtime_status` returns `v1` before reaching the tuple compare.
  
  Reviewed and **not** accepted: the review's fallback for 6.2.5 -- adding `VocabularyAuthorityConflict` to the `runtime_status` handler -- was verified not to change the blast radius, because `mutations_allowed` is false on `unavailable` too and the probe closes hosted writes either way. It fixes only the secondary escape into other `runtime_admission` callers, which the relocation retires. Two named one-liners (no platform-side ingress NetworkPolicy on 8443; the `schema_v4` pin sniff skipping silently) were raised as observations, not recommendations, and are left as such.
- [ ] 6.3 Run the real mounted-image/worker/sidecar flow for first capture, cited recall, all acknowledgement loss cuts, renewal races, stale Secret refresh, sidecar restart and pod replacement. Retain bounded private diagnostic evidence before fixture cleanup; do not repeat an unchanged failed cluster run.
- [ ] 6.4 Re-run the connected ordinary empty-installation journey using the released signed image and final consumer commit. Complete owner consent/usefulness acceptance before friends; retain the original invite, tenant and durable attempt. This repair does not complete the later gateway/self-hosting/public redesign.

## Status, 2026-09-20

Boxes are checked only where code and executing tests were both confirmed in this
worktree. Evidence for the checked set:

- Runtime scope — `HELM_BIN=... .venv/bin/python -m pytest -q -p no:randomly`
  over the activation, journal, recovery, writer-lease, composition-lock and
  Helm-contract suites: 611 passed, 2 failed. The two failures are task 5.2's own
  red tests for the platform admission policy, which are the next work item and
  were previously skipped for want of `HELM_BIN`.
- Provisioner scope — `uv run --frozen --with-editable . pytest -q` over
  `infra/provisioner/tests`: 1713 passed, 69 skipped, 0 failed.
- Helm rendering — `tests/test_hosted_helm_contract.py` with real Helm 3.19.4:
  72 passed.

Deliberately left unchecked, with the reason:

- 1.4 — a prior session reported independent adversarial review of the
  preparation, but no evidence of it survives in the repository. Re-obtain it.
- 2.1, 2.3, 2.4 — the private recovery descriptor and the mutator seams exist and
  their own tests pass, but neither the writer-lease binding nor each selector's
  result recipe has been checked against the full contract these tasks state.
- 2.5 — no crash-cut or fresh-process recovery test exists yet. Caught exceptions
  are not the cuts this task requires.
- 5.1 — the cell side renders (trust ConfigMap, 8443 egress). The platform side
  has no activation-ack listener, Service or certificate Secret at all.
- 5.2 — in progress; its two tests are red by design until the platform admission
  policy lands.
- 5.3 — implemented except its matrix entries. Decision 9 records the destination
  kind, the expiry placement, the two rotation tiers and the test-CA marker.
  5.3.1, 5.3.3, 5.3.4 and 5.3.5 are done and green. 5.3.2 is the operator step:
  the matrix entries and the selection line can only land together with a real
  issued artifact, which needs the SOPS age recipients. The selection
  completeness check already counts the TLS kind, so that route cannot land
  unnoticed. The trust half was already governed and needed no new work.
- 5.4 — 5.4.1 done: the classifier exists and is green. 5.4.2 wiring and 5.4.3
  rollback/forward-recovery remain.
- 5.6 — done by refusal; see the task note.
- 6.1-6.4 — not started.

Two interactions found while recovering this work are not covered by design.md and
must be closed before 6.3:

1. A failed acknowledgement blocks serving-membership readiness attestation, so the
   provisioner's hourly window renewal cannot complete; once that window expires the
   cell cannot recover. Every acknowledgement failure is a one-hour countdown, which
   changes how a rehearsal may be run and what 5.4 preflight must detect.
2. Vocabulary authority v2 pins a persisted activation generation to
   `control.activation_epoch` and requires equality for every grant. This repair
   advances that epoch on every governed write. It is gated behind
   `vocabulary_authority_floor == 2`, the default is 1, and no provisioner code
   raises it, so it is latent rather than an alpha blocker.

## Execution ownership

The root owns contract adjudication and integration. After section 1 freezes interfaces, one runtime lane owns sections 2 and 4 plus the runtime proof route; one provisioner lane owns section 3's control-plane handler and section 5. Their file ownership must be explicit before dispatch. Shared descriptors/schema fixtures stay root-owned until frozen; do not concurrently edit server_hosted.py or chart files. An independent review lane attacks the trust/recovery boundary; final verification owns the actual mounted/connected behavior. Model and effort are selected from live harness capabilities at dispatch, with consequential judgment on the strongest appropriate allowed model.
