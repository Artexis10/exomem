## 1. Baseline and lane preparation

- [x] 1.1 Re-run the existing relation registry, graph/filter, structural suggestion, queue, mutation-terminal, bootstrap, Studio, and graph-value focused tests in the integration worktree; record assertion failures separately from the known machine-global state-root interference in the lane packet, and verify no source file changes
- [x] 1.2 Preserve red-first synthetic reproductions for invalid promotion skeletons, post-registration graph unavailability with no durable recovery demand, two Markdown parses per queue-eligible page, per-page corpus cosine work, lossy graph-native candidate evidence, and ref-only decision rescans; verify each reproduction fails for the intended pre-change reason before assigning implementation lanes
- [x] 1.3 Prepare orchestrator-owned `$lane-delegate` scratch packets from this OpenSpec change with absolute plan path and SHA-256, base revision, explicit file allowlists, isolated worktree/state/temp roots, dependency edges, red tests, acceptance commands, forbidden actions, stop conditions, and a fresh author-independent reviewer for every lane; verify each packet is internally consistent and its read-only environment preflight passes before launch
- [x] 1.4 After explicit plan approval, commit the OpenSpec artifacts on the integration branch as the immutable lane base; verify the packet plan hash matches committed bytes, and keep the branch unpushed until integrated implementation gates pass

## 2. Lane A — relation resolution and registry deltas

- [x] 2.1 Add red-first resolver tests in a lane-owned test module for complete core inventory, `part_of` reuse, full-registry exact canonical/alias collision checks, paginated extension and unregistered evidence with explicit totals/continuations, survivor-directed immediate/terminal replacement metadata, honest `relates_to`/no-edge outcomes, false-specificity abstention, and two-vault cache isolation; verify the new tests fail before production edits
- [x] 2.2 Implement the deterministic relation-vocabulary resolver and inventory helpers in a new `src/exomem/relation_vocabulary.py`, using injected/indexed recurrence evidence and no model or mutation path; verify `uv run python -m pytest -q tests/test_relation_vocabulary_resolution.py`
- [x] 2.3 Add red-first tests for namespaced clean-alias proposal construction, required parent/description/direction, complete duplicate-search metadata, additive alias/deprecate deltas, stale-hash convergence, immutable immediate replacement links, later intermediate deprecation with one active terminal survivor, cycle/terminal refusal, semantic-continuity validation of both delta and full-document candidates, and preservation of unrelated definitions; verify they fail before changing registry helpers
- [x] 2.4 Implement proposal, pure delta merge, additive-alias, deprecate/replace, terminal-survivor/predecessor closure, immediate-link immutability, and semantic-continuity helpers in `relation_registry.py` and `memory_schema.py`, and emit valid-but-incomplete namespaced promotion candidates plus date-bounded denominators; leave canonical persistence to Lane D after the graph-epoch API exists, then verify `uv run python -m pytest -q tests/test_relation_vocabulary_resolution.py tests/test_relation_registry.py tests/test_memory_schema.py`
- [x] 2.5 Have a fresh reviewer inspect Lane A's actual diff and rerun its part-of, truncated-inventory, alias-duplicate, semantic-immutability, multi-hop terminal/broad-survivor, stale-hash, false-specificity, and tenant-isolation reproductions; return findings to the author and verify the reviewer records acceptance only after fixes and focused tests pass

## 3. Lane B — graph rebind and relation query semantics

- [x] 3.1 After Lane A is accepted, add red-first graph-schema and parity tests for persisted resolver context, exact method-specific review inputs (including raw body-wikilink target plus internal first-occurrence order/location without changing public evidence), indexed queue/census metadata, raw/canonical identity, alias activation, scoped extension resolution, survivor-directed replacement, semantic-unit relations, unresolved targets, and a clean-full-rebuild versus registry-rebind comparison; verify the tests fail on the old graph schema
- [x] 3.2 Bump the derived graph schema and persist every exact registry-resolution and review/census fingerprint input, including activation signal version, plus covering indexes in `epistemic_graph.py`; verify pre/post public evidence objects, refs, signal versions, fingerprints, dismissals, and existing graph build/traversal/filter/hosted tests with a rebuilt sidecar
- [x] 3.3 Make `graph_sync.epoch_writes` detect the exact protected registry target and delegate to `registry_epoch_writes`, keeping `vault.batch_atomic_write` as the sole floor/checkpoint injector; implement the private-copy registry rebind and one full-marker dispatcher shared by live synchronization and `index_sync` deferred drain, choosing rebind only from proven registry/source/schema/generation state and otherwise rebuilding; test caught pre-commit rollback, harmless retained debt, abrupt mid-batch newer-floor/older-checkpoint recovery, completed-batch stop before worker registration, interrupted publication, and compare-and-swap marker retirement, then verify `uv run python -m pytest -q tests/test_relation_registry_graph_sync.py tests/test_epistemic_graph.py tests/test_index_sync.py tests/test_deferred_index.py tests/test_semantic_unit_graph.py`
- [x] 3.4 Expand exact, alias, parent-family, and active-terminal-survivor predecessor filters through one registry plan across graph, find, and traversal-profile consumers while preserving raw/canonical identity, existing `matched_via` values, additive `replacement`, immediate/terminal diagnostics, separate requested/resolved alias diagnostics, and exact precedence; verify `uv run python -m pytest -q tests/test_epistemic_graph_relation_filter.py tests/test_find_relation_filter.py tests/test_traversal_profiles.py tests/test_retrieval_explainability.py`
- [x] 3.5 Add the bounded relation-review batch API to the graph owner module with activation-parity coverage and set-based candidate rows for every deterministic graph-representable method; verify fixed snapshot/query counts, branch caps, ordering, byte-exact evidence/ref/fingerprint parity, and dismissal stability in the lane-owned graph tests
- [x] 3.6 Have a fresh reviewer inspect Lane B's actual diff and independently compare a rebound graph with a clean rebuild, prove the save caller supplies no epoch files, reproduce the pre-commit-marker race plus every ordered-batch/publication crash cut and restart recovery, verify live and deferred success CAS-clear exactly one full marker, inspect exact body-wikilink evidence and the review-batch query plan, exercise a multi-hop deprecated chain ending at `relates_to`, and check no inverse edge is synthesized; return findings to the author and verify focused graph tests pass after fixes

## 4. Lane C — graph-native relation review

- [x] 4.1 After Lane B is accepted, add red-first call-count tests proving a 3,600-page queue read opens one graph snapshot, uses a fixed indexed query budget, parses zero corpus Markdown pages, invokes zero embedding searches, and returns typed warming instead of fallback; verify the old queue fails these bounds
- [x] 4.2 Replace relation queue activation walks and per-page suggestion calls with orchestration over Lane B's bounded graph batch in `relation_queue.py`, preserving deterministic method order, refs, fingerprints, filters, coverage, and honest truncation; verify `uv run python -m pytest -q tests/test_relation_queue.py tests/test_relation_queue_graph_native.py tests/test_structural_relation_suggestions.py`
- [x] 4.3 Require source hints for newly returned accept/triage items, rederive one source candidate and reread at most that page, and limit hintless legacy compatibility to the bounded current queue prefix; verify drift, hash, fingerprint, already-authored, placeholder, dismissed, in-prefix legacy-ref, out-of-prefix deterministic-ref, and retired embedding-ref refresh tests pass in `tests/test_relation_queue_commands.py` and the lane-owned queue tests
- [x] 4.4 Add an explicitly synchronized concurrency integration test showing queue reads do not acquire or wait on the Markdown mutation boundary while a writer is held and graph publication states remain typed; verify behavior without a tight wall-clock assertion
- [x] 4.5 Have a fresh reviewer inspect Lane C's actual diff, rerun the original double-parse/cosine/ref-rescan reproductions and queue eligibility/fingerprint/dismissal parity cases, and verify no hidden Markdown, prefix widening, or embedding fallback remains before accepting the lane

## 5. Lane D — public command, mutation, and agent contract integration

- [x] 5.1 After Lane B is accepted, add red-first shared-surface tests for `connect_memory(operation="resolve-relation")`, `schema_memory` relation propose/save selectors, candidate continuation, date scope, mandatory source hints for new review items, bounded hintless compatibility, generated MCP/REST/CLI schemas, selector read/write classification, and invalid argument matrices; verify all public facades reach the same leaves
- [x] 5.2 Wire the resolver and proposal/delta-save operations through `commands.py`, command schemas, CLI parsing, REST/OpenAPI generation, hosted capability manifests, and egress selector coverage; add the invocation-specific narrow writer boundary and inner atomic registry guard, then verify command-surface, writer-lease, receipt, retry, and hosted tests
- [x] 5.3 Update `relation_registry.save_registry` to supply only registry YAML to `vault.batch_atomic_write` for both delta and legacy full saves, consume Lane B's automatically injected epoch and shared full-marker convergence outcome, and project completed/pending/failed state without turning a committed registry write into a retry; verify callers cannot append/bypass epoch files, in-place meaning/alias-removal/immediate-replacement changes commit neither registry nor new epoch, plus caught pre-commit rollback, retained-debt recovery, abrupt mid-batch recoverable epoch, completed-batch stop before registration, publication interruption, settled, contended, idempotent replay, and restart/reconcile tests
- [x] 5.4 Add red-first mutation-terminal tests for one bounded `relation_advisory` after committed unknown-relation writes across remember/observe/edit/replace, and for no advisory on registered, `relates_to`, no-edge, validation-only, or failed writes; verify the tests fail before terminal changes
- [x] 5.5 Project the existing unregistered facts centrally through `mutation_terminal.py` with point-only optional recurrence evidence and no corpus scan, and verify `uv run python -m pytest -q tests/test_mutation_terminal.py tests/test_relation_advisory_surface.py tests/test_note.py tests/test_semantic_creation_writers.py tests/test_semantic_lifecycle_writers.py`
- [x] 5.6 Update compact/full/diagnostic bootstrap, hand-authored generic scaffold, workflow skills, and operation references with the resolve/reuse/fallback/propose/save loop and bounded registry currency; regenerate checked public schemas and verify bootstrap budgets, fixture parity, workflow-skill tests, and `tests/test_scaffold_no_leak.py`
- [x] 5.7 Have a fresh reviewer inspect Lane D's actual diff and rerun public MCP/REST/CLI requests, C-lane source-hint interface fixtures, canonical registry-and-epoch batch crash cuts, mutation-boundary instrumentation, compact terminal cases, bootstrap size/leak checks, and graph-pending terminals before accepting the lane

## 6. Lane E — Studio relation worklist

- [x] 6.1 After Lanes C and D are accepted, add red-first Studio model/browser tests for server-order preservation, source-hint payloads, stale refresh, visible coverage/truncation, and distinct warming/pending/unavailable states; verify existing UI behavior fails the new states
- [x] 6.2 Update versioned Studio assets to render bounded relation queue states and send source path/hash/fingerprint/audit inputs without client-side reranking or per-page fallback; verify `uv run python -m pytest -q tests/test_studio_ui_model.py tests/test_studio_governed_flows.py tests/test_studio_routes.py`
- [x] 6.3 Inspect the real local Studio in Chrome DevTools at narrow and desktop widths and complete a keyboard-only queue refresh, accept, drift-refusal, dismiss, snooze, warming, and truncated flow; verify focus, labels, status semantics, network payloads, and console remain correct
- [x] 6.4 Have a fresh reviewer exercise the rendered Studio and its browser tests independently, return usability/accessibility or contract findings to the author, and verify acceptance only after fixes

## 7. Lane F — synthetic lifecycle, census, performance, and docs

- [x] 7.1 After Lane E is accepted, extend the synthetic graph-value manifest and evaluator with policy applicability, core `part_of` reuse, alias duplicate prevention, honest generic, false specificity, parent roll-up, survivor-directed replacement migration, and two-vault isolation; use an explicit scripted caller step so resolver candidate presence/rank never counts as selection or mutation, then verify `uv run python -m pytest -q tests/test_graph_value_benchmark.py`
- [x] 7.2 Add a deterministic relation-review scale/concurrency harness for at least 3,600 pages and twenty controlled request streams; use barriers to overlap queue reads with canonical Markdown changes on eligible graph pages, require every success to advance a graph checkpoint, and reject validation/no-op/excluded substitutes; report available/unavailable latency separately, committed/busy/failed outcomes, checkpoint generations, availability ratio, final-commit recovery latency, and structural counters without private content; verify the quiesced Linux run has at least two committed graph-relevant mutations, at least 90% available queue completions, current availability within 5,000 ms after the final graph-relevant commit, one snapshot, fixed queries, zero corpus parses/embeddings, available p95 below 1,000 ms and max below 2,000 ms, and unavailable p95 below 250 ms
- [x] 7.3 Add a privacy-safe synthetic relation census command/report with optional origin-date/page-type scopes; core/extension/deprecated/generic/unregistered counts; distinct zero-authored-relation and zero-body-connection counts; and included/undated/excluded denominators, then verify stable old/current cohort output without filesystem-mtime inference, automatic debt scoring, registry promotion, or edge authoring
- [x] 7.4 Update relation registry, authoring, review Studio, graph diagnostics, and benchmark documentation with the shipped workflow and typed warming/recovery semantics; verify every example is synthetic and documentation names only exported fields and operations
- [x] 7.5 Have a fresh reviewer rerun the lifecycle corpus, scale harness, census privacy checks, and documentation examples against the actual integrated commands before accepting the lane

## 8. Integration and completion gates

- [x] 8.1 Reconcile the integration branch against every independently accepted lane diff in dependency order, resolve shared signature/generated-fixture changes in the orchestrator lane, and verify branch bytes, review dispositions, and task state agree before running `git diff --check` plus the combined focused relation/graph/queue/terminal/bootstrap/Studio/benchmark suite
- [ ] 8.2 Run `uv run ruff check .`, the repository lean test suite, and `tests/test_latency_gate.py` on its supported Linux gate; verify any red is classified against the pre-change baseline and all attributable failures are fixed
- [ ] 8.3 On a quiesced machine, run `PYTHONPATH=src EXOMEM_DISABLE_EMBEDDINGS=1 uv run python -m pytest -q` and the dedicated relation-review scale/concurrency gate; verify no assertion or teardown failure remains before claiming completion
- [ ] 8.4 Run a synthetic end-to-end public-surface smoke test that authors an unknown alias, receives compact guidance, resolves/proposes/saves it, proves the committed registry has a durable graph epoch across a simulated restart, then traverses and filters the exact extension, parent family, clean alias, active-survivor predecessor, and deprecated-key history while preserving raw identity
- [ ] 8.5 Run `openspec validate make-relation-vocabulary-self-evolving --type change --strict` and `openspec validate --all --strict`; inspect predecessor changes `surface-relation-vocabulary-promotion` and `add-relation-acceptance-queue`, and archive them only if their remaining tasks are now evidenced and their deltas can be synchronized without dropping later requirements
- [ ] 8.6 Have a fresh verifier exercise the integrated public workflow, concurrency behavior, full test evidence, privacy boundary, and OpenSpec-to-code traceability; fix every attributable finding and obtain an explicit READY verdict

## 9. OpenSpec closure and delivery

- [ ] 9.1 Mark tasks complete only from code, test, benchmark, review, and rendered-UI evidence; synchronize this change's delta specs into canonical specs, archive it with the OpenSpec CLI, and verify strict validation before and after archive
- [ ] 9.2 Inspect contribution, PR-title, CI, release, and privacy rules; commit only intended scope with no private identifiers or AI attribution, integrate current `origin/main` safely in the feature worktree, and verify the completion gates still pass
- [ ] 9.3 Push the feature branch and open a ready-for-review Conventional Commit pull request containing the outcome and verification evidence; keep the worktree alive for review follow-ups and do not merge without explicit authorization
