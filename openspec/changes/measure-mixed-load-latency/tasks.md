## 1. Harness

- [x] 1.1 Add failing tests for timing cohorts, small-sample tails, failed calls, exact read/search assertions, and disposable-root refusal; verify the focused harness tests after implementing the public driver.
- [x] 1.2 Add failing tests and a read-only graph proof rejecting stale bytes, missing files, legacy/unacknowledged lineage, pending receipts, changing membership and wrong workload graph content; verify tests and real smoke catch-up.
- [x] 1.3 Run fresh idle/media smoke cases with real pinned extraction; inspect raw outcomes, initial index proof, individual timings and final graph proof.

## 2. Evidence and delivery

- [x] 2.0 Add current-source adapters and focused regressions; smoke both locked builds through public Markdown read/write/search, relation replacement, concurrent writes and process-crash recovery in disposable state.
- [x] 2.0a Add separate post-crash and eventual post-restart Markdown file proofs, preserving exact body bytes outside metadata/final-LF presentation; pass focused negative tests and exercise both current sources through disposable public smokes, retaining all product failures. Keep earlier cohort evidence explicitly labeled with its original proof coverage.
- [x] 2.1 Run at least three alternating fresh idle/media pairs at the declared corpus size with task tests quiesced; retain all observations and timeout/failure evidence.
- [x] 2.1a Correct premature Basic Memory startup admission using its native completion stamp and a 900-second common startup bound; retain search membership/miss diagnostics, pass negative tests and disposable current-source smokes, and preserve the original invalid cohort unchanged.
- [ ] 2.2 Run three alternating common-workload repetitions for both latest pinned revisions. Track the reproducible report, runtime/fixture/source hashes, sample counts, all correctness/recovery failures and limitations; ground source comparisons in those revisions. Identify required product repairs before claiming broader parity.
- [ ] 2.3 Obtain independent review of the harness and report, resolve findings, and pass focused tests, privacy/lint/OpenSpec checks and the completion-boundary full CI run.
- [ ] 2.4 Merge after user approval, synchronize the canonical spec and archive with the CLI; verify strict OpenSpec validation and remote main, then remove the clean published task worktree.
