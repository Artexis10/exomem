## 1. Red-first semantic fixtures

- [ ] 1.1 Add the generic versioned baseline contribution input at `tests/fixtures/hosted_v5_contributions/personal_baselines.json`, covering stable preferences, recurring routines, historical baselines, durable affiliations, and their fleeting, one-off, incidental, trivial, or tentative twins.
- [ ] 1.2 Pin expected Entity, compiled-observation, Records, and no-write routes without embedding private vault content.
- [ ] 1.3 Retain separate executed-method positive, failure, parameter-boundary, and unreusable-one-off fixtures.
- [ ] 1.4 Add mutants that remove stability, reusable-comparison value, or semantic routing and prove each is killed by the fixture validator.

## 2. Carrier contract tests

- [ ] 2.1 Add failing semantic-parity tests for compact, full, and diagnostics bootstrap in balanced and maximal profiles and quiet-behavior tests for off and light.
- [ ] 2.2 Add failing parity tests for scaffold, capture workflow, operations routing, prominence output, documentation blocks, structural reminder, and generated Claude Code copies.
- [ ] 2.3 Assert that filtered descriptors retain command-free doctrine and never advertise an unavailable command.
- [ ] 2.4 Assert the compact bootstrap budget, generic scaffold leak guard, and generated-copy sync gate before changing prose.

## 3. Portable and bootstrap implementation

- [ ] 3.1 Add the baseline predicate and routing to bootstrap while preserving the separate executed-method contract and existing compact budget.
- [ ] 3.2 Update the generic scaffold, capture workflow, relevant operations reference, prominence renderer, documentation blocks, and structural reminder coherently.
- [ ] 3.3 Regenerate defined Claude Code plugin copies from the scaffold and verify privacy, byte-parity, and sync gates.

## 4. Hosted v5 candidate

- [ ] 4.1 Establish one Hosted implementation lane as owner of every v5 source, fixture, profile, render, lock, archive, and promotion file; sibling lanes may edit only their generic inputs under `tests/fixtures/hosted_v5_contributions/`.
- [ ] 4.2 Add v5 profile and candidate-owned resolution for every shipped skill and pin v1-v4 to their historical command schemas so mutable canonical command objects cannot change their descriptors or actual-wire admission.
- [ ] 4.3 Validate and canonicalise the four contribution inputs, freeze their snapshot into one candidate-owned combined fixture, and bind its digest through compatibility, package, locks, archives, and promotion evidence before the first v5 render or lock.
- [ ] 4.4 Render one v5 package and lock set, prove the package carries the exact combined-fixture digest, add a mutation/freshness test proving fixture drift invalidates package verification, and prove one complete v1-v4 manifest remains identical across source bytes, generated bytes, locks, archives, definitions, fixtures, promotion records, resolved command schemas, and actual-wire identities.
- [ ] 4.5 Capture clean supported-client traces for all four changes' positive and negative fixtures and require the combined bound evidence for promotion.
- [ ] 4.6 Add rollback tests proving pre-lock candidate rebuild is allowed, promoted v5 is immutable, and any post-promotion correction requires v6.

## 5. Verification and delivery

- [ ] 5.1 Run focused fixture, carrier, bootstrap-budget, Hosted profile/package/promotion, plugin sync, public-input, and scaffold leak tests.
- [ ] 5.2 Run required mutation tests and record killed-mutant evidence.
- [ ] 5.3 Run `ruff check`, the full non-model pytest suite, and `openspec validate --all --strict`.
- [ ] 5.4 Report false-positive results separately and do not claim comparative no-nudge benchmark evidence without its governed amendment.
- [ ] 5.5 Run delegation-envelope fixtures proving baseline writes never bypass the mapped class disposition or confirmation.
- [ ] 5.6 After all four changes are implemented, independently reviewed, merged, and represented in promoted v5 evidence, refresh each delta against the then-current canonical specs and archive in this exact order: `add-governed-curation-lane`, `complete-recurring-entity-lifecycle`, `preserve-adopted-generated-artifacts`, `capture-durable-personal-baselines`; preserve requirements introduced by earlier archives and run `openspec validate --all --strict` before and after every archive.
