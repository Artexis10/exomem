## Context

The service and repository identified as 0.71.0 when the incident was captured. The resumed before/after measurements use the current 0.73.1 product baseline at `5fc2e55d`; the later integrated main revisions do not change product source bytes. The incident ledger establishes slow server work, not just a connector gap. The existing fast-ACK feature remains default-off. Its receipt/overlay guarantees are already implemented and must survive these changes. Live queues observed after the incident were empty; this does not establish their state during it.

Current ordinary hybrid retrieval admits a maintained lexical snapshot, but optional graph fallback requests an exactly current resolver. A resolver refusal escapes candidate collection and discards all otherwise valid lexical/vector results. Separately, the media command synchronously calls the multi-consumer deferred drain after reconciling an artifact. Neither coupling is necessary for its public semantics.

## Goals / Non-Goals

Measure and reduce the critical path from durable writes to a verified useful result. Preserve canonical ownership, exact pending visibility, policy admission, immutable evidence, and strict relation-filter semantics. No live-vault benchmark writes, deployment, global flag enablement, or new model is part of development acceptance.

## Decisions

1. **Separate optional expansion from mandatory recall proof.** Handle typed resolver-warming outcomes at the optional graph resolver seam in `find.py`/`find_candidates.py`. Omit unproven graph contributions and disclose `graph` as warming. Do not catch catalogue, pending-visibility, governance, structured-filter, or relation-query refusals. Preserve offline fallback. Never call a fallback resolver implicitly after the seam returned unavailable. An old graph snapshot is not served as current.

2. **Media requests own enqueue, not convergence.** Remove synchronous deferred-index draining from `commands._process_media`. Existing fields report zero synchronous refresh and the aggregate global full-upsert count; they do not imply per-item index ownership. The normal background owner retains durable custody. Add a mutually exclusive bounded `paths` selector (1–32 unique canonical governed artifacts), accepted for process/retry only. Reject selector conflicts, invalid shape/bounds and canonical aliases before work; reject paths with status. Execute canonical reconciliation serially under existing per-item guards; expensive extraction already belongs to independent media jobs. Preflight errors are no-work failures; subsequent artifact races/failures are per-item outcomes. Exact full-batch replay uses the existing request digest and returns the same terminal without reconciliation. Retrying a failed subset is a new request with only failed paths, never reusing the original identity with changed arguments. Add a validated compact media projection (at most 32 items): normalized path, closed outcome/state, media type, sidecar path/job id, requeued count, bounded code/remediation. Reject/drop malformed projection by the existing terminal convention; never copy raw errors. Preserve single-path fields. No batch-wide guard or atomic all-or-nothing claim.

3. **Fix amplification at the proven producer/consumer seam.** Reproduce current writer echoes, busy withdrawal, predecessor admission, and long graph drains against disposable state. Known canonical events must advance consumers through exact dirty paths; unknown external edits retain withdrawal-before-clear. A retained complete delta can coalesce intermediate generations only when its source/target and path custody are proven. Missing lineage keeps explicit recovery. Never clear `external_pending` on timeout or weaken byte/policy proof. The implementation must be justified by rerunnable evidence before accepting this task.

   The current reproductions place watcher events both before destination replacement (snapshot timestamp restoration) and between replacement and post-commit self-write registration. Repair these windows with transaction-scoped exact publication intents. For an existing destination, register atomically after snapshot raw SHA-256, size and stable identity are proved, but before the first timestamp restoration. The token contains that before-image and the already staged after-image. For a missing destination, register an after-image token after absence is checked and before replacement. Do not blanket-hold unknown bytes before snapshot proof exists. Fast-receipt behavior remains separately protected; do not expand its foreground work or replace its receipt/overlay lifecycle.

   Each token progresses through `prepared -> installing -> installed -> postpublish_verified` before terminal success. BEFORE bytes may enter held custody only when their digest has completed and the same token is still active and prepared under the registry lock. Binding an observation while active does not prove when its bytes were read. Move the exact path token to installing immediately before replacement and installed immediately afterward, before any post-replacement callback. AFTER bytes remain admissible through active installation phases. A late observation whose proof crosses phase or terminal state rechecks current bytes once and may suppress only exact AFTER; an old descriptor or a foreign restoration of BEFORE is not transaction proof. Mismatched, unreadable, oversized or identity-unstable bytes take the external path. Hash outside the suppression lock. If the current token changes during a hash, retry once against the replacement token; unresolved churn remains external.

   Register watcher/path observation custody before hashing, using deduplicated weak subscriptions rather than one strong callback per event. Abort or expiry must fence even an observation whose hash is still running and has not entered the held queue. Notify outside the suppression lock and coalesce replay under the watcher lock. A token created inside snapshot capture is locally aborted if capture/restoration raises, and outer staging failure aborts every earlier token. After corpus publication returns successfully, reprove exact AFTER bytes for every intended token and advance only the same active installed tokens to postpublish_verified. Finalization accepts success only from that phase. A failed corpus publication or post-publication reproof removes legacy self-upsert suppression and retains the index-sync publication fallback from current bytes; a multi-path failure cannot claim batch-wide corpus publication. No filesystem I/O runs under the suppression registry lock.

   Held events remain in the watcher's coalesced custody and schedule deadline wakes. Success drops them only after proven post-commit registration. Abort, failed registration, partial rollback, expiry and capacity eviction actively promote observed paths to external pending and replay them; never silently evict an observation. End-of-transaction failure marks pending before returning, including rollback-incomplete remnants. Overflow or unsupported/unreadable/oversized proof fails open to the existing external path. Expiry must progress without unrelated events, not rely on opportunistic pruning. Tests cover normal publish-before-register, duplicate events, foreign bytes, rollback remnants, registration failure, expiry alone, capacity, and hashing overlapping finalization. The separate appeared-target topology scan under the mutation boundary remains measured follow-up work, not part of this race repair.

   **Media computation hands publication to the service parent.** Extraction success, processing failures, binary-stale pending presentation and legacy blocked-presentation repair must no longer replace canonical Markdown in the disposable child. Add a bounded, schema-versioned canonical-JSON computation-result table beside the existing media jobs, not a cross-process publication-intent protocol or claim-lease subsystem. Add a monotonically increasing `claim_revision` to jobs: `attempts` is user-facing retry accounting and can be refunded, so it cannot fence an old claim. Fence child result publication, completion, failure and deferral against the current claim revision and state; parent result transitions also compare the exact payload digest.

   A child captures the exact sidecar-before hash and the existing binary identity, computes, persists its result, then returns a distinct handoff outcome that terminates that claim before CLIP/re-embed. Success leaves the job running; a failure result and blocked/failed job state persist atomically. The parent clears only the completed OCR stage; remaining or newly added stages receive a fresh claim. Recovery and worker launch exclude result-bearing jobs. `join()` includes all pending results, including failed/blocked presentation. Preserve the current binary-identity contract (SHA-256 through 8 MiB, stat certificate above); the inherited larger-file limitation is not exact byte proof. Do not add unbounded binary hashing under mutation authority in this repair.

   The parent supervisor drains bounded results while the child is alive. Under its existing writer guard it revalidates the result/claim, sidecar preimage and binary certificate, renders the exact target, adds a full receipt, and persists target hash/size plus that exact receipt revision before replacement. A pure extraction renderer shares the existing preservation rules and bounded text cap; result validation also bounds speaker metadata and error/action fields before SQLite. Canonical publication uses the existing governed media writer and deferred graph completion. An explicit `publication_intents_out` collector carries local tokens out of guarded media writes into parent fanout; arbitrary no-fanout writes acquire no implicit suppression owner. Same-process media reconciliation uses the same collector. Do not replace the fast-ACK receipt lifecycle with this collector.

   After releasing authority, complete the exact graph handoff and parent fanout without awaiting a whole rebuild. If the floor/predecessor moved, use `recover_full_receipt_graph_epoch(build=False)` and retain debt unless recovery and fanout are proved; do not silently abandon held intents on an early return. A prepared result is recovered by current sidecar bytes: expected-before retries publication; exact target-after resumes graph recovery and parent fanout; a third value is stale/foreign and is never overwritten. Clear only the stored full-receipt revision after successful fanout, then CAS-finalize the result/job. A crash before finalization may repeat idempotent fanout, never lose newer debt. Failure/expiry retains durable recovery and fences observed paths. Binary-only stale input is re-presented as pending by the parent before fresh-claim retry; an independently changed sidecar is preserved. Legacy blocked presentation repair moves to the supervisor.

   This tranche covers the PDF/image extraction producers observed in the incident. Scene-frame JPEG/sidecar publication from CLIP remains a named residual cross-process producer when that optional mode is enabled; a future bounded spool-manifest handoff can move it to the same parent boundary. Do not claim every media child canonical writer has been removed.

4. **Use existing MCP concurrency for generic dependency ordering.** Independent read/media requests can be issued in one agent tool batch; canonical conflicting edits remain ordered. Selected media batching closes a schema gap. Multi-edit already batches edits within a page, and semantic observations/relations can be authored in the initial note. Document a generic client dependency graph and validate it in the benchmark; a second server transaction engine is unnecessary for this repair. Source-independent tracker, background-note and unrelated capture work may proceed during extraction. A compiled note citing a media sidecar depends on that artifact's relevant extraction completion: source closure fingerprints the sidecar and stages guarded backlink writes, so bypassing a concurrent extraction change could overwrite fresh evidence. Keep that guard strict and order only the dependent evidence-backed write after media completion, not all independent note work.

5. **Measure the real public path.** A new benchmark drives registered product commands through a persistent MCP client/server, using disposable isolated state and production managed warm-up. Model-free runs use auto/FTS5 and declare embeddings disabled; graph scheduling and watchers remain active. Seed realistic heterogeneous Markdown, an active tracker, archived related note, critique, a PDF and two images. Preserve/process evidence, update observations and relations, correct a stale edge, then verify direct bytes and search. Model-free proves preservation and durable enqueue/custody only. A separately labelled real-extraction variant must await public completion and assert unique PDF/image content, report extraction engine/dependency versions and convergence, or report blocked/null. Run the optimized workflow with product-required calls and bounded final verification; run a separate stress variant with per-write probes and include them in its full timing/call counts. Probes can trigger repairs or warm caches, so their costs cannot simply be subtracted from the same run. Record successful graph-warming disclosure as optional lag, never as a call-level refusal.

6. **Attribution is additive only where measured.** The ledger provides leaf and server-wrapper durations. Join request identifiers where available. Report its first inferred start to last completion as `ledger_observation_span_ms` and unoccupied intervals as `server_idle_within_observed_span_ms`. Actual workflow wall time/client gaps require a client trace and remain null in ledger-only reports. Compute interval unions separately from summed server work. Model planning and connector overhead remain null unless independently timed. Instrument derived elapsed time, graph incremental/rebuild calls, and source scan counts/bytes in the benchmark. Keep raw personal logs and paths out of published evidence.

7. **Compare a shared functional subset under the existing fairness programme.** The immediate result is an internal diagnostic, not a publishable competitive-suite ranking. Pin Basic Memory 0.23.2's verified wheel digest and resolved dependencies, preserve config provenance/glue accounting/variant identity, and use a warm persistent MCP session for both products with identical Markdown edits, text queries and exact-read assertions. Any adapter fault invalidates its row. Allow native background indexing to converge and measure it separately; do not invoke post-write reindex. Evidence immutability/media are separate capability observations. Publication as a competitive result must follow the existing `add-competitive-benchmark-programme` own-harness/direct-row and independent fairness-review requirements. No internal SQL search substitutes for public APIs.

8. **Keep admitted catalogue reads on one proven projection.** Health and background repair may demote strict readiness while a compatible published catalogue remains usable. After warm-up, re-prove request admission independently of that ready bit. Bind only lagging admitted checkpoints in an immutable request-local context keyed by resolved vault and catalogue scope; an explicit checkpoint takes precedence. Every nested `find` starts an empty context and restores its predecessor on exit. Stale path projection reads exact catalogue entries at the admitted checkpoint; it never reconstructs a newer canonical triple. Catalogue readiness, pinned query transactions and resolver-entry reads recheck current access/policy and semantic identities. Exact pending rows overlay the published projection, while strict health remains unavailable until its live proof succeeds.

## Risks / Trade-offs

- Optional graph omission changes ranking during repair: disclose it and never apply the relaxation to graph-dependent predicates.
- A batch can partially commit: validate shared input first and return item outcomes; retry only failed items with stable mutation identity.
- Background convergence can remain slow after fast ACK: benchmark it and gate warming/refusal independently of ACK percentiles.
- Instrumentation adds cost: report configuration and avoid presenting synthetic/model-free timings as full production extraction performance.
- Concurrent machine load affects timing: run performance samples serially after test workers quiesce; retain configuration and timing provenance.

## Migration Plan

Additive media selector and unchanged single-artifact fields require regenerated public tool contracts. Review actual diff and independent reproductions, run the full test corpus once at delivery, then open a ready PR. Production rollout and the existing fast-ACK rollout gates remain explicit. Archive only after merge/shipping evidence and non-optional acceptance are complete.
