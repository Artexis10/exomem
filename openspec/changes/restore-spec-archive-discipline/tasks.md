## 1. Baseline and archive census

- [x] 1.1 Capture current active/archive/canonical counts and prove `openspec validate --all --strict` is green on current `origin/main`.
- [x] 1.2 Replay the task-complete archive set in a disposable worktree, oldest first with tranche validation, and record the mechanical successes and exact refusals.
- [x] 1.3 Corroborate the selected archive set with shipped code, tests, and merge evidence; include implemented prerequisites with stale open benchmark/deployment tasks only when their delivered scope is independently clear.

## 2. Canonical spec migration

- [x] 2.1 Archive the mechanically safe shipped changes in ascending creation/dependency order and keep strict validation green after each bounded tranche.
- [x] 2.2 Repair and archive the vector-backend chain (`add-sqlite-vec-backend`, `make-sqlite-vec-opt-in`) from the current full `find-recall-efficiency` and `live-index-freshness` requirements.
- [x] 2.3 Repair and archive the bootstrap/product chain (`redesign-product-command-surface`, `productize-cognition-layer`, `productize-pack-surface`) without dropping current bootstrap scenarios.
- [x] 2.4 Repair and archive stale current-superset deltas in `complete-low-interrupt-mode`, `make-records-first-class-and-recoverable`, `surface-authored-contradictions`, `add-epistemic-inbox`, `add-one-command-onboarding`, and `remote-connector-quickstart`.
- [x] 2.5 Reconcile `close-technical-memory-gaps` against its already-canonical requirement and repair the missing-base `allow-source-to-evidence-promotion` delta before archiving both with explicit equivalence evidence.
- [x] 2.6 Inspect the resulting canonical spec diff for missing requirements, duplicate requirement names, or scenario loss; run `openspec validate --all --strict` over the post-migration tree.

## 3. Archive discipline gate

- [x] 3.1 Add red tests for a stdlib-only archive-debt audit: fully checked active tasks fail and name the change; unchecked, missing, malformed, and archived task lists do not produce false completion claims.
- [x] 3.2 Implement the read-only audit with deterministic ordering, machine-readable output, and actionable failure text; make its tests green.
- [x] 3.3 Change the required OpenSpec CI job to validate `--all --strict` and then run the archive-debt audit; add a focused workflow-contract test.
- [x] 3.4 Add the evidence-first same-delivery archive rule to both repository agent instruction files and keep their shared project guidance aligned.

## 4. Verification and closure

- [ ] 4.1 Run the focused archive-audit/workflow tests, strict OpenSpec validation, Ruff, `git diff --check`, and the lean project suite with embeddings disabled.
- [ ] 4.2 Review the complete migration diff independently for requirement/scenario loss and verify the required checks from a clean process.
- [ ] 4.3 Synchronize and archive `restore-spec-archive-discipline` itself so `openspec-record-discipline` is canonical and no task-complete active change remains.
