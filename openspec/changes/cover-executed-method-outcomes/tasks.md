## 1. Establish the real cause before writing any text

- [x] 1.1 Confirm the failing surface's prominence level and that its capture clause already said "keep the bar low". Outcome: ChatGPT is in `HOOKLESS_SURFACES`, so it runs `maximal`, which already did.
- [x] 1.2 Confirm delivery was not the failing link. Outcome: the user's client carried the documented custom-instructions block, so the re-armer was present and the miss happened anyway.
- [x] 1.3 Read the actual enumeration on each carrier and confirm the case is absent rather than merely under-emphasised.

## 2. Carriers

- [x] 2.1 Add the class and its routing to the bootstrap epistemic contract, the only contract a hosted or generic MCP client ever sees.
- [x] 2.2 Add it to the `balanced` and `maximal` capture clauses; leave `light` and `off` untouched, since they do not capture on the agent's own judgment.
- [x] 2.3 Add it to the scaffold skill's stepping-stone block and its implicit-routing table.
- [x] 2.4 Add it to the documented copy-paste blocks, the only lever on a client with no hooks and no skill.
- [x] 2.5 Regenerate the packaged Claude plugin skill copies from the scaffold rather than editing them.

## 3. Budgets

- [x] 3.1 Keep the compact bootstrap payload under its byte ceiling. Measured: 56,277 -> 56,929 against a 58,000 ceiling, leaving 1,071.
- [x] 3.2 Keep each copy-paste block under its documented character cap. Measured: maximal 1,137 -> 1,264 and balanced 1,020, against ~1,500.

## 4. Verification

- [x] 4.1 Add a contract test pinning the class on every carrier, so a future edit cannot quietly drop it from one of them.
- [x] 4.2 Run the bootstrap contract, budget, prominence, scaffold-leak, plugin-sync, hosted-plugin, and schema-fidelity gates.
- [x] 4.3 Confirm the packaged tool-surface fingerprint is unchanged.
- [x] 4.4 Record honestly that this change is not falsifiable by the deterministic bench: it is agent behaviour, and the AT-1 agent track that would measure it is a sequence-3 amendment that has not been filed.
