## ADDED Requirements

### Requirement: Collection-claims replay families f28 and f29
The pre-registration SHALL register family f28 `collection_promotion_replay` and family f29 `claimed_collection_routing_replay` (operational journeys; no public coverage) through one dated, reasoned §7 amendment entry as sequence 4 with a pending `preregistration-amendment-receipt.v1`, and the frozen registry SHALL mirror them (`f28 → 4`, `f29 → 4`). Both SHALL be withheld from comparative runs, scores and claims until founder acknowledgment under the same typed refusal as sequences 1 to 3. Their trajectories SHALL use only the existing operation vocabulary. f28 SHALL pair `collection_candidate_surfaced_within_budget` — a quiet-capable signal assertion composing `signal_absence_checked_across_all_surfaces` for a frequency-matched one-off twin — with `ledger_state_matches_expectation`, a state assertion over the fold of the corpus annotations that requires a subject and a snapshot pair. f29 SHALL pair `claimed_observation_reflected` — an item per annotated publication whose natural key matches and whose `sources` link the preserved artifact — with the existing `no_structured_write_beyond_expectation`. Both corpora SHALL be pure functions of their inputs, SHALL include turns that land nothing, and SHALL refuse at construction and at load any user turn matching the store-bearing vocabulary extended for these families with the words naming a collection, ledger, schema, claim or the act of tracking. The families SHALL add no budget constant beyond the provisional detector constants they read, no catastrophic assertion and no operation kind; `COMPOSES_ABSENCE_META` and `REQUIRES_SUBJECT` SHALL gain the f28 quiet assertion; any projector change SHALL be additive and version-bumped.

#### Scenario: Registered and withheld at once
- **WHEN** the sequence-4 receipt is present but pending
- **THEN** f28 and f29 appear in the §1 registry and every surface that runs, scores or records them refuses with the typed pending-acknowledgment error naming sequence 4, while f01–f27 proceed as their own receipts prescribe

#### Scenario: A store-bearing turn refuses at load
- **WHEN** a corpus turn for f28 or f29 says to make a collection, keep a ledger, or track something
- **THEN** corpus construction and scenario loading refuse naming the turn and the match

#### Scenario: The twin stays quiet on every surface
- **WHEN** the f28 twin episode with one-off purchases is replayed
- **THEN** `collection_candidate_surfaced_within_budget` passes only if no candidate signal names the domain on any checked surface, including the due-state counters

#### Scenario: Coverage never publishes without its dual
- **WHEN** an f28 or f29 run is reported
- **THEN** the state assertion is reported beside its false-write or quiet dual from the same run and arm, and no figure aggregates across tiers or arms
