# Tasks — route observed state through collection claims

Every test lands red first (failing output recorded before the implementation, then green). Scoped suites are named per task; the full suite runs once at the end. Measured sizes and pins are written here when the task is ticked. Depends on `repair-records-writer-representation-and-held-records` for the `coverage` block; rebase onto it before task 5.

## 1. Claims (design D1)

- [x] 1.1 `structured_collections.py`: `_claims_json_schema` (four bounded string lists, ≤ 24 each), `CollectionManifest.claims`, `_parse_claims`, `describe` documentation. Red-first tests in `tests/test_structured_collections*.py`: round-trip through inspect and describe; oversized list and non-string entry refuse naming the list and entry; manifests without `claims` unchanged.
- [x] 1.2 Effective-claims computation (`record_governance.effective_claims(manifest, observed)`): declared ∪ derived from enum values, low-cardinality string values (`MAX_DERIVED_VALUES`, PROVISIONAL) and a declared array-of-string tags field; `_terms` normalisation; `BREADTH_TAGS` exclusion; values must recur on ≥ 2 items; routing-target predicate (≥ `MIN_CLAIM_TERMS`, active lifecycle). Red-first pure tests: free-text field contributes nothing; generic values excluded; single-item value excluded.
- [x] 1.3 Claims projection in `due_state.py` (`claims[<manifest path>]`): rebuilt by reconcile from the inspection census; folded by `apply_record_write_delta` from the written values without re-reading the collection. Red-first tests: reconcile equals fold after an append; a withheld manifest's claims are never served.

## 2. Routing advisory (design D2)

- [x] 2.1 `records_routing` computation (`collection_claims.route(terms, targets)`): coverage per routing target; strict-winner rule at `MIN_CLAIM_COVERAGE` (PROVISIONAL, start 2); payload `{collection, title, matched_terms ≤ 6, natural_key, strength}`; `strong` when a natural-key-shaped value is present. Red-first pure tests: winner, tie → none, miss → none, strength rule, bounded terms.
- [x] 2.2 Wire into compiled writes (`semantic_writes.py`, after the guarded write, fail-open like `_structure_suggestion`) and the Evidence preserve path (sidecar tags, title, description); `mutation_terminal` projects a bounded top-level `records_routing` with its own validation. Red-first tests: note and Evidence writes carry the advisory with one shape; terminal fields unchanged; a raising computation leaves the write committed with no key, warning or error; a withheld collection is never named (release-filter fixture).
- [x] 2.3 `tests/test_mcp_schema_fidelity.py`, tool-surface fingerprint and compact-envelope pins updated deliberately with the reason recorded here.

## 3. Coverage family (design D3)

- [x] 3.1 `due_state.py`: second structured family `unreflected_observations` with its own bucket, entry identity `(collection_id, observation_ref)`, composer, `OBSERVATION_GRACE_HOURS` pending/due handling, fingerprint `sha(collection_id, observation_ref, sorted matched_terms)`; registered in `PROJECTION_CATEGORIES`, `DELTA_CATEGORIES` (structured), `attention.DEFAULT_ATTENTION_CATEGORIES`, `audit.ALL_CATEGORIES`. Red-first tests: entry added by a matching write; pending inside grace, served after; fingerprint changes when matched terms grow; family appears in `registered_families()` and `triage_memory(quiet)` works.
- [x] 3.2 Record-write settle rules in `apply_record_write_delta`: drop entries the written record links (`link` fields, `sources`) or whose matched terms contain a written natural-key value; only the written collection's entries change. Red-first tests: settle by link; settle by key; other collection untouched; delta equals reconcile.
- [x] 3.3 `audit._check_unreflected_observations` (recompute over Evidence sidecars and compiled pages within `OBSERVATION_LOOKBACK_DAYS` against current effective claims); heal on reconcile when the page is gone or the collection no longer covers the terms. Red-first tests: resolve by state change; serve-time recomposition and cross-audience count leak test (the #758 lesson).
- [x] 3.4 Mutation-removal probe: removing the family from `STRUCTURED_DELTA_CATEGORIES` stops write-time maintenance while reconcile still produces the finding.

## 4. Candidate sensor (design D4)

- [x] 4.1 New `collection_candidate.py`: pure detector over a units table with PROVISIONAL constants (`SPREAD_MIN_PAGES`, `DATES_MIN`, `SPAN_MIN_DAYS`, `STATE_UNITS_MIN`, `IDENTITIES_MIN`, `MAX_DOMAIN_TERMS`, `MAX_EVIDENCE_UNITS`), default state lexeme list, currency-amount and ISO-date shapes, exclusions (breadth tags, project keys, core categories, terms covered by effective claims). Red-first pure tests: the positive fixture (four generic pages, three dates, two weeks, purchases and amounts, two identities) fires strong or moderate deterministically; twins stay quiet (single page; same-day pages; no state units; already-claimed term); byte-stable ordering.
- [x] 4.2 `audit._check_collection_candidate(vault_root, pages)` registered in `ALL_CATEGORIES`, `EPISTEMIC_REVIEW_CATEGORIES`, `due_state.PROJECTION_CATEGORIES` (recompute-only); finding shape (`review_partition` = term, `meta.domain_terms`, `meta.evidence_units`, `signal_version`); release filter applied to pages. Red-first tests: finding present after reconcile and counted in the due-state block served on bootstrap and recall; absent from the default attention listing and present when requested; resolves after a claiming manifest is created; withheld pages contribute nothing; new evidence resurfaces a dismissed candidate.

## 5. Coverage and describe (design D3, D6)

- [x] 5.1 `record_governance.inspect_collection` coverage gains `unreflected` (count, ≤ 20 refs) and `pending`; inventory gains `unreflected`; complete/partial/blocked derivation documented in the response. Red-first tests including release filtering on both page and manifest. (Rebase onto the held-records change first; `held` comes from there.)
- [x] 5.2 `describe` state-ledger example (identity + effective date natural key, status enum, `observed_precision` enum, `sources` links) with the backfill rule sentence; `tests/test_scaffold_no_leak.py` and the describe contract test green.

## 6. Contract text (design D5, D7)

- [x] 6.1 Bootstrap (`commands.op_bootstrap`): `records_routing` handling clause, `collection_candidate` category and the collection-creation confirm rule; trim equivalent bytes first; record compact size before and after and the ceiling; extend `tests/test_epistemic_bootstrap_contract.py` pins red-first.
- [x] 6.2 Envelope teaching: bootstrap serves collection creation as `restructure_execution`; `envelope.py` docstring/class description names it; standing-delegation refusal covers it. Red-first test in the envelope suite.
- [x] 6.3 Scaffold `references/planning-records.md` one paragraph; plugin copy md5-identical; hosted renders regenerated; skill-sync tests green.

## 7. Bench, amendment sequence 4 (design D8)

- [x] 7.1 Corpus builders `benchmarks/epistemic/journeys/f28_promotion_replay.py` and `f29_claimed_routing_replay.py` as pure functions with per-turn annotations, distractor turns, the twin episode for f28, and the extended store-bearing refusal list (`collection`, `ledger`, `schema`, `claims`, `track` plus the existing vocabulary); generic vocabulary under the no-leak rule. Red-first tests: turn-for-turn pin between fixture YAML and corpus; a store-bearing turn refuses at construction and at load.
- [x] 7.2 Assertions `collection_candidate_surfaced_within_budget` (composes `signal_absence_checked_across_all_surfaces`; requires subject), `ledger_state_matches_expectation` (requires subject and snapshot pair), `claimed_observation_reflected` (requires subject) in `assertions.py`; registry rows, `COMPOSES_ABSENCE_META`, `REQUIRES_SUBJECT`, `REQUIRES_SNAPSHOT_PAIR`, `PREREGISTERED_FAMILIES`, amendment mapping `f28 → 4`, `f29 → 4`. Red-first tests: mirror-drift checks; withheld-until-acknowledged refusal at load, evaluation and manifest.
- [x] 7.3 `PREREGISTRATION.md` §1 rows, §2 assertion list, §4 predicates and the dated §7 sequence-4 entry; receipt `benchmarks/epistemic/contracts/amendment-2026-09-collection-claims.v1.json` with `acknowledged_on: null`; ratified-identity drift check green with the receipted chain.
- [x] 7.4 Fixture scenarios `fixtures/sequence4/f28-*.yaml`, `f29-*.yaml` (two arms each, seed snapshots per phase); one development run per arm recorded as a finding, never a claim (harness faults block, not score). If the attention or due-state surface is not projected, add one additive projector field and bump the projector version, recording it in the receipt rationale.

## 8. Verification

- [x] 8.1 End-to-end journey on a fixture vault through the MCP-facing surfaces: preserve an Evidence artifact carrying a claimed platform and account → `records_routing` names the collection → inspect shows `pending: 1` → after the grace window (clock injected) the due-state block counts one `unreflected_observations` → `record_memory(append)` with `sources` linking the artifact → count zero, coverage complete; then a compiled-notes fixture with a recurring unclaimed domain → reconcile → `collection_candidate` counted on bootstrap → create a claiming collection → candidate gone.
- [x] 8.2 Gates: scoped suites during iteration (`tests/test_record_*.py tests/test_structured_*.py tests/test_due_state*.py tests/test_attention*.py tests/test_audit*.py tests/test_semantic_writes*.py tests/test_mutation_terminal*.py tests/test_epistemic_*.py tests/test_tool_surface_fingerprint.py tests/test_mcp_schema_fidelity.py tests/test_scaffold_no_leak.py`, exact names confirmed at start); at completion the full suite, `uv run ruff check`, mypy as CI runs it, `openspec validate route-observed-state-through-collection-claims --strict`, `openspec validate --all --strict` (pre-existing Purpose warnings excepted), `uv run python scripts/validate-public-artifacts.py --repository`; record counts here.
Post-deploy follow-up (optional, outside implementation closure): inspect a live claimed collection after reconcile, preserve a matching observation and check routing, and inspect candidate findings for claimed and unclaimed domains. Run after a release containing PR #1212 is installed, through governed tools. This acceptance remains pending; fixture MCP journeys cover the implementation contract.


## Implementation evidence

- Benchmark corpus, assertion, registry, loader and real-projector scope: 96 passed; existing no-nudge/meta-composition scope: 70 passed. After benchmark commit `ff80e747`, the history/governance scope returned 103 passed and one stale withheld-family expectation; the corrected expectation passed its focused rerun. All sequence-four load, evaluation and manifest gates remain armed.
- Independent benchmark review reproduced and corrected missing attachment input, pre-confirmation twin writes, incomplete client turns, orphan files, dangling provenance, display-text normalization and artifact/sidecar identity collisions. Actual preservation tests cover Markdown, sidecar paths, stable memory references and artifact paths; tampered bytes and unrepresented Markdown/text files fail the paired checks.
- Projector 0.6.0 adds authored collection claims/values, item locators, candidate domain/evidence metadata, stable page identities, exact source body, observed artifact hashes and non-Markdown Evidence/Records file inventory. Existing display text keeps its normalization. Sequence-four receipt remains pending; no comparative results are released.
- Development attempts covered f28 positive, its twin and f29 in both native arms. All six were blocked before client launch by the repository-ancestor isolation guard; zero agent turns ran. These are recorded harness setup findings, with no product score or replay-success claim. The guard remains enforced.
- Compact bootstrap baseline after the merged request-budget change: 59,737 bytes. This change: 60,505 bytes (+768), against the unchanged 63,300-byte ceiling, leaving 2,795 bytes. The original design's approximate 100-byte headroom premise predates the request-budget trimming.
- Repository checks before final commit: OpenSpec 1.10.0 strict 196/196; Ruff correctness and the full CI contract gate pass; CI mypy scope passes on 14 source files; public-artifact privacy passes. Final corpus and merge evidence is recorded below.

- Product scopes after corrections: collection claims 58 passed; audit and attention 45; due-state 154; Records and structured collections 194; mutation terminals and semantic-write latency 100; note/preservation regressions 77. Review regressions cover reflection support after observation edits, UTC datetime strength, per-item recurrence, whole natural keys, audience-relative support, artifact aliases, grace boundaries, retained overdue observations, stale census handling, dispositions and Unicode normalization.
- Contract scopes: MCP schema, tool fingerprint, compact bootstrap, scaffold privacy and workflow skills 45 passed; delegation/resource envelopes 86; plugin synchronization 11; hosted rendering, legacy schema and source-identity contracts 105 passed. Tool input schemas and the connector fingerprint remain unchanged. The bootstrap stays within its existing ceiling; only workflow skill contract stamps and teaching text change.
- Hosted generation exposed an older stale v1 OpenAI lock. Its regenerated digest now agrees with the already-current compatibility descriptor and Claude lock; the matching generated-file fixture was refreshed. Restoring the prior envelope constants in memory produces identical generated bytes, confirming this is existing lock drift. Hosted source definitions, skill bundles, archives, registered app identity and promotion records remain byte-identical.
- Final support regression: changing audience-visible derived claims revalidates natural-key reflection before suppressing a gap; direct source-link support remains valid. The two-case regression passed, and the complete feature scope passed 58 tests.
- Independent final review approved after all findings were corrected (nine focused product checks and two hosted checks). Actual 29-tool FastMCP journeys passed routing, exact grace, artifact-link settlement across repeated reconcile, audience-specific reflector/candidate filtering, candidate resolution and quiet-to-normal disposition transitions; runtime state stayed outside the fixture vault. Final full CI passed as recorded below.

- Completion gate: [CI 34705384600](https://github.com/Artexis10/exomem/actions/runs/34705384600) passed on `ed9a3532e1fce9a15b6584f136451b1ebda1038e`: all 12 core shards (15,374 passed, 378 skipped), all four harness shards (2,767 passed, 59 skipped), Windows native NTFS, installed-product E2E, package, lint/type, OpenSpec and the required gate. Zero test failures or errors; the failure-name set is empty against the exact base. Independent verification downloaded all 16 JUnit artifacts.
- Final integration scopes: 151 assertion discrimination tests, 37 receipt/replay tests and 58 review-queue tests passed after updating the extended amendment chain and intentional registry/family pins. Registry equality and sequence-three ratification checks remain enforced.
- Delivered by [PR #1212](https://github.com/Artexis10/exomem/pull/1212), squash-merged 2026-09-12 at `b840d58e7f956352f20ce01e23b5a4c1550d2a28`. Delivery is merged source; production acceptance above remains a later release/deployment operation.
