## 1. Terminal persistence precedes derived acknowledgement (writer lease)

- [x] 1.1 Red first: fault-injection test in `tests/test_writer_lease*.py` / `tests/test_mutation_terminal*.py` where the derived acknowledgement raises after a canonical commit; assert today's `MUTATION_COMMITTED_ACKNOWLEDGEMENT_UNCERTAIN`, then the target: success terminal, `derived_sync="pending"`, warning naming the component class, and a same-identity retry replaying the persisted terminal without re-executing the leaf.
  - Evidence: `tests/test_writer_lease.py::test_lost_derived_acknowledgement_persists_a_pending_success_terminal`, bound to the post-persistence hook. Red: `_PostCommitOutcomeUncertain: {"code":"MUTATION_COMMITTED_ACKNOWLEDGEMENT_UNCERTAIN",...}` raised at `writer_lease.py:664` (1 failed). Green: 1 passed. Per 1.5 it asserts the derived component (`embeddings` in `derived_sync_components` and in the warning) and that no exception class name reaches the terminal. Files: `tests/test_writer_lease.py`, `src/exomem/writer_lease.py`.
- [x] 1.2 Reorder `writer_lease.py` so `_persist_completed_from_canonical` runs before the derived acknowledgement; project acknowledgement failure or budget exhaustion into the persisted terminal (`derived_sync`, warnings) and update the persisted record when acknowledgement later succeeds within the call.
  - Evidence: `IdempotencyStore.run` persists the canonical terminal, then runs `after_operation_guard`; `_derived_acknowledgement_pending` degrades on `_PostCommitOutcomeUncertain` and `_advance_completed_terminal` refreshes the persisted row on success. Green: `tests/test_writer_lease.py tests/test_mutation_terminal.py tests/test_fast_write_ack.py` 296 passed. Files: `src/exomem/writer_lease.py`, `tests/test_fast_write_ack.py` (two tests asserted the repealed behaviour and now assert the modified requirement).
  - Resolved 2026-09-10 by design.md D1 (invariant paragraph): the guard is split by command class at the composition site; see 1.5. The earlier OPEN note is retired: `after_operation_guard` also finalizes a `reconcile(rebuild_graph=True)` handoff, which must precede persistence, and the two crash-window tests in `tests/test_graph_rebuild_availability.py` are that invariant's executable form.
- [x] 1.5 Split per D1: `IdempotencyStore.run` keeps `after_operation_guard` in its pre-persistence position (as on main) and gains a post-persistence acknowledgement hook whose `_PostCommitOutcomeUncertain` degrades via `_derived_acknowledgement_pending` and whose success refreshes the row via `_advance_completed_terminal`. At the composition site one shared predicate (today's `joins_unbounded`: `reconcile`, `maintain_memory(mode="reconcile")`, or a `_graph_rebuild_handoff` in the leaf) binds `finish_fast_ack_and_graph` to the pre-persistence hook for those commands and to the post-persistence hook for every other command; its internal order is unchanged. The degraded terminal sets `derived_sync_components` from the components whose acknowledgement did not complete and its warning names that component, not the exception class. `tests/test_graph_rebuild_availability.py::test_crash_after_canonical_terminal_persistence_never_replays_the_write` and `::test_manager_rebuild_graph_resumes_canonical_handoff_after_restart_without_waiter` pass unchanged; the 1.1 and 1.3 tests bind to the post-persistence hook.
  - Evidence: the new hook is `after_terminal_acknowledgement`. `writer_lease._carries_graph_rebuild_handoff` plus the `joins_unbounded_graph` closure is the one predicate, read by `wait_for_graph_sync` (replacing its inline copy) and by the two binding wrappers `finish_before_terminal_persistence` / `finish_after_terminal_persistence`; the handoff clause is result-dependent, so the choice is made when the guard is reached and recorded in `guard_ran_before_persistence` so a guard that already ran (and stripped the handoff that selected it) is not run twice. `_acknowledge_derived_batches_timed` now tracks which components it still owes and attaches them to `_PostCommitOutcomeUncertain.derived_components`; `_derived_acknowledgement_pending` sets `derived_sync_components` from that and names the components in the warning. Green: `tests/test_writer_lease.py tests/test_fast_write_ack.py tests/test_graph_rebuild_availability.py` -> 357 passed, 3 skipped (Windows), 1 pre-existing failure (`test_rebuild_publication_hold_runs_no_disk_or_sqlite_work`, reproduced against a pristine `git archive HEAD src`). Both named crash-window tests pass on the file unchanged.
- [x] 1.3 Keep committed-uncertain for terminal-persistence failure: red-first test that injects a storage error in `_persist_completed_from_canonical` and asserts the unchanged committed-uncertain error and the canonical-resume retry.
  - Evidence: `tests/test_writer_lease.py::test_terminal_persistence_failure_precedes_the_derived_acknowledgement`. Red: `assert proofs == []` -> `AssertionError: assert ['prove'] == []` (the acknowledgement ran before persistence). Green: 1 passed; code, `status="committed"`, `committed=True` and the canonical-resume replay unchanged. Files: `tests/test_writer_lease.py`.
- [x] 1.4 Bound the acknowledgement step by the remaining request budget minus a fixed delivery reserve (constant, one module, PROVISIONAL), so a slow index or graph fan-out cannot consume the whole 60 s origin budget after the terminal is safe.
  - Evidence: `_MUTATION_REQUEST_BUDGET_SECONDS = 60.0` / `_TERMINAL_DELIVERY_RESERVE_SECONDS = 5.0` in `src/exomem/writer_lease.py`. Superseded in shape by 7.4 (C4): the budget now bounds only the component wait, via `deadline = min(fast-ack deadline, budget_deadline)`, and the earlier `_check_acknowledgement_budget` guard is gone. Current test: `tests/test_writer_lease.py::test_derived_acknowledgement_budget_bounds_the_waiting_only`.

## 2. Per-file terminal states and content-hash idempotency

- [x] 2.1 Red first: `preserve_artifacts` batch returns `request_id`, `receipt_id`, per-file `state` (`stored` / `already_stored` / `failed`) with path, ref, hash, hash_algorithm, size, media_id, content_type, warnings; summary counts all three; `outcome` mirrors `state` for one release (`already_stored` → `outcome="stored"` with `duplicate_of`).
  - Evidence: `tests/test_client_artifacts.py::test_preserve_artifacts_reports_one_terminal_state_per_file`. Red: `KeyError: 'state'`. Green: 1 passed. `request_id`/`receipt_id` come from the existing `committed_terminal` envelope and are asserted there. Files: `src/exomem/client_artifacts.py` (`_batch_result`, per-file rows), `tests/test_client_artifacts.py`.
- [x] 2.2 Red first: duplicate bytes under a new identity yield `already_stored` naming the existing path and ref; no new file or sidecar; audit chain unchanged; identical bytes under a different category are stored (two facts).
  - Evidence: `tests/test_client_artifacts.py::test_duplicate_bytes_under_a_new_identity_are_reported_not_stored`. Red: `KeyError: 'state'`. Green: 1 passed; the retry adds no directory entry and the same bytes under `food/butter-mayo` are stored. Superseded in one detail by 7.1 (C1): the retry now *does* call `mark_active_mutation_committed`, which is what earns it a terminal envelope; the directory listing is what proves nothing was written, and the reviewer confirmed by full vault hash diff that nothing is written and the audit chain does not advance.
- [x] 2.3 Implement the destination-scoped sha256 lookup over sidecars in `client_artifacts.py` after staging and before commit; apply the same states to `preserve_evidence`.
  - Evidence: `preserve.find_duplicate_artifact` (sidecar `governance_companion.artifact_sha256` under `Evidence/<scope>/<category>/`) called from `client_artifacts.preserve_artifacts` before the mutation guard and from `commands.op_preserve`. `PreserveResult.as_dict` carries `state`. `tests/test_client_artifacts.py::test_preserve_evidence_reports_a_duplicate_instead_of_a_second_copy` 1 passed.
- [x] 2.4 Mixed batch: middle file fails, first and third store; per-file states and summary are exact; existing scenario tests stay green.
  - Evidence: `tests/test_client_artifacts.py::test_mixed_batch_states_are_exact_when_the_middle_file_fails` 1 passed. Scoped gates `tests/test_client_artifacts.py tests/test_preserve.py tests/test_call_ledger.py tests/test_writer_lease.py tests/test_mutation_terminal.py` 365 passed; adjacent `tests/test_attachment_source_ingestion.py tests/test_due_state_bulk_carriers.py tests/test_rest_registry.py tests/test_reserved_admin_paths.py tests/test_personal_baseline_contract.py` 347 passed.

- [x] 2.5 Compact projection: `mutation_terminal.py::_artifact_receipt_projection` carries per-file `state` (one of `stored`, `already_stored`, `failed`) and a bounded `duplicate_of` (`path`, `ref`, each a string of at most 2048 characters) on the rows it already validates; `_path_projection` and the ledger `target_paths` (4.4) count only rows whose `state` is `stored`. Red first through `response_detail="compact"` in `tests/test_mutation_terminal*.py` or `tests/test_client_artifacts.py`.
  - Evidence: `tests/test_mutation_terminal.py::test_compact_artifact_rows_carry_the_terminal_state_and_duplicate`, `::test_compact_artifact_paths_count_only_what_this_call_stored`, `::test_compact_artifact_row_with_an_unknown_state_is_invalid`. Red: 3 failed (`state` and `duplicate_of` absent from the projected rows; an unknown state projected as a valid stored row). Green: 3 passed. `duplicate_of` is now the bounded mapping the task names, so `client_artifacts.preserve_artifacts` and `commands.op_preserve` emit `{"path": ..., "ref": ...}` instead of the bare path they emitted at 2.2/2.3. `_artifact_state` reads `state` and falls back to `outcome` for the adoption/source lanes, which have no states; `_path_projection` and `commands._note_committed_artifact_targets` both use it, so a duplicate contributes neither a terminal path nor a ledger target. Beyond the task text: the projected summary is counted by state as well, and carries `already_stored` when the leaf reports it -- otherwise compact would have said `stored: 1` for a batch whose own rows said `already_stored`. Green: `tests/test_mutation_terminal.py tests/test_client_artifacts.py tests/test_call_ledger.py tests/test_preserve.py` 165 passed.

## 3. Destination segments are validated, never normalised

- [x] 3.1 Red first: `scope="food/caviarhouse-group-order"` refuses with `INVALID_PRESERVE`, `details.field="scope"`, reason naming `/`, accepted form in the message; zero handles fetched (assert the fetcher is never called) and no Evidence directory created. Same for a category with `:` or a control character, and for leading/trailing whitespace.
  - Evidence: `tests/test_client_artifacts.py::test_destination_segments_are_refused_never_normalised` (5 cases) and `::test_preserve_evidence_refuses_a_reserved_character_in_category`. Red: `Failed: a malformed destination must not fetch` (the fetcher ran) and `DID NOT RAISE OpError` -- 6 failed. Green: 6 passed.
- [x] 3.2 Replace sanitisation of `scope`/`category` in `_validate_destination` and the destination join with strict single-segment validation in `client_artifacts.py` and the `preserve.py` entry used by `preserve_evidence`; keep `_sanitize_segment` for filenames.
  - Evidence: `preserve.destination_segment_refusal` + `DESTINATION_ACCEPTED_FORM`; `client_artifacts._validate_destination` raises `OpError` with `details={field, reason, accepted_form}`; the adoption join uses the validated values byte-for-byte; `preserve.preserve` refuses instead of sanitising (still `PreserveError`, so `/upload` and the media worker keep their handlers); `commands.op_preserve` refuses with the same `details`. `_sanitize_segment`/`_sanitize_filename` still own filenames.
- [x] 3.3 Valid segments are used byte-for-byte; existing preservation tests stay green.
  - Evidence: `tests/test_client_artifacts.py::test_valid_destination_segments_are_used_byte_for_byte` 1 passed. `tests/test_client_artifacts.py tests/test_preserve.py tests/test_attachment_source_ingestion.py` 136 passed; `tests/test_upload_endpoint.py tests/test_hosted_transfer_v2.py tests/test_media_processing.py tests/test_media_worker.py` 272 passed, 1 failed -- `test_private_v1_allows_only_one_active_upload`, reproduced identically against a pristine `git archive HEAD src` checkout, so pre-existing and unrelated.

## 4. Hosted error shape and ledger targets

- [x] 4.1 Red first: `_hosted_mutation_error_details` returns `status="uncertain"`, `committed=None` for `MUTATION_OUTCOME_UNKNOWN`; add a test that enumerates the terminal's status-bearing codes and asserts each has a shape entry.
  - Evidence: `tests/test_hosted_private_routes.py::test_hosted_outcome_unknown_carries_the_structured_fields` and `::test_every_status_bearing_mutation_code_has_a_hosted_shape` (AST scan of `writer_lease.py` for codes given a `status`+`committed` detail pair). Red: 2 failed -- `Extra items in the left set: 'MUTATION_OUTCOME_UNKNOWN'`. Green: 2 passed.
- [x] 4.2 Add the shape entry in `server_hosted.py`.
  - Evidence: `"MUTATION_OUTCOME_UNKNOWN": ("uncertain", None)` in `_HOSTED_MUTATION_ERROR_SHAPES`; the hosted route returns 409 with `status="uncertain"`, `committed=None`, `receipt_id`.
- [x] 4.3 Red first: the ledger row for a mixed `preserve_artifacts` batch carries the committed vault-relative paths in `target_paths` and no URL, handle id or content; extend the "no note content reaches the ledger" CI test with a file handle fixture.
  - Evidence: `tests/test_call_ledger.py::test_an_artifact_write_records_what_landed_and_no_handle` drives the real wrapper through `CallTraceMiddleware`. Red (with the wrapper's ledger call commented out): `AssertionError: assert [] == [...]` -- the live `target_paths: []`. Green: 1 passed; the signed URL, `sig=`, `download_url`, `file_id` and the bytes appear nowhere in the ledger directory.
- [x] 4.4 Hand outcome paths to the ledger from the `preserve_artifacts`, `preserve_evidence` and `capture_source` command wrappers.
  - Evidence: `call_ledger.note_committed_targets` / `pop_committed_targets` (keyed by `call_spans.MCP_CALL_TOKEN`, the same bridge spans use), merged into `_target_paths`; `commands._note_committed_artifact_targets` called from `op_preserve_artifacts`, `op_preserve` (both lanes) and `op_capture_source`'s file lane. Scoped gates `tests/test_call_ledger.py tests/test_hosted_private_routes.py` 99 passed.

## 5. Contract text and derived artifacts

- [x] 5.1 `src/exomem/_scaffold/_Schema/references/mutation-results.md` (and the plugin copy): the three per-file states, the same-identity retry rule for a lost acknowledgement, and that a duplicate under a new identity is reported rather than stored twice; one paragraph, no nudging.
  - Evidence: one paragraph added after the `graph_sync`/`derived_sync` paragraph; the plugin copy is regenerated, not hand-edited. `tests/test_scaffold_no_leak.py` green.
- [x] 5.2 Regenerate in order and commit together: `exomem package-skills --plugin-root plugins/claude-code`; `scripts/hosted-plugin.py render`; `scripts/hosted-plugin.py render --candidate hosted-alpha-agent-v5 --platform all --openai-app-id <id>` and `check`; `scripts/generate-capabilities.py` then `--check`; tool-surface contract sha and `tests/fixtures/mcp_tool_schemas.json`; `deploy/chatgpt/personal-plugin-contract.json` pending digest.
  - Evidence: `scripts/refresh-skill-contract.py` first (the packager refused a stale stamp) -> contract `1cafa2e8...`; `package-skills` -> "Synced plugin v0.77.0"; `hosted-plugin.py render` and the v5 render with app id `plugin_asdk_app_6a5e3d26f2b08191a04424d1c1b33fc0` -> `check` prints "Hosted generated artifacts are current"; `generate-capabilities.py` then `--check` -> "docs/capabilities.md is current". The hosted renders and `docs/capabilities.md` produced no diff. The tool response schema did not change (`outputSchema` is part of the fingerprint and `tests/test_tool_surface_fingerprint.py` is green), so `tests/fixtures/mcp_tool_schemas.json`, `tool_surface_contract.json`'s sha and `deploy/chatgpt/personal-plugin-contract.json`'s `pending_tool_surface_sha256` (`d1e78d2e...`) are unchanged and current. `package-skills` also rewrote `plugins/claude-code/.claude-plugin/plugin.json`'s version 0.76.0 -> 0.77.0; reverted, because `tests/test_plugin_sync.py` excludes that field by design and the release tooling owns it. Gates: `tests/test_tool_surface_fingerprint.py tests/test_mcp_schema_fidelity.py tests/test_plugin_sync.py tests/test_personal_baseline_contract.py tests/test_hosted_v5_candidate.py tests/test_scaffold_no_leak.py` 93 passed.

## 6. Verification

- [x] 6.1 Scoped gates during iteration: `tests/test_writer_lease*.py tests/test_mutation_terminal*.py tests/test_client_artifacts*.py tests/test_preserve*.py tests/artifact_adoption/ tests/test_hosted_private_routes.py tests/test_call_ledger*.py tests/test_tool_surface_fingerprint.py tests/test_mcp_schema_fidelity.py tests/test_plugin_sync.py tests/test_personal_baseline_contract.py tests/test_hosted_v5_candidate.py`, each run with `XDG_STATE_HOME` pointed at a scratch directory. `tests/artifact_adoption/` was added after the CI shard suite found preservation-path failures there twice that no gate list covered.
  - Evidence: final run on the delivered bytes 2026-09-11 (the list above plus `tests/test_graph_rebuild_availability.py`, `tests/test_fast_write_integration.py`, `tests/test_fast_write_ack.py`, `tests/test_scaffold_no_leak.py`, `tests/test_workflow_skills.py`): 861 passed, 3 skipped (native Windows contracts), 1 failed = `test_rebuild_publication_hold_runs_no_disk_or_sqlite_work`, which fails identically on the untouched base tree 245fc488 (`GraphRebuildLockUnavailable` on this host). Independent review: round 1 REQUEST_CHANGES (section 7), round 2 REQUEST_CHANGES (7.21), round 3 APPROVE.
- [x] 6.2 Completion boundary: full suite in the CI shard layout (12 `EXOMEM_TEST_TIER=core` + 4 `harness` pytest-split shards), failure-name diff against the branch base, `uvx ruff check . --select F`, CI full-ruff file list, `uvx mypy` CI file list, `openspec validate --all --strict` with the CI pin (`infra/tool-versions.env`), `scripts/check_openspec_archive_discipline.py`, `scripts/validate-public-artifacts.py --repository`, `scripts/generate-capabilities.py --check`; record counts here.
  - Evidence: full suite on the delivered bytes 2026-09-11 as 12 core + 4 harness shards: 17,893 passed, 44 failed, 8 errors, 426 skipped. Failure-name diff against 245fc488: 52 branch ids, 51 base ids, none only on base, 1 only on branch (`tests/test_foreground_activity.py::test_interrupted_foreground_entry_restores_background_scope`), which passes unloaded in 0.17 s; the 51 shared failures are environmental here (permission-safety tests refusing the 0775 worktree dir under umask 0002, installer and benchmark tests needing `bun` or `netstat`, hosted-platform tests needing signing inputs, one perf budget under shard load). `uvx ruff check . --select F` pass; CI full-ruff list pass; CI mypy list 14 files pass; `openspec validate --all --strict` (pin 1.10.0) 194 passed; `check_openspec_archive_discipline.py` flags the records writer change as archive-ready, archived in this delivery; `validate-public-artifacts.py --repository` clean, 4053 files; `generate-capabilities.py --check` current; `hosted-plugin.py check` current for the base and the v5 candidate.
- Post-deploy operational follow-up, not a task of this change: re-preserve the two files written under the flattened `Evidence/foodcaviarhouse-group-order/...` destination under `Evidence/food/caviarhouse-group-order/` through governed tooling and confirm the ledger records the repair. Tracked in the delivery runbook for the release that carries this change; the change itself is complete at merge.

## 7. Review corrections

Independent review returned REQUEST_CHANGES on 2026-09-10; every item below is
the orchestrator's decision from `correction-round-1.md`, not a suggestion.
Round 2 (2026-09-11) verified 19 of 20 and found one new major (7.21); round 3
(2026-09-11) verified 7.21 to 7.28 with probes and returned APPROVE, leaving
three nits accepted without change: the resolved-duplicate remediation prose
inherits the graph boundary (documented in design D4), `note_committed_targets`
takes its lock twice, and the whole-call refusal test asserts the Evidence
path negative but not the vault parent (covered by the segment-refusal test).

- [x] 7.1 (C1, blocker) An all-duplicate batch returns no mutation terminal.
  - Evidence: red first — `tests/test_client_artifacts.py::test_all_duplicate_batch_still_carries_the_mutation_terminal` and `::test_duplicate_preserve_evidence_still_carries_the_mutation_terminal`, both driven through `writer_lease.invoke_command` because every earlier duplicate test called the leaf directly and never saw the envelope. Red: `KeyError: 'ok'` in both lanes. Fix: a resolved duplicate calls `mark_active_mutation_committed()` — nothing is written, but the call has a terminal answer about canonical state, so it earns the same committed terminal a stored file gets. Both lanes (`client_artifacts.py` duplicate branch, `commands.op_preserve`). Green: 2 passed.
- [x] 7.2 (C2, major) A duplicate whose ref cannot be resolved was reported as a failure.
  - Evidence: fixed at the source — `preserve._duplicate_from_sidecar` returns `None` when `ref_from_markdown` yields nothing, so with no ref there is no duplicate and the file stores normally; `DuplicateArtifact.ref` is now `str`, not `str | None`, and the projection's `duplicate_of` validator can no longer send a live row to `invalid_row`. `tests/test_client_artifacts.py::test_a_duplicate_without_a_resolvable_ref_is_stored_not_failed` 1 passed.
- [x] 7.3 (C3, major) The shipped tool description still documented the old behaviour.
  - Evidence: `op_preserve_artifacts` and `op_preserve_evidence` docstrings now state the three terminal states, what `already_stored` means, the same-identity replay rule, and that `scope` and `category` are each one path segment. The fingerprint moved as predicted: `d1e78d2e…` -> `8e378e5d…`. Full 5.2 chain re-run — `package-skills`, `hosted-plugin.py render`, the v5 render and `check` ("Hosted generated artifacts are current"), `generate-capabilities.py` and `--check` ("docs/capabilities.md is current"), `scripts/dump-tool-schemas.py` for `tests/fixtures/mcp_tool_schemas.json` + `tool_surface_contract.json`, and `deploy/chatgpt/personal-plugin-contract.json`'s `pending_tool_surface_sha256`. Tree disagreement: the first dump truncated the `scope` arg help to "One path segment, not a" because `parse_args_help` reads a continuation line beginning `path:` as a new parameter; reworded to avoid the colon and re-dumped. Green: the six derived-artifact suites plus `test_workflow_skills` 107 passed.
- [x] 7.4 (C4) The acknowledgement budget skipped the whole acknowledgement after a slow leaf.
  - Evidence: `_check_acknowledgement_budget` and `_DerivedAcknowledgementBudgetExhausted` are gone; the budget now survives only as `deadline = min(fast-ack deadline, budget_deadline)`, which bounds the component *wait* and nothing else. Proof, publication and signalling always run. `tests/test_writer_lease.py::test_derived_acknowledgement_budget_bounds_the_waiting_only` asserts `proofs == ["prove"]` with every wait deadline already expired; 1 passed.
- [x] 7.5 (C5) The crash window persisted a terminal with no derived state at all.
  - Evidence: `run` gains `acknowledgement_expected`, bound at the composition site to `fast_ack_session is not None` (not to "a hook is bound", which is true for every non-reconcile command including those owing no derived work). When set, `_awaiting_derived_acknowledgement` writes `derived_sync: "pending"` before persistence, so the crash window and a failed `_advance_completed_terminal` both leave a row that says pending rather than nothing.
- [x] 7.6 (C6) The degraded terminal dropped the advisory fields.
  - Evidence: `_claimed_advisory_ref` resolves the retained claim's stable result ref and rides on `_PostCommitOutcomeUncertain.advisory_result_ref`; `_derived_acknowledgement_pending` carries `advisory_sync: "pending"` with that ref. When no ref resolves it says nothing rather than asserting `not_required` for a job that may well be required.
- [x] 7.7 (C7) Duplicate detection read every sidecar once per staged file.
  - Evidence: `preserve.destination_duplicate_index` builds the sha256 map once; `preserve_artifacts` calls it once per batch and inserts each file it stores, so intra-batch visibility is preserved. `tests/test_client_artifacts.py::test_the_destination_is_read_once_for_the_whole_batch` (a four-file batch scans once) and `::test_identical_files_inside_one_batch_are_one_fact` — 2 passed.
- [x] 7.8 (C8) The sidecar's `artifact_path` was trusted without containment.
  - Evidence: `_duplicate_from_sidecar` requires `os.path.realpath(vault_root / artifact_path)` to sit directly inside the destination directory. `tests/test_client_artifacts.py::test_a_sidecar_pointing_outside_its_destination_is_not_a_duplicate` 1 passed.
- [x] 7.9 (C9) Destination validation missed several hostile forms.
  - Evidence: `DESTINATION_SEGMENT_MAX_CHARS = 255` (chars and UTF-8 bytes), trailing dots, the Windows reserved device names with or without an extension, and every code point in Unicode general category `C*` (which covers `U+200B` and the bidi overrides) are refused with the same `field`/`reason`/`accepted_form` shape. `::test_hostile_destination_segments_are_refused` 6 cases passed. The 255 bound is above the 129-character segments an existing test uses, so it did not disturb them.
- [x] 7.10 (C10) Destination validation ran after the empty-files early return.
  - Evidence: `_validate_destination` moved above the `files=[]` shortcut. `::test_an_empty_batch_still_refuses_a_malformed_destination` 1 passed.
- [x] 7.11 (C11) The hosted enumeration scanned one module.
  - Evidence: `_status_bearing_mutation_codes` now walks every `*.py` under the package. Confirmed wider, not merely rewritten: the scan now returns `MAINTENANCE_REQUIRES_CLI` (declared outside the writer lease) alongside the five it already found, and still no false positives — the `codes <= shapes` assertion holds. 1 passed.
- [x] 7.12 (C12) `derived_sync_components` could under-report across batches.
  - Evidence: `owed` is a per-batch dict unioned by `still_owed()`; a superseded batch registers an empty set because the batch covering it owns its convergence.
- [x] 7.13 (C13) Test-coverage gaps in the D1 split.
  - Evidence: four new tests in `tests/test_writer_lease.py` — `::test_a_graph_rebuild_handoff_binds_the_pre_persistence_hook` (a non-reconcile leaf carrying a handoff acknowledges *before* persisting), `::test_an_ordinary_command_binds_the_post_persistence_hook` (the mirror), `::test_a_committed_failure_still_runs_the_derived_acknowledgement`, `::test_an_unkeyed_mutation_degrades_a_lost_acknowledgement` — plus `::test_identical_files_inside_one_batch_are_one_fact` for intra-batch duplicates. 5 passed.
- [x] 7.14 (C14) `_graph_sync_checkpoint` reached the persisted row for non-graph commands.
  - Evidence: `finish_before_terminal_persistence` strips it at the seam where the guard used to. The *canonical* row keeps it, which is what `resume_graph_sync` and the legacy migration read.
- [x] 7.15 (C15) The ledger target bridge had no TTL and evicted by insertion order.
  - Evidence: `_committed_targets` entries carry `at`, `_sweep_targets_locked` drops anything older than `_TARGETS_TTL_SECONDS = 300.0` on both note and pop, and over-capacity eviction now takes the oldest entry — the same two independent guards `call_spans` uses.
- [x] 7.16 (C16) One command, two response shapes.
  - Evidence: `op_preserve`'s duplicate payload now carries `outcome`, `size`, `hash`, `hash_algorithm`, `media_id` and `content_type` alongside the fields it already had, matching `PreserveResult.as_dict`.
- [x] 7.17 (C17) Stale test docstring.
  - Evidence: `tests/test_fast_write_ack.py::test_real_custody_failure_degrades_the_persisted_terminal_to_pending` now says it names the derived components, which is what the body asserts.
- [x] 7.18 (C18) Confirm, do not change: `_target_paths` de-duplicates argument-derived paths.
  - Evidence: confirmed and kept. No test asserts a repeated entry — the two note-write assertions in `tests/test_call_ledger.py` are single-element lists and there is no same-value `old_path`/`new_path` case — and neither the canonical `openspec/specs/call-ledger/spec.md` nor this change's delta says anything about multiplicity; both speak of "the addressed note path" recorded structurally.
- [x] 7.19 (C19) The delivery reserve is advisory, not enforced. — Written into design.md D1 by the orchestrator; not touched here.
- [x] 7.20 (C20) The duplicate lookup runs outside the mutation boundary.
  - Evidence: took the **documentation** option. Moving the lookup inside the per-file boundary would either restore the O(files x sidecars) rescan that C7 exists to remove, or hold one boundary across the whole batch, which the narrow-boundary design exists to prevent. The race is also today's behaviour without dedup, so it is a narrowing that regressed nothing. Stated in design.md D4 as a "Known residue" paragraph naming the two rejected shapes and that a destination-scoped lock is a different change.
- [x] 7.21 (C21, major) `acknowledgement_expected` marked commits owing no derived work as permanently pending.
  - Evidence: red first — `tests/test_writer_lease.py::test_a_commit_that_registers_no_derived_batch_owes_no_pending` runs a leaf that marks the mutation committed without registering a batch, with `EXOMEM_FAST_DURABLE_ACK=1`. Red (with the round-1 predicate restored): `AssertionError: assert 'derived_sync' not in {'derived_sync': 'pending', ...}`, and the replay repeats it. Fix: `acknowledgement_expected` is now a callable in the same shape as `commit_observed`, read after the leaf returned, bound to `fast_ack_session is not None and bool(fast_ack_session.batches)` — the session is built before the leaf from a feature flag and a vault root, so its existence never said anything about what would be registered. Green: 1 passed, and the replay carries no `derived_sync` either.
- [x] 7.22 (C22, minor) A duplicate call claims queued graph repair for pages it did not change.
  - Evidence: took the **documentation** option. Suppressing the fields needs a new "this call changed no canonical path" signal threaded from the leaf through the composition site into the graph wait, whose `registered_checkpoint` is vault-wide and carries no per-call attribution — the restructure the packet said not to make. Stated in design.md D4 as a second "Known residue" paragraph, which also records what the packet did not: for a *mixed* batch, one that stored at least one file, the graph fields are correct outright, so only the all-duplicate case is misattributed.
- [x] 7.23 (C23, minor) The response-shape asymmetry flipped instead of closing.
  - Evidence: `PreserveResult.as_dict` now carries `"outcome": "stored"` beside `"state"`, so `outcome` mirrors `state` on both branches for its one release.
- [x] 7.24 (C24, minor) One file, two content types.
  - Evidence: the duplicate branch derives its content type from `preserve_module._sanitize_filename(filename)`, the same input `preserve()` uses on the stored branch.
- [x] 7.25 (C25, minor) The component-owed accounting disagreed with itself.
  - Evidence: `still_owed()` returns `None` when no batch was examined and a tuple otherwise; `_PostCommitOutcomeUncertain.derived_components` is `tuple[str, ...] | None`; `_derived_acknowledgement_pending` falls back to the full set only for `None`. `None` and `()` are now distinct answers — nothing known versus examined and owing nothing — and both functions read them the same way.
- [x] 7.26 (C26, nit) `note_committed_targets` returned before sweeping when it notes nothing.
  - Evidence: the TTL sweep runs before the empty-paths return.
- [x] 7.27 (C27, nit) Two small cleanups.
  - Evidence: the destination length check compares UTF-8 byte length only, since a byte length can only exceed a character length under UTF-8; and `test_a_duplicate_without_a_resolvable_ref_is_stored_not_failed` lost both the vestigial `memory_refs` import and the assertion that existed only to justify it.
- [x] 7.28 (round 3) Two `tests/artifact_adoption/` tests asserted shapes this change repeals.
  - Evidence: verified both readings against the shipped specs before editing, and agree. `::test_ordinary_calls_render_no_adoption_block_and_keep_existing_response_shape` asserted a two-key summary; the `client-artifact-preservation` delta says the summary counts all three states, so it now asserts `{"stored": 1, "already_stored": 0, "failed": 0}`. `::test_invalid_evidence_destination_only_fails_the_selected_handle` asserted per-handle destination failure with `scope=".."`; nothing in `openspec/specs/client-artifact-preservation/spec.md` or `openspec/specs/adoption-studio/spec.md` requires that or mentions an `unselected` outcome, while the delta's ADDED requirement refuses "before any file handle is fetched or staged" — so it encoded an implementation choice, not a contract. Renamed to `::test_invalid_evidence_destination_refuses_the_whole_call` and rewritten to assert the `INVALID_PRESERVE` refusal with `details.field`, that the fetcher is never called, and that no Evidence path is created. `tests/artifact_adoption/` added to the 6.1 gate list. Green: `tests/artifact_adoption/` 98 passed.
