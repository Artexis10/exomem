## 1. Amendment and governance

- [x] 1.1 Author the §7 sequence-3 entry (dated, reasoned): f27 `lifecycle_routing_replay`, kind operational, no public coverage, the two assertions, the tiers, the harness-fault rule, the store-bearing gate, the paired-readout rule, the expected-partial declaration, no catastrophic additions, no budget constants. Add the §1 row, the §2 assertion names, and the §4 representation-neutral predicates.
- [x] 1.2 Mint `benchmarks/epistemic/contracts/amendment-2026-08-lifecycle-replay.v1.json` (sequence 3, pending: `parent_contract_sha256` = the sequence-2 `contract_sha256`, `contract_sha256` = the amended document) and prove `validate_working_preregistration` folds the chain.
- [x] 1.3 Mirror in the registry: `PREREGISTERED_FAMILIES` gains `("f27", "lifecycle_routing_replay")`, the assertion-name tuple gains the two names, `AMENDMENT_INTRODUCED_FAMILIES` gains `f27 → 3`; `tests/test_epistemic_registry.py` drift tests green. Red fixture `red-sequence3-withheld-family.yaml` refuses at load with the typed error naming sequence 3 and f27.

## 2. Corpus and gate

- [x] 2.1 `benchmarks/epistemic/corpora/lifecycle_replay.py`: seeded vault (slice-1 manifests: Planning keyed `[title]`, Records keyed `[occurred_on, title, event_type]` joined on `title`, the outcome/initiative chain, no items), the transcript with per-turn tier annotations (six deliverables; at least one tentative claim, one elapsed-time remark, one deferral annotated `none`; no two expected events sharing `(title, event_type)`), the folded expected end-state, and `corpus_digest()`. Red-first: the fold test and the no-leak test go red before the module exists.
- [x] 2.2 `STORE_BEARING_RE` and `assert_no_store_bearing_utterance`, run at corpus construction and at scenario load. Red fixture `red-sequence3-store-bearing-utterance.yaml` ("save this one") refuses naming the turn and the token; the ordinary-language turns are admitted. Cite the retrieve-nudge regex as the sibling and state why it is not reused.
- [x] 2.3 `benchmarks/epistemic/fixtures/sequence3/f27-lifecycle-routing-replay.yaml`: two phases (`hookless`, `hooked`), each `configure` → one `agent_turn` per corpus turn → `snapshot`, both assertions expected per phase, fairness block in the f26 shape.

## 3. Assertions

- [x] 3.1 `lifecycle_consequence_landed_unprompted` in `assertions.py`: the collections gate (`blocked` on empty / missing profile), per-tier counting with the pinned normalisation, pass only when every tier is complete, fractions and missing keys in the detail. Red-first, then mechanism removal: drop the gate → blocked case passes; drop the normalisation → case-variant title misses; drop the tier loop → partial landing passes.
- [x] 3.2 `no_structured_write_beyond_expectation`: extras over plan items, records, collections and pages against the allowlist; pass only on an empty extras set. Red-first, then mechanism removal: drop the page check → a stray note passes; drop the status check → a wrong status passes; drop the collection check → a created collection passes.
- [x] 3.3 `tests/test_epistemic_no_nudge_families.py` (or a sibling module): both assertions evaluated through `evaluate_scenario` on a hand-built snapshot for the green, the partial and the spurious cases; the `blocked` branch for the unprojected section.

## 4. Journey driver

- [x] 4.1 `benchmarks/epistemic/journeys/f27_replay.py`: envelope discovery (`claude --version`, refuse when absent), the environment floor (strip `CLAUDECODE` / `CLAUDE_CODE_*` / `CLAUDE_PID`), `build_turn_argv` (`--setting-sources project`, benchmark project cwd, `--strict-mcp-config --mcp-config` via track C's `write_mcp_config`, `--output-format stream-json --verbose --include-hook-events`, `--allowedTools` for the exomem server, `--max-turns`, `--model`, `--session-id` then `--resume`), arm configuration (hookless: no plugin, `--tools ""`, `--append-system-prompt-file` = the `maximal` block from `docs/prominence.md` cited by line, prominence `maximal` written through `python -m exomem prominence` under the arm env; hooked: `--plugin-dir plugins/claude-code`, `--tools Skill`, `EXOMEM_HOOK_HOME` under the workdir, prominence `balanced`), injectable runner, per-turn transcript capture via `parse_stream_json_transcript` plus hook-event counting, vault projection through `VaultProjector`, persistence through the evidence module, `report.json` and `manifest.json` in the D7 shape.
- [x] 4.2 Harness-fault semantics: non-zero exit, error subtype, `is_error`, "Not logged in", malformed line → arm blocked with reason, no snapshot, both assertions `blocked`. Red-first with an injected failing runner.
- [x] 4.3 `python -m epistemic.journeys.f27_replay --arm {hookless,hooked,both} --out <dir> [--model M] [--dry-run]`; dry-run prints argv and the environment delta per turn and runs nothing. Test the dry-run output against the installed CLI's declared options (`required_options` pattern from f26).
- [x] 4.4 Tests with recorded transcripts: a fabricated-but-well-formed stream for each arm drives the full path offline (parse → project → evaluate → report), so the harness is proven before any live run.

## 5. Verification

- [x] 5.1 `tests/test_epistemic_*` green; `tests/test_epistemic_amendment_governance.py`, `tests/test_epistemic_contract_receipts.py` and `tests/test_epistemic_registry.py` green on the sequence-3 chain; `tests/test_scaffold_no_leak.py` green over the corpus.
- [x] 5.2 `uvx ruff check . --select F` clean; `openspec validate --all --strict` green; `scripts/check_openspec_archive_discipline.py` ok.
- [x] 5.3 Dry-run output pasted: both arms, every turn's argv, the stripped variables.
- [x] 5.4 Development run (orchestrator-run on the subscription login, recorded here as the finding, not a claim): per arm, per tier coverage beside extras, nudge count, write tool-use count, model and CLI pins. Three runs were made on 2026-08-23, all `--arm both --model sonnet`, out-of-tree `--out`, pins: claude `2.1.240`, model `sonnet` (served as `claude-sonnet-5`), exomem `0.58.0`, corpus digest `7c26fdfc…`, fixture digest `c52ceb68…`, sequence 3 unacknowledged (`claim_status` in the manifest says so). Run A is the one on the shipped driver; B and C are kept beside it because they differ from it in ways the family should know about.
  - **Run A** (shipped driver), 2026-08-23T11:08:23Z, `--max-turns 40`. Both arms completed all ten turns with no harness fault; 23 seeded pages per arm; `seed_warnings` one routine line per arm (`ERROR exomem.graph_sync: graph rebuild stopped checkpoint_sha256=… generation=1`).
    - hookless (prominence `maximal`, instructions block `docs/prominence.md:82-88`): coverage intent 0/4 · outcome 0/5 · transition 0/3; extras 8 (eight prose pages `Knowledge Base/Notes/Insights/this-week-s-production-tasks-current-run{,-2..-8}.md` — one `remember` on turn 2, then a superseding `replace_memory` on turns 3, 4, 5, 7, 8, 9, 10); structured write tool-uses `record_memory` 0 / `plan_memory` 0; tool uses `bootstrap` 1, `ask_memory` 5, `remember` 1, `read_memory` 2, `edit_memory` 11 (all on turn 3, all refused: `SEMANTIC_CONTRACT_BLOCKED`, then `INVALID_EDIT: conflicting values for validate_only` on every retry), `replace_memory` 7; hook invocations 0; capture-nudge firings 0. The elapsed-time distractor (turn 6) produced no write.
    - hooked (prominence `balanced`, plugin `exomem@inline 0.58.0`): coverage intent 4/4 · outcome 1/5 · transition 0/3; extras 4 (records `batch layout draft (finished)`, `colour proof sheet (client_signed_off)`, `colour proof sheet (sent_to_printer)`, `packing insert (finished)`); structured write tool-uses `record_memory` 5 / `plan_memory` 28; tool uses `ask_memory` 3, `plan_memory` 33, `schema_memory` 1, `read_memory` 3, `record_memory` 7; hook invocations 30 (UserPromptSubmit and Stop on every turn); capture-nudge firings 1 (turn 1). Turn 1: `plan_memory(inspect, collection=<title>)` → `INVALID_COLLECTION_PATH`, three `INVALID_PLAN` refusals on `kind` before the agent chose `outcome`, two `INVALID_PLAN_RELATION`, one `schema_memory` `INVALID_CONTRACT`; then all four deliverables were added as plan items on turns 1–2 (kind `outcome`, status `planned`). Turn 3: eighteen `plan_memory(update)` shapes and one `triage`, all refused `INVALID_PLAN_ARGUMENTS: arguments do not match the selected Planning action` — the closest shape carried the complete required set plus a surplus `item` key, and the remediation names neither the missing nor the surplus argument (`src/exomem/plan_memory.py:39-49`, `:68-70`); the agent said on turns 3 and 7 that it could not flip the status, and every status is still `planned` at the end. The five events went to Records in the agent's own vocabulary; turns 4 (tentative), 6 (elapsed time) and 9 (deferral) made no write.
  - **Run B** (round-1 driver, before the review corrections), 2026-08-23T10:01:05Z, `--max-turns 40`: both arms complete, no fault. hookless: 0/4 · 0/5 · 0/3, extras 8 (the same prose-note supersession shape as A: `remember` once, `replace_memory` ×7), `edit_memory` 9 refused on turn 3, hooks 0, nudges 0. hooked: 0/4 · 0/5 · 0/3, extras 0, `ask_memory` 3 on turn 1 and no other tool call in ten turns; hook invocations 30; capture-nudge firings 1 (turn 1, the only turn with tool activity). The agent tracked the state in its replies and said each turn that it had not logged anything and would on request.
  - **Run C** (`--max-turns 12`, 2026-08-23T09:55:54Z) faulted both arms at the cap (hookless on turn 3 after 13 agent turns, hooked on turn 1 after 13) and is kept for the product friction it surfaced: `plan_memory(action="inspect", collection="<title>")` → `INVALID_COLLECTION_PATH` (the tool accepts the manifest path only), `plan_memory(action="add", kind="task"|"action")` → `INVALID_PLAN: kind is invalid` with a remediation that does not name the profile's kinds (`src/exomem/planning.py:18`: `area`, `outcome`, `initiative`, `work-item`; `inspect` does not publish them either), and `bootstrap(profile="full")` returned 91,075 characters and overflowed the client's tool-result limit.
  - Reading. The hookless arm is stable in shape across A and B: the maximal block routes every consequence into one prose Insight note and supersedes it each turn (the false-write dual fails with 8 extras; coverage 0 on every tier). The hooked arm differs between A and B on identical pins: in B it routed nothing and waited to be told; in A it routed every intent to Planning and every outcome to Records. That variance is the agent's, not the harness's, and it is why the family forms no aggregate and why one development run is a finding and not a claim. Two findings about the family itself, for the founder rather than this change: (1) the outcome tier keys records on `(title, event_type)`, but the seeded manifest declares `event_type` as a free required string, so the corpus vocabulary (`produced`, `approved`, `delivered`, `rejected`) is not discoverable by the agent — A's four extras are the same four events in the agent's words, and its one hit (`rejected`) echoed the utterance; whether the manifest should publish the vocabulary or the tier should key on title alone is a sequence-4 decision; (2) the transition tier cannot land while `plan_memory(update)` refuses without naming the argument at fault — that is a product defect on the exact path the tier measures. The capture nudge keys on tool activity, so a turn that is pure conversation with a durable state change never fires it (B, turns 2–10). All readouts are per arm, per tier, paired with their extras.
- [x] 5.5 `benchmarks/README.md` names f27 and the sequence-3 receipt; the f26 docstring cross-references f27 as the agent-driven sibling.
