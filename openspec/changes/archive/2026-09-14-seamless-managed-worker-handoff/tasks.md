## 1. Phase 1: stop the post-handoff rebuild loop

- [x] 1.1 Turn the reproduction into failing regression tests: a running watcher with one unattributed edit before each of ten governed writes, asserting that no write is fenced into a whole-vault rebuild, that acknowledgement stays within a stated incremental bound, and that the graph repair queue drains to zero; a second, watcher-driven test with no competing writer asserts the rebuild count itself is zero; record the failing output before implementation.
- [x] 1.2 Apply the external-pending fence only to reads that require a current projection; route an unreadable predecessor to queued incremental repair with a distinct outcome; record external marks per path; pass 1.1 and the existing graph, freshness, watcher and alarm suites.
- [x] 1.3 Add failing tests for a bounded standalone join (request-deadline-derived and default budgets, pending outcome past the budget) and for rebuild-demand coalescing (ten writes during one flight yield at most one follow-up rebuild); implement both and pass the graph locking, drain and idempotency suites.
- [x] 1.4 Add a test that performs a real worker replacement across two interpreters and asserts the first ten writes after it are incremental, with at most the one whole-vault pass a fresh process's snapshot adoption costs and none after it; pass the managed-service e2e, ingress and service-manager suites. (Delivered in two parts: the two-interpreter replacement over the same vault and state root in `tests/test_graph_post_handoff_writes.py`, and the promotion arm under 2.1 and 2.8 in `tests/test_standby_handoff_writes.py`, whose `seed` variant performs the watcher's boot seed between promotion and the first write and asserts ten incremental writes with zero whole-vault registrations. The live confirmation is the 0.85.1 deployment recorded under 2.7.)
- [x] 1.5 Update `docs/` for the changed outcomes and the doctor's guidance; regenerate any derived artifact the outcome vocabulary touches; run the scoped suites, lint, strict OpenSpec validation and the privacy gate.
- [x] 1.6 Obtain an author-independent review of the phase-1 diff covering the fence narrowing, the predecessor-outcome split, path-scoped marks, the join budget and coalescing; resolve findings and recheck; open a ready pull request with the evidence and merge under the standing authority.
- [x] 1.7 Release, deploy through the managed upgrade, and verify on the personal service that writes after the handoff acknowledge within the bound with no rebuild sidecar chain and a draining repair queue; record the numbers. (Deployed as 0.83.1 and 0.84.1, both cold sequences: 0.83.1 handoff wall 19.9 s with one 19.5 s client request, the post-restart rebuild storm gone -- two bounded rebuilds, then incremental, converged in 8 min with the repair queue at 0; 0.84.1 19.4 s / 18.9 s with one rebuild, through admission-before-adoption rather than the dispatch defect, and writes of 212/80/78 s attributed to embeddings and post-persist work rather than graph. Both rounds' residues are tasks 1.14-1.16.)

- [x] 1.8 Hoist snapshot adoption and the recall resolver primer out of the cache-preload gate onto the unconditional start-up path, keeping the admitted-catalogue ordering adoption needs; test that a warm-up with CPU cache preloading disabled still adopts, records the residue and logs the adoption line, and that the gate still skips the disposable caches.
- [x] 1.9 Give `resolver_snapshot_unavailable` queue coverage: a defer disposition that queues the proven-complete recall delta and a dispatch outcome of its own, `graph_repair_cold_resolver`, claiming durable per-path coverage alongside the other two; audit the remaining rebuild-disposition reasons and record which are unreadable stored state that must stay whole-vault; test that a cold resolver at dispatch time yields queued incremental repair with zero whole-vault rebuilds and a draining queue.
- [x] 1.10 Bound the watcher's seed-time graph validation against a busy mutation boundary: re-prove the cheap admission while a governed mutation is in flight, and take the read barrier through the same bounded retry the availability withdrawal uses; test that a validation meeting a held boundary succeeds after release instead of aborting startup.
- [x] 1.11 Split the write path's fan-out into per-stage spans (per derived component, embedding model load vs encode vs matrix load vs catch-up, vocabulary delivery) and add a span from the canonical commit to the terminal state; give the lexical deferral a completion line; document the span vocabulary in `docs/observability.md` and pin it both ways.

- [x] 1.12 Make a governed write whose refreshed paths are under a path-scoped external-pending mark queue exactly those paths as graph repair receipts and report a dispatch outcome of its own, `graph_repair_external_pending`, claiming durable per-path coverage; do not treat the watcher's own repair of the mark as that coverage; test the probe shape (live watcher, an unattributed edit to each write's own path before it dispatches) for zero deferrals without queue coverage, queued coverage on every fenced write, a draining queue and acknowledgements within the incremental bound; audit the remaining deferral sources that reach dispatch without coverage and record their dispositions.

- [x] 1.13 Close the self-inflicted predecessor gap a deferral leaves: make the predecessor probe treat a lineage gap fully covered by durable graph receipts as covered rather than as a proven divergence, instead of advancing an acknowledgement nothing projected. Prerequisite: receipts must carry the generation they were queued for, or the probe must count only receipts queued at or after the acknowledged generation, which also closes the staleness hole in `index_sync`'s `graph_rels <= graph_receipt_rels` blessing. Test the batch-ingest rate at which the gap currently returns per write, and that it does not after.

- [x] 1.14 Order adoption ahead of write admission, and gate admission on it. Run `warm_graph_handoff` immediately after the retrieval catalog and before the semantic corpus build, on the unconditional path -- no resource mode, corpus build or `catalog_ready` gate may skip it; make the mutation admission gate that returns `MUTATION_WARMING` wait for a `graph_handoff` readiness component as well as `semantic_corpus`, with the same retryable refusal, `retry_after_ms` and `committed: false`, and reads unaffected; mark the component settled on every exit so a skipped or declined adoption cannot hold writes. Give each cold-proof decline a name of its own and carry it on the warm-up's adoption line, and name the generations an uncovered lineage gap could not prove. Test red-first: a replacement process whose write arrives during warm-up is refused until adoption has run and is then incremental; the warm order pinned; the decline reasons enumerated from the source. Fold in the two 1.13 review findings: enumerate the generation-recording enqueue sites so a partial-set site must declare itself, and make the relaxed per-write rebuild oracle prove from `graph_upserts` that an admitted rebuild's missing generation really had no receipt instead of allowlisting the reason.
- [x] 1.16 Make an adopted recall origin reachable from the delta history. `adopt_recall_origin` names a local generation as a foreign checkpoint's origin but seeds no history entry there, and a promoted worker's watcher re-seeds the registry -- which resets the retained history to empty at a new generation while the adopted origin, held at the older one by the deliberate `min`, survives it. Seed a zero-path history entry at the adopted origin so the bridge exists, without widening `recall_delta_incomplete`, which must keep answering genuine `DELTA_HISTORY_LIMIT` overflow with a rebuild. Test red-first: a replacement process whose first write after adoption yields a complete delta and zero whole-vault registrations, a delta that still reports exactly the paths changed after the re-seed, the guard still refusing on real overflow, and a re-adoption that cannot raise the floor out from under an older origin.
- [x] 1.15 Attribute the write fan-out, instrumentation only and no behaviour change. Emit the collected `MutationTimings` stages into the ledger row unconditionally, leaving `EXOMEM_WRITE_TIMINGS` to govern only the caller's response envelope; time the write advisory's cosine sweep and its grouping/emission on both surfaces, carrying what was encoded; split `derived.canonical_to_committed` into `derived.fanout` and `derived.terminal_persist`; give every step inside the fan-out that had no span one of its own, and give the per-item component spans the count of paths they covered; give `embeddings.encode` a text and character count. Let a span carry bounded named integer counts beside its duration, omitted when empty so no row already written changes shape. Test red-first: a governed edit records the advisory sweep with a text count, the write stages reach a row with the envelope flag unset, and the window's leaf spans account for it. Document every new span name in `docs/observability.md`, with the both-ways pin, and note there that `mutation_lock` events dominate the log under writes and how to raise the rotation or lower that logger without changing the default.

## 2. Phase 2: standby warm-up and promotion

- [x] 2.1 Add failing tests for standby readiness (`cutover` component set; no lease, publication or scheduler while standby), the separate warm budget with discard-and-record on expiry, and promotion order including the declared-migration skip and the re-proof after a migration; implement in the supervisor, worker runtime, readiness and warm-up modules. A worker child must also be spawned with the unit's environment file as it stands at spawn time, so an edited service environment reaches the next worker without a supervisor restart. Include a supervisor-driven handoff assertion: the first ten governed writes after a real promotion are incremental and schedule no whole-vault rebuild, which the standby's snapshot adoption is what makes possible.
- [x] 2.2 Add failing tests for stream reattachment retrying through promotion within the cutover budget; implement in the ingress.
- [x] 2.3 Add failing tests for background vocabulary recovery draining after activation once the projection is current; implement in the activation sequence.
- [x] 2.4 Re-measure recall during a single rebuild after phase 1 on a production-sized fixture; if the degradation still exceeds the stated bound, implement the child-process rebuild with reduced priority behind the existing single-flight owner and its in-process fallback reporting; otherwise record the measurement and close this task as not needed. (Measured 2026-09-14 on a 4,000-note fixture, recall during a single whole-vault rebuild: p50 7.9 -> 18.7 ms and 8.2 -> 20.7 ms, 2.4x to 2.5x, p95 up to 4.6x, all inside the 300 ms p50 ceiling; the 2.3 s query is a synthetic-vault artifact outside the ceiling even when quiet; the ratio grows with corpus size. The bound that governs is the recall ceiling, and recall stays inside it, so this closes as not needed. D10 leaves this change for a named follow-up in design.md's Open questions, with this measurement as its entry condition.)
- [x] 2.5 Update the release operator scripts and `docs/` for the standby sequence and budgets; enable model preload in the personal service environment as a recorded operator step.
- [x] 2.6 Obtain an author-independent review of the phase-2 diff covering ownership during standby, promotion ordering, budget expiry and stream retry; resolve findings and recheck; open a ready pull request and merge under the standing authority.
- [x] 2.7 Release and deploy; verify on the personal service that the upgrade after this one keeps the endpoint usable throughout, with the measured unavailable window, cutover component timings and first-write latency recorded; then synchronize and archive this change. (First standby-sequence upgrade 0.84.1 -> 0.85.0 under the 0.85.0 supervisor, 2026-09-14: standby warm 10:21:36-10:22:33, adoption `adopted=True residue=0` at +37 s, bge preload at +43 s; promotion 46.8 ms, `snapshot=current` re-validated by checkpoint comparison (no migration ran, so no full source proof was owed), `residue_applied=0`; migration skipped `declared_none`; supervisor `unavailable_ms=1672.9` and one 1.635 s request seen by an observed client over 245 samples on an unbroken TCP connection, against 19.4 s and 18.9 s for the two cold sequences before it. The first write after promotion was refused ~30 s and then took 124 s with one rebuild -- tasks 2.8 and 1.16 -- and writes 2 and 3 were incremental at 61 s and 51 s. Ten-minute observation: one transient rebuild sidecar, interval CPU 120-199%, `graph_upserts` draining to 0 and `vocabulary_recovery_jobs` 39 -> 30, so D12 is draining. Prerequisite recorded in design.md: the supervisor runs from the launcher venv, so phase 2 only takes effect after the documented launcher maintenance; an ordinary worker upgrade never touches it.)
- [x] 2.8 Stop a promoted standby from repeating its own warm. The standby's warm also builds the semantic corpus -- read-only, published nowhere -- and reports it as a cutover component, because the write admission gate waits on it and a standby that skips it cannot hand over a worker that admits writes. At promotion, record which readiness components the standby completed and promotion either re-verified or cannot invalidate, and have the promoted worker's own `warm_all` subtract exactly those, re-marking them synchronously before the warm thread starts because `begin_warm` clears every event. `graph_handoff` is carried only on a `current` snapshot verdict. Report the carried set on the promotion record, the readiness payload and the warm-complete line. Test red-first: a promoted warm that repeats neither the adoption nor the corpus build, a partial carry that still warms what it did not name, the carried components ready before the warm thread starts, and a cold start that carries nothing and runs every step unchanged. (Measured on the handoff harness (200-note fixture, fresh interpreter, promotion to the moment the admission gate's own predicate clears for both components): 545.9 ms without the carry, 0.4 ms with it -- the 0.4 ms being the synchronous re-mark, with no wait left at all. The live 4,271-page vault's equivalent was about thirty seconds.)
