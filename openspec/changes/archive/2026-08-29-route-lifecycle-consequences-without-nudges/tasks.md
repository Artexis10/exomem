# Tasks — route lifecycle consequences without nudges

Every test lands red first (verbatim failing output recorded before the
implementation, then green). Measured budgets are written into this file when the
task is ticked, not estimated.

## 1. Contract — capture classes (design D1, D2, D8)

- [x] 1.1 `src/exomem/prominence.py`: add the two lifecycle capture classes and the pairing rule to the `balanced` and `maximal` capture axis; `light` and `off` unchanged. Red-first test in `tests/test_prominence.py` (or the existing contract test) asserting both levels name intent→Planning, outcome→Records and the pairing rule, and that `light` does not.
- [x] 1.2 `src/exomem/_scaffold/_Schema/SKILL.md` and the plugin copy (md5-identical): stepping-stone block gains the two classes; the Planning section's "never infer it from prose" becomes "never infer it from elapsed time — a stated outcome is evidence, the clock is not"; the implicit-proactive block lists `record_memory` and `plan_memory` beside `ask_memory`, `capture_source`, `remember`; one paired worked example in domain language. `tests/test_scaffold_no_leak.py` and the skill-sync test stay green.
- [x] 1.3 `commands.op_bootstrap`: `intent_boundary` gains the two classes and the pairing rule; `capture_examples` gains one paired example; `_SIMPLE_ACTIONS` gains `plan`. Extend `tests/test_epistemic_bootstrap_contract.py` (intent-boundary pin) and the simple-action pin red-first. Measure the compact payload before and after and record both sizes here; raise `COMPACT_BYTE_CEILING` by the minimum needed, not past 61,400. Measured: before 60,066 B, after 61,306 B, ceiling 61,400. Re-measured on the merged tree (round 2): `origin/main`'s entity-types guidance took the payload to 61,455, 55 over the ceiling, so this slice's own text was trimmed rather than the ceiling raised — 61,455 → **61,297**, 158 bytes out, 103 under the unchanged 61,400. Every rule survives with its pins tightened onto the new text; the attribution is in the `COMPACT_BYTE_CEILING` docstring.
- [x] 1.4 Workflow skills `continue` and `capture`: one lifecycle paragraph each (session start checks the Planning inventory and the due-state block; capture routes intent and outcomes). Hosted skill renders and generated locks regenerated by the existing scripts.
- [x] 1.5 `_hooks/exomem_capture_nudge.py`: `_KB_WRITE` counts `record_memory`, `plan_memory`, `observe_memory`; the reminder names the lifecycle classes in one line. Red-first test: a transcript whose only KB write is a `record_memory` append is not nudged; the reminder text contains the lifecycle line.

## 2. Substrate — identity (design D3)

- [x] 2.1 `records.append_record`: derive the item key from the declared natural key when `item_key` is omitted and every natural-key field is present, via `natural_key_serialization` + `inferred_item_key`; `uuid4` otherwise; explicit key still wins. Red-first tests in `tests/test_record_mutation.py`: omitted key → derived key equals the read-path inferred key; identical re-append → `replayed`; same natural key, different payload → `RECORD_ID_CONFLICT`; missing natural-key field → random key as before.
- [x] 2.2 `RECORD_NATURAL_KEY_CONFLICT`: an append whose serialised natural key equals an existing item's under a different key refuses naming the existing item(s); two pre-existing duplicates are both named. Red-first test with a `uuid4`-keyed seeded item. Error code added to the allowlisted codes wherever codes are enumerated (`review_state.STORE_ERROR_CODES` pattern, docs).
- [x] 2.3 Planning inherits: `plan_memory(action="add")` without `plan_id` on a `[title]`-keyed collection derives the key; adding the same title twice with different fields refuses. Red-first test in the Planning suite.

## 3. Substrate — inventory and binding (design D4, D5)

- [x] 3.1 `plan_memory(action="inspect")` with `collection` omitted returns the Planning inventory in the Records inventory shape, disclosure-filtered; with a collection unchanged. Validation matrix updated (`collection` optional for `inspect` only). Red-first tests: inventory lists exactly the planning-profile manifests; a withheld manifest is absent; every other action still requires `collection`.
- [x] 3.2 `structured_collections.PlanLink` gains optional `join`; manifest validation accepts 1–4 pairs, record-side names must be declared fields, plan-side names bounded non-empty text; `record_governance._project_plan_link` round-trips it; `describe` documents it. Red-first tests: valid join round-trips unchanged through inspect; unknown record field refuses; five pairs refuse; Records inspect performs no Planning lookup (assert no resolver call).

## 4. Substrate — the family and the carriers (design D6, D7)

- [x] 4.1 `audit.py`: `_check_unreflected_outcomes` over join-bearing Records manifests → resolved Planning manifests → open items (lifecycle `active`, status ∉ {completed, cancelled}) with ≥ 1 joined record; finding fields per design D6; `signal_version` from authored state; registered in `ALL_CATEGORIES`. Red-first tests: fires on the touched items only; a twin Records collection without `join` produces nothing; a completed item produces nothing; an unresolvable reference produces nothing and is reported as such in the audit's own unevaluated set, never silently.
- [x] 4.2 Fingerprint: item reference + sorted joined record keys; a new joined record changes it and a dismissed item resurfaces; removing the record restores the old fingerprint and the dismissal holds. Red-first tests through `review_state`.
- [x] 4.3 `attention.DEFAULT_ATTENTION_CATEGORIES` gains the family; `registered_families()` therefore includes it (assert, no restatement); `triage_memory(ref="exomem://review/family/unreflected_outcomes", action=quiet)` works through the existing surface. Red-first test.
- [x] 4.4 `due_state.PROJECTION_CATEGORIES` and `DELTA_CATEGORIES` gain the family; `apply_write_delta` handles the two structured write shapes with the bounded counterpart read; `reconcile` recomputes. Red-first tests: the delta after a record append equals a full recompute for the touched pair; the delta after a triage to `completed` clears it; reconcile after an out-of-band manifest edit heals.
- [x] 4.5 Carriers: `record_memory` append/update and `plan_memory` add/update/triage responses carry the due-state block via the shared helpers under emission governance. Red-first tests: the append that opens a gap carries a block naming the family with count 1; twelve appends inside one `batch_scope` deliver once; `quiet` disposition removes it from the block; an unreadable review state yields no block and the write still commits.
- [x] 4.6 Disclosure: a withheld record or plan item contributes nothing to the finding, the count or the reference list; the served view equals the vault with it absent. Red-first test through the release plane fixture used by the other categories.
- [x] 4.7 Cost: measure the write-time delta for a record append against a bound Planning collection of 100 items and a plan triage against a bound Records collection of 1,000 records on the development box; record the numbers here and keep both under 50 ms. Round 0 measured append 157 ms / triage 334 ms — both over. After the round-1 restructure (record side: one unfiltered Planning snapshot, no Records re-read, no release-plane call; plan side: no read unless `add` or a join-side move; a persisted `bindings` index instead of vault discovery; one policy load per release filter), re-measured on the merged round-2 tree with a 25-sample settle: **append median 42.8 ms** (samples 43.1, 37.1, 37.8, 42.8, 44.3) and **triage median 2.3 ms** (samples 2.1, 2.2, 2.3, 2.4, 2.8), each the median of the five samples after the settle. Round 1's triage figure of 3.5 ms was a transient: settling only five writes leaves the tail of `reconcile`'s background graph rebuild inside the kept samples, which is how a recheck measured the same code at 16.4 ms. The settle is now 25 writes on both shapes and lives in the script; its discarded triage samples decay 19.4 → 2.0 in the open, so the settling is visible rather than asserted. Of the append, 23.1 ms is the parse of the one bound Planning snapshot. An unbound append costs 5.6 ms all in — 2.2 ms of projection load and binding lookup plus the fsynced `writes` counter that m3 requires — with binding discovery itself unmeasurable at 0.00 ms. The audit pass over 1,000 records went 637.4 ms → 593.3 ms. Method, full samples and the settling protocol are in `.task/evidence/round2/4.7-measure_delta.py`, its output in `.task/evidence/round2/4.7-measured.txt`, and `.task/RESULT.md`.
- [x] 4.8 `docs/epistemic-inbox.md` and the Records documentation: the family, the binding, what resolves it, what never happens (no runtime transition).

## 5. Acceptance (design D10)

- [x] 5.1 `benchmarks/epistemic/projectors/exomem_vault.py`: `VaultProjector` emits a `collections` section (profile, manifest reference, items with key, lifecycle/status where present, natural-key values), additive and versioned. Red-first test: a seeded Planning + Records pair appears; the pre-existing snapshot fields are byte-identical for a vault without collections.
- [x] 5.2 `tests/test_lifecycle_routing_journey.py`: generic batch-production fixture (Planning collection keyed on `[title]`, Records collection keyed on `[occurred_on, title, event_type]` with a `join` to it, plus one unbound twin collection). Journey: seed queued deliverables → append `produced` events for three → assert the family fires on exactly those three and the carrier block on the third append reports it → triage the three to `completed` → assert cleared by state change (no dismissal recorded) → append one more event on a fourth → dismiss → re-run passes → assert dismissal holds → append a second event on the fourth → assert resurfaced. Snapshot both collections through the projector at the end and assert the expected durable state.
- [x] 5.3 Mechanism removals recorded in `.task/RESULT.md`: disable the natural-key derivation (journey duplicates), remove the join (family silent), remove the family from `DELTA_CATEGORIES` (carrier stale until reconcile) — each goes red. Round 1 adds three: turn off the serve-time survivor filter (an external reader is served a finding built from a record withheld from them), revert the `PAGE_DELTA_CATEGORIES` split to `DELTA_CATEGORIES` (an ordinary page write deletes a live collection-pair finding), and remove the natural-key check from `update_record` (an update silently creates the twin append refuses forever). Evidence in `.task/evidence/round1/5.3-removal-{4,5,6}-*.txt`. Round 2 adds two more: key outcome entries by `(item_path, item_key)` alone instead of per binding (two Records collections bound to one plan item collapse into one entry, and the write-time block offers a fingerprint no audit ever produces, so a dismissal taken against it binds to nothing), and restore the second `_unbucket` definition (a malformed list-shaped bucket raises inside the page delta, costing that write its page-category updates and its `writes` bump). Evidence in `.task/evidence/round2/5.3-removal-{7,8}-*.txt`.

## 6. Closure (design D9)

- [x] 6.1 `add-planned-vs-recorded-review` task 7.1: `review_memory` `mode` docstring documents `plan-progress`; `plan_memory` description documents the inventory form; regenerate `tests/fixtures/mcp_tool_schemas.json`, `src/exomem/tool_surface_contract.json`, the v1 release-identities fixture, hosted generated locks/directory packets, and the ChatGPT plugin contract pending digest (`refresh_required: true`) via the scripts `docs/remote-quickstart.md` names. Record the old and new digests here: `30d2e420473b188e647aa4bff443d297395f6add53757bc098cf1c529485033b` → `bb0c6cf8da9dff993506fe7919ce0901b09d30a3046536f84f7de47bdb6a3af0`.
- [x] 6.2 Tick 7.1 in that change, refresh its stale `MODIFIED` block against the current canonical `records` spec, `openspec validate --all --strict`, `openspec archive add-planned-vs-recorded-review --yes`, validate again, `scripts/check_openspec_archive_discipline.py` → `ok`.

## 7. Gates

- [x] 7.1 Gate 1: the new and touched suites (`test_record_mutation`, Planning suite, `test_prominence`, `test_epistemic_bootstrap_contract`, `test_bootstrap_compact_budget`, the hook test, the due-state and review-state suites, `test_lifecycle_routing_journey`).
- [x] 7.2 Gate 2: tool-surface / plugin sync / hosted marketplace release / scaffold no-leak / skill sync / attention / epistemic inbox suites.
- [x] 7.3 Gate 3: `tests/test_latency_gate.py` + `tests/test_semantic_write_latency_gate.py`.
- [x] 7.4 `uvx ruff check . --select F`: clean. `openspec validate --all --strict` and `scripts/check_openspec_archive_discipline.py` both pass. `uvx ruff check . --select I` reports **169 pre-existing** unsorted-import findings repo-wide, none attributable to this change — the four that were mine are fixed (173 → 169) and the three remaining rows in touched files are on lines this diff never opens (round 1 re-ran it: still 169 repo-wide, and of the three `--select I` rows inside files this change authored, all three are present unchanged at the base commit; one row that round WAS mine — a `dataclasses` / `contextlib` inversion in `due_state.py` — and it was fixed). The `--select I` repo-wide count is recorded as repo lint debt rather than a per-change gate; this box gates on `--select F` clean plus the two OpenSpec/archive checks. Evidence in `.task/RESULT.md` (G5), `.task/evidence/gate5-ruff.txt`, and `.task/evidence/round1/gate5-ruff.txt`.
- [x] 7.5 Gate 4: the lean suite once, single process, quiet box, no early stop.
