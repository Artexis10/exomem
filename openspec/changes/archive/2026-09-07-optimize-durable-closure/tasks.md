## 1. Current-path evidence

- [x] 1.1 Reconcile the eight historical notes with installed code, current source, incident ledger/logs and live queue state; record a content-free investigation with exact time bounds and measurement limitations.
- [x] 1.2 Add a reusable ledger workflow decomposition with overlap-safe server occupancy and unknown client attribution; prove synthetic overlapping/refused/malformed-row cases in `tests/test_durable_closure_ledger.py`.
- [x] 1.3 Establish deterministic current graph/watcher and media-drain reproductions before fixes; retain red output and source anchors in the task evidence.

## 2. Keep the workflow usable

- [x] 2.1 Add red-first ordinary hybrid recall under resolver lag, strict relation-query and corrupt-pending regressions; implement optional graph degradation and pass affected retrieval suites.
- [x] 2.2 Add red-first proof that media process/retry never invoke the deferred drain, plus bounded selected-path batch/partial-failure cases; implement the async terminal and pass media/public-surface suites.
- [x] 2.3 Repair diagnosed graph/watcher amplification while retaining unknown-external-edit and lineage fences; demonstrate a burst converges exact current state without per-write full rebuilds in scoped graph/watcher tests.
- [x] 2.4 Transfer observed extraction/failure/pending media publications to the service parent through bounded durable results and monotonic claim fencing; prove child handoff stops before later stages, exact parent recovery preserves newer receipt debt, and same-process deferred fanout retains local publication custody.

## 3. Durable closure benchmark and orchestration

- [x] 3.1 Build a persistent public-MCP workflow benchmark with disposable state, realistic life-admin fixture, separate optimized/stress and model-free/real-extraction variants, graph/scan instrumentation and honest per-layer metrics; prove failure on an injected call-level refusal and success with optional graph-warming hits in `tests/test_durable_closure_benchmark.py`.
- [x] 3.2 Audit mutation result fields and portable guidance; document generic dependency ordering, independent reads/media, combined authoring and one bounded final verification; verify guidance and generated contracts with their existing tests.
- [x] 3.3 Run before/after workflow samples at approximately 3,800 and 8,000 pages with production watcher/graph paths and declared optional-model configuration; retain machine-readable results with final exact-state assertions.
- [x] 3.4 Run an internal diagnostic of the shared persistent-MCP Markdown subset against immutable Basic Memory 0.23.2 bytes and Exomem with identical inputs/config provenance; report acknowledgement/read/search convergence and capability differences. Competitive-suite publication remains subject to the existing fairness programme's paired own-harness/direct rows and review.

## 4. Review and delivery

- [x] 4.1 Obtain author-independent diff review and rerun important reproductions in isolated state; resolve blocking findings to APPROVE.
- [x] 4.2 Run the full test corpus once at the completion boundary, public-artifact privacy validation and `openspec validate --all --strict`; fix attributable failures and record actual outputs.
- [x] 4.3 Integrate current remote main safely, commit intended changes, push and open a ready pull request with measured results and validation evidence.
- [x] 4.4 After separately authorized merge and shipping evidence, synchronize the delta specs and archive through OpenSpec, validating strictly before and after; do not claim rollout completion before this evidence exists.

Delivery evidence: implementation merged in [PR #1101](https://github.com/Artexis10/exomem/pull/1101) at `0aa3b7e5`, after [CI on the reviewed head](https://github.com/Artexis10/exomem/actions/runs/34121289480) passed. OpenSpec synchronized nine added requirements and archived this change through the pinned CLI. Source delivery is complete; production deployment remains separate.
