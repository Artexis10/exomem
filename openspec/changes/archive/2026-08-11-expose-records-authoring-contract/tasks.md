## 1. Contract and red-first acceptance

- [x] 1.1 Add strict OpenSpec deltas for agent-facing discovery, read-only validation, inventory, selector safety, and compact bootstrap.
- [x] 1.2 Add empty-vault generic-client tests covering bootstrap, describe, validate, create, inspect, and append with no fixture manifest.
- [x] 1.3 Add focused tests for actionable closed-enum errors, governance-filtered inventory, and zero mutation during validation.

## 2. Records authoring discovery

- [x] 2.1 Implement the deterministic content-free manifest contract and generic minimal/laboratory examples.
- [x] 2.2 Implement shared read-only create preflight and reuse it inside guarded create.
- [x] 2.3 Implement bounded collection-less inventory while preserving targeted first-class and legacy inspection.

## 3. Generated surfaces and bootstrap

- [x] 3.1 Add and classify `describe` and `validate` across the canonical command registry, MCP, REST, CLI, and OpenAPI.
- [x] 3.2 Keep bootstrap product-facing and bounded while advertising the agent workflow and exact contract route.
- [x] 3.3 Regenerate tool schemas/capabilities and advance only the pending connector fingerprint required by the intentional surface change.

## 4. Verification and delivery

- [x] 4.1 Run focused Records, bootstrap, registry, governance, and writer-lease tests.
- [x] 4.2 Run strict OpenSpec validation, Ruff F checks, and package/schema gates locally; use the full Python suite in CI as the authoritative gate.
- [x] 4.3 Run an independent review over contract fidelity, read-only guarantees, governance, and generated-surface scope.
