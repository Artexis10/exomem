## ADDED Requirements

### Requirement: Role and transient-state replay tests require delivered signals and state resolution

The benchmark SHALL define immutable ordinary-conversation replay families for same-topic artifact-role promotion and transient-state hygiene, with protocol-only, already-promoted, historical and different-episode controls. The positive role replay SHALL cover two distinct reusable methods and multi-source synthesis. Acceptance SHALL verify the signal on an actually served carrier after the qualifying write, then a correct governed proposal or correction consistent with authority and eventual resolution by represented state. Source history and exact unit provenance SHALL be preserved; fabricated dismissal SHALL NOT count as resolution. Fixture introduction SHALL use a new pending amendment after sequence four without mutating prior receipts. Pending families SHALL remain withheld at load, evaluation and comparison-manifest gates; incomplete or guard-blocked native runs SHALL remain harness findings without a product score.

#### Scenario: A projected but undelivered candidate is insufficient
- **WHEN** a candidate exists only in internal projection and no client-visible carrier returned it
- **THEN** the delivered-signal acceptance fails

#### Scenario: Ordinary input separates promotion from protocol
- **WHEN** the immutable positive episode reports reusable success and a twin contains only an experimental protocol
- **THEN** only the positive episode is expected to surface a role signal, without adding storage commands to either user transcript

#### Scenario: Resolution preserves source material
- **WHEN** the agent extracts a represented method and corrects transient wording through governed operations
- **THEN** snapshot assertions verify exact supporting provenance, retained history and cleared findings without recording a synthetic dismissal

#### Scenario: Pending amendments stay outside comparative scoring
- **WHEN** the new amendment lacks acknowledgment or a native run stops before its required turns complete
- **THEN** no comparative product score is emitted for that family
