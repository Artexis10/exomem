## 1. Pure evidence predicates

- [x] 1.1 Add red-first generic paired fixtures in `tests/test_artifact_role_review.py` for same-topic reusable success, two distinct method groups, attributed synthesis and every D2 negative control; verify each positive fails without its defining cue or provenance.
- [x] 1.2 Implement the pure role detector in `src/exomem/artifact_role_review.py` using the pinned D2 cue grammar and unambiguous context/exact-reference joins; verify shared batch context cannot lend success to a second method and the task 1.1 scope passes without filesystem, SQLite or model access.
- [x] 1.3 Add red-first single-episode pending/result, authored chronology, undated coexistence, historical, quoted, conditional, future-trial, taste-versus-safety and ambiguous multi-trial cases; implement the finite D4 subject/episode grammar and verify no test relies on line order, mtime or invented unit status.
- [x] 1.4 Implement deterministic material fingerprints and D5 limits; verify unrelated edits and duplicate units preserve identity, changed evidence resurfaces, ordering is stable and exhausted input reports unknown/capped coverage.

## 2. State-based role coverage

- [x] 2.1 Build derived support from existing corpus eligibility, exact current document unit resolution and line-ending-only substantive-content matching, including supported compiled types in custom Notes subdirectories; test correct-links/wrong-content, changed case-sensitive identifiers/internal indentation, missing/stale/duplicate anchors, role mismatch, parent-only links, unsupported kinds and retained origin history.
- [x] 2.2 Implement audience-relative predicate and coverage recomposition while retaining complete shared dependency descriptors; test private synthesis sources, private destinations, eight private supports plus a ninth public one, source aliases, source-generation/policy changes and hidden counts before and after reconcile.
- [x] 2.3 Implement settlement and reopening for origin rewrites, valid supersession, result retraction, destination edits/deletion/rename and inactive lifecycle; verify partial method coverage leaves the second method unresolved and no dismissal is written.

## 3. Audit, delta and carrier integration

- [x] 3.1 Add audit adapters and register both categories; verify actual audit output equals the pure detector result and existing topic-promotion findings remain unchanged.
- [x] 3.2 Extend `due_state.py` with the additive projection version, page-delta categories, complete lightweight dependency descriptors and shared support epoch; compare write-delta state with reconcile across origin/source/destination changes, restart and stale-generation fixtures.
- [x] 3.3 Bound dependent-origin updates to eight per written page and invalidate overflow in constant cost through the support epoch; test thousands of dependents, incomplete edge extraction, explicit audit/review `meta.coverage` with zero findings, compact omission without shape changes and no extra corpus walk, destination read, Records census or model call on synchronous writes.
- [x] 3.4 Register default attention, dispositions and exact-fingerprint triage behavior; test normal/quiet/off, explicit all-state access, first-surfaced recording and unchanged review decisions after unrelated edits.
- [x] 3.5 Exercise actual mutation/recall/bootstrap carriers within existing count, reference and byte ceilings; inject detector exceptions and assert unchanged committed receipts, stale-advice omission and bounded request latency.
- [x] 3.6 Remove each family from page-delta maintenance in a mutation-removal probe; verify immediate maintenance stops while full reconcile still restores the finding, demonstrating that the production seam is exercised.

## 4. Agent guidance and contract parity

- [x] 4.1 Add the compact role/state handling clause to bootstrap and detailed generic examples to the hand-authored scaffold's relevant workflow references; test role-first home selection, Records handoff, existing restructure authority, authorized local edits and the tool-free-turn limitation.
- [x] 4.2 Synchronize shipped plugin skill copies using repository conventions; run plugin/scaffold checks and measure bootstrap before/after against the current unchanged ceiling.
- [x] 4.3 Verify tool schema fidelity, tool-surface fingerprint, terminal envelope, CLI/REST/MCP parity and generated hosted contracts; refresh generated bytes only where the authored contract actually changes, preserving registered app and benchmark receipt identities.

## 5. Immutable replay acceptance

- [x] 5.1 Allocate the next free two family identifiers and a new pending amendment after sequence four; author generic immutable positive/twin conversation fixtures and artifact seeds under `benchmarks/epistemic`, with red-first storage-command and turn-drift checks.
- [x] 5.2 Add discriminating assertions for actual carrier delivery, role-appropriate represented provenance, retained history and state settlement; verify internal-only signals, unrelated destinations, false writes and synthetic dismissals fail their paired cases.
- [x] 5.3 Wire registry, receipt-chain, loader, evaluator and comparison-manifest withholding; verify unacknowledged families cannot contribute scores and all earlier receipt identities remain unchanged.
- [x] 5.4 Run development replays in each supported native arm on safe isolated state, recording completion, boundary findings and artifacts; classify isolation faults or incomplete turns as harness findings with no product score, and never bypass the isolation guard.

## 6. Completion and delivery

- [x] 6.1 Run fixture-vault journeys through actual MCP-facing tools: qualifying experiment write → delivered role/state signal → governed extraction/correction under authority → repeated reconcile → resolved state with origin history and exact source-unit provenance intact; include a private-support audience twin and record observable results.
- [x] 6.2 Obtain independent review of detector controls, audience-dependent support, projection freshness, authority and synchronous cost; correct findings and rerun only affected scopes, recording the final review verdict.
- [ ] 6.3 Run the affected suites during iteration and the full core/harness corpus at completion, with failure-name comparison to the exact base; run CI lint/type, privacy, capabilities, schema/hosted/skill and pinned OpenSpec strict checks, recording counts and exact revision.
- [ ] 6.4 Deliver through a committed, pushed ready PR; after authorized merge, synchronize canonical specs and archive with the pinned OpenSpec CLI, validate strictly before/after, verify the merged revision and retire the clean owned worktree.

Post-deploy acceptance is an optional operational follow-up: after installing a release containing this implementation, inspect the new families through governed tools on the running cell. Fixture acceptance and merge evidence do not establish live deployment. Do not mark this follow-up as completed without observed live results.

## Verification evidence

- Product detector/projection/carrier scope: 154 passed. Governance, receipt, registry and replay scope: 93 passed. Compatibility, schema, terminal and plugin scope: 140 passed.
- Independent review approved implementation `7457ad9dfcce1c2b375fbfd15e9a2af3c34baef0` after all five reproduced findings were corrected. The permitted 400-unit case measured 0.058 seconds for actual write delta and 0.010 seconds for serving. Release-only integration is `b3aadc694d2936871207f4acf94548adf6250800`.
- Temporary-vault MCP journeys delivered role/state findings on writes, bootstrap and recall, then settled represented method/synthesis extraction and guarded pending correction after repeated reconcile while preserving source text, references and fingerprints. Audience-filter twins covered private sources and destinations.
- Six native replay corpora were attempted in both supported arms. All 12 stopped at the existing ancestor Git isolation guard before a turn; these are harness findings with no product scores. The sequence-five amendment remains pending and withheld. Prior receipt bytes are unchanged.
- Pinned OpenSpec validation: 196 passed. Global Ruff correctness, targeted types, capabilities, package build and the public-artifact privacy gate passed. Full core/harness CI and merge/archive evidence belong to tasks 6.3 and 6.4.
