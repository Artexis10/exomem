## Lane A — Backend core (run store, state machine, command surface)

- [x] A.1 Red-first `tests/test_adoption_run.py`: `test_start_requires_kb_and_initialize_kb_flag_bootstraps`, `test_start_snapshots_candidate_inventory_and_fingerprint`, `test_select_validates_paths_and_invalidates_plan`, `test_plan_previews_exact_targets_frontmatter_and_hashes`, `test_apply_requires_current_plan_id_and_selection_hash`, `test_apply_copies_with_provenance_and_updates_indexes`, `test_apply_partial_failure_records_per_item_outcomes`, `test_apply_is_idempotent_and_retry_failed_replans_subset`, `test_interrupted_apply_is_visible_and_recoverable`, `test_cancel_rules`, `test_status_flags_stale_selection`, `test_finish_runs_recall_check_and_first_question` (byte-snapshot originals-untouched throughout).
- [x] A.2 Refactor `src/exomem/adopt.py`: extract `plan_import_items` (pure) + `commit_import_items` (single `batch_atomic_write`) from `_copy_as_sources`; expose the run-manifest helpers. Keep `_copy_as_sources`'s external result shape; `tests/test_adopt.py` stays green.
- [x] A.3 Implement `src/exomem/adoption_run.py`: `AdoptionRunStore` (canonical `run.json` written only via `batch_atomic_write`, module `_LOCK`), id/fingerprint helpers, `build_inventory`/`probe_staleness`, and the seven actions (`start`, `status`, `select` with server-side `{include, exclude, overrides, include_junk}` materialization + per-path validation, `plan`, `apply` with write-time re-validation + single validated-subset batch + phase recompute, `cancel`, `finish`).
- [x] A.4 Add `context_refs.adoption_run_ref`.
- [x] A.5 Add `op_adoption_studio` to `src/exomem/commands.py` (ten-action dispatch, Google-style Args for every param), the `_PRODUCT_SPEC` registry entry (`routes=("adopt",)`, `first_run_safe=True`), and `_ADOPTION_STUDIO_READ_ONLY_ACTIONS` + the `invocation_is_read_only` branch.
- [x] A.6 Red-first `tests/test_adoption_studio_surface.py`: `test_registry_entry_and_route_validation`, `test_read_only_action_classification`, `test_rest_facade_round_trip`, `test_mutation_replay_is_idempotent`.
- [x] A.7 Golden schema: run `tests/test_mcp_schema_fidelity.py`, confirm the failure names exactly `adoption_studio`, `review_memory`, `triage_memory`, `review_item_context`; regenerate `tests/fixtures/mcp_tool_schemas.json` via the test's own helpers; commit with an explicit intentional-change note; gate green.

## Lane B — Proposals / agent contract (depends on Lane A run-store interface, file-disjoint after)

- [x] B.1 Red-first `tests/test_adoption_proposals.py`: `test_work_item_is_bounded_and_read_only`, `test_propose_validates_each_kind`, `test_propose_binds_run_fingerprint_and_source_hashes`, `test_propose_never_touches_markdown`, `test_review_memory_adoption_mode_lists_open_items`, `test_triage_adoption_ref_round_trip_and_resurfacing`, `test_review_item_context_adoption_dispatch`, `test_apply_proposal_compilation_routes_through_remember`, `test_apply_proposal_relation_requires_expected_hash_and_refuses_drift`, `test_apply_proposal_supersession_uses_replace_cas`, `test_apply_proposal_stale_binding_refuses`, `test_apply_proposal_requires_fingerprint_and_why`.
- [x] B.2 Implement `src/exomem/adoption_proposals.py`: ref namespace + `review_state`-based identity/fingerprint; `work_item` (bounded, read-only, `corpus_aware.suggest_related` for existing context); `propose` (two-phase validation, writes only `proposals.json`, invalid persisted with findings); the five proposal kinds with governed apply routing (`op_remember` / `op_link` / governed `edit` / `op_replace_memory` / reconciliation sub-kind); `build_queue`, `triage`, `assemble_context`, `apply_proposal`.
- [x] B.3 Wire the existing-verb dispatch in `commands.py`: `op_review_memory` (`mode="adoption"`), `op_triage_memory` (`is_adoption_ref`), `op_review_item_context` (adoption refs). Extend each docstring (intentional golden change, folded into A.7).
- [x] B.4 Add the zero-argument `continue_adoption` MCP prompt and the `exomem://adoption/run/<id>` MCP resource (with `list_changed` on creation) in `src/exomem/server.py`.

## Lane C — Studio UI (`?view=adopt`, starts against Lane A contract with a mocked adapter)

- [x] C.1 Asset bumps + manifest: rename `app.v3.js→app.v4.js`, `state.v1.js→state.v2.js`, `styles.v1.css→styles.v2.css`; add `adoption.v1.js` + `adoption-model.v1.js`; set `manifest.json` version 4. Update `tests/test_studio_routes.py` (manifest version/set + add the two adoption assets to the inertness concatenation: no vault strings, no `http(s)://`, no `localStorage`).
- [x] C.2 `state.v2.js` route model (`view`, `run`, `astep`), emit-only-when-non-default so legacy review URLs serialize byte-identically; static markup for all nine screens in `index.html`; adoption styles appended to `styles.v2.css` (reuse existing tokens; no inline styles/scripts per CSP).
- [x] C.3 Red-first `tests/test_studio_adoption_ui_model.py` (node-eval): tri-state math incl. nested override + junk default-off; `selectionCounts`/`planBullets` exact sums; `countLine`; `legalStep`/`phaseScreen` incl. unknown-phase safe screen; `failureGroups` code map + unknown-code server-reason fallback; `state.v2` round-trip incl. byte-identical legacy review URLs; `pollDelay` schedule.
- [x] C.4 Implement `adoption-model.v1.js` (pure) then `adoption.v1.js` controller against the single `adoptionApi` adapter object; screen renderers, `pollRun` `setTimeout` chain, reuse of `#action-dialog` and `guardedWrite`-style drift handling; guarantee badge + per-step contract lines + honest verification line.
- [x] C.5 Red-first `tests/test_studio_adoption_flows.py` (TestClient, against landed Lane A commands): create→status inventory + scan mutates nothing (byte-compare); select+preview mutate nothing; apply copies with provenance + originals byte-identical + verified counts; partial (`.png` + deleted-after-scan) coded skips; stale (touch a scanned file) → 409 `ADOPTION_SOURCE_CHANGED`, vault byte-identical; cancel → `cancelled`, no writes; retry `scope:"failed"`; proposals in review-item shape, reject removes on re-read, approve writes + `log.md` entry, drift 409 leaves target untouched.
- [ ] C.6 Red-first `tests/browser/adoption.spec.mjs` (Playwright, mock `/api/*`): happy path start→scan→findings→choose→preview→confirm→apply→verify→skip organize→ask→done (guarantee badge on every step; no write-class call before dialog confirm); stale re-check preserves selection; partial "Try those 14 again" issues retry with `scope:"failed"`; cancel dialog during apply; handoff Copy button + 120 s timeout notice (mock clock); suggestions approve/reject incl. drift refresh; deterministic-only run never calls handoff/proposal endpoints; 390×844 viewport; keyboard-only folder selection.

## Lane D — Hosted entrypoint (touches `hosted_transfer_routes.py` + its tests only)

- [x] D.1 Red-first hosted tests: `adoption_studio` admitted by generic dispatch (read/write via `invocation_is_read_only`), NOT in the intercept set; ZIP with a traversal entry rejected and confined under `_Staging/adoption/<run_id>/`; entry-count and total-size caps enforced; `adoption_studio(start, path="_Staging/adoption/<run_id>")` scans staged files identically to a local subtree.
- [x] D.2 Implement the staging landing + cell-side ZIP expansion (zip-slip protection via `resolve_under_vault`, entry/size caps) in `src/exomem/hosted_transfer_routes.py`; land raw files under vault-relative `_Staging/adoption/<run_id>/` outside `Knowledge Base/`. Do NOT add `adoption_studio` to the intercept set; do NOT edit `writer_lease.py`.

## Integration verification

- [ ] V.1 Real-vault end-to-end journey via `exomem studio` on a messy sample vault: start → scan → findings → choose → preview → apply → verify → (skip) organize → ask → done, with byte-snapshot proof that originals are untouched at every step.
- [x] V.2 Deterministic-only journey completed with NO assistant connected; `finish` `recall_check.ok` true on the sample vault.
- [x] V.3 `openspec validate --all --strict`, `pytest tests/test_scaffold_no_leak.py -q` (all new UI strings clear the scaffold-leak gate), the full 29 new tests + existing suites (`pytest -q` lean), the latency gate, the golden-schema gate naming exactly the four intended tools, and Ruff on changed files.
- [ ] V.4 Confirm the branch diff is within the scope allowlist (`tests/golden/` untouched except the documented regen; `.github/`, `writer_lease.py` untouched) and hand the Substrate Home integration spec to the concurrent hosted session.
