# RED fixture: family_id outside the pre-registered §1 family table.
scenario_id: red-unregistered-family
family_id: f99
kind: corpus
public_coverage: none
phases:
  - phase_id: p1
    ops:
      - op: snapshot
        ref: s1
    expect:
      - assert: uncertainty_declared
        subject: claim-1
fairness:
  why_neutral: red fixture; never scored
  public_coverage_subtraction: none
  mechanisms:
    - provider_role: subject
      mechanism: none
      verdict: unknown
      evidence: benchmarks/epistemic/PREREGISTRATION.md:14
  privileged_endpoint_check:
    - driver_tool: read-only state projection
      competitor_equivalent: documented list/read endpoint
  acceptance_predicate: none
