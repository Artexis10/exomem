# RED fixture: catastrophic_if_failed escalates a REGISTERED assertion that is
# not in the frozen §3 catastrophic set. The §3 set is frozen with the
# pre-registration hash; a scenario may not widen it.
scenario_id: red-catastrophic-escalation
family_id: f03
kind: corpus
public_coverage: none
phases:
  - phase_id: p1
    ops:
      - op: snapshot
        ref: s1
    expect:
      - assert: contradiction_visible
        subject: claim-a
        counterpart: claim-b
    catastrophic_if_failed:
      - contradiction_visible
fairness:
  why_neutral: red fixture; never scored
  public_coverage_subtraction: none
  mechanisms:
    - provider_role: subject
      mechanism: none
      verdict: unknown
      evidence: benchmarks/epistemic/PREREGISTRATION.md:39
  privileged_endpoint_matrix:
    - driver_surface_id: state.read
      provider: fixture
      variant: native
      disposition: equivalent
      audit_scope: read-only projected state
      evidence: benchmarks/epistemic/PREREGISTRATION.md:32
      reason: The fixture projects only documented neutral state.
      competitor_surface: documented list/read endpoint
  acceptance_predicate: none
