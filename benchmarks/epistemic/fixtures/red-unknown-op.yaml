# RED fixture: uses an operation outside the pre-registered vocabulary.
# Loading this file MUST fail before any provider runs.
scenario_id: red-unknown-op
family_id: f12
kind: operational
public_coverage: none
phases:
  - phase_id: p1
    ops:
      - op: rewrite_history
        ref: nope
      - op: snapshot
        ref: s1
    expect:
      - assert: external_edit_authoritative_within
        subject: file-1
        freshness_bound_s: 60.0
fairness:
  why_neutral: red fixture; never scored
  public_coverage_subtraction: none
  mechanisms:
    - provider_role: subject
      mechanism: none
      verdict: unknown
      evidence: benchmarks/epistemic/PREREGISTRATION.md:39
  privileged_endpoint_check:
    - driver_tool: read-only state projection
      competitor_equivalent: documented list/read endpoint
  acceptance_predicate: none
