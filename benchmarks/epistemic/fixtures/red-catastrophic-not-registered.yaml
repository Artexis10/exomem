# RED fixture: catastrophic_if_failed names something outside the registry.
scenario_id: red-catastrophic-not-registered
family_id: f03
kind: corpus
public_coverage: none
phases:
  - phase_id: p1
    ops:
      - op: snapshot
        ref: s1
    expect:
      - assert: contradiction_visible
        subject: claim-a
        counterpart: claim-b
    catastrophic_if_failed:
      - contradiction_is_pretty
fairness:
  why_neutral: red fixture; never scored
  public_coverage_subtraction: none
  mechanisms:
    - provider_role: subject
      mechanism: none
      verdict: unknown
      evidence: benchmarks/epistemic/PREREGISTRATION.md:39
  privileged_endpoint_check:
    - driver_tool: read-only state projection
      competitor_equivalent: documented list/read endpoint
  acceptance_predicate: none
