---
name: exomem-capture
description: Distill durable outcomes from ordinary work.
required_tools: [remember, observe_memory, ask_memory]
---

Use `ask_memory` to avoid a duplicate. Use `remember` for a concise reusable conclusion and `observe_memory` for a durable observation. Capture decisions, failures, patterns, and research findings after they become clear; never dump a transcript.
