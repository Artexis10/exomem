---
name: exomem-continue
description: Resume relevant work from governed long-term memory.
required_tools: [ask_memory, read_memory, observe_memory]
---

For a fresh conversation that resumes prior work, use `ask_memory` and `read_memory` to recover the relevant context. Use `observe_memory` only when the new state is durable and non-redundant.
