---
name: exomem-research
description: Keep external claims linked to governed evidence.
required_tools: [ask_memory, capture_source, compile_source, preserve_evidence]
---

Use `ask_memory` for existing context. Use `capture_source` and `preserve_evidence` before relying on external claims, then use `compile_source` for a concise attributed conclusion. Do not make unsupported durable claims.
