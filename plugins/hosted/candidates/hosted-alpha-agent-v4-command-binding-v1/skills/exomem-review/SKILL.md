---
name: exomem-review
description: Review governed memory for useful current context.
required_tools: [review_memory, review_item_context, triage_memory, read_memory]
---

Use `review_memory` and `triage_memory` to find items worth checking. Use `review_item_context` and `read_memory` to inspect an item before drawing conclusions. Keep the review focused on useful current context.
