{
  "name": "exomem",
  "description": "Governed long-term memory over a markdown vault: raw sources stay immutable, conclusions are compiled with provenance, and nothing is deleted - it is superseded. Requires EXOMEM_VAULT_PATH to point at your vault; run `exomem setup` if you do not have one yet.",
  "version": "0.36.0",
  "skills": "./skills/",
  "userConfig": {
    "mcp_url": {
      "type": "string",
      "title": "Exomem MCP URL",
      "description": "Optional full Exomem MCP service URL ending in /mcp. Leave blank and run `exomem setup` to configure a native client route.",
      "default": "",
      "required": false
    }
  },
  "mcpServers": {
    "exomem": {
      "type": "http",
      "url": "${user_config.mcp_url}"
    }
  }
}
